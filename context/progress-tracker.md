# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

- Phase 7: Quality, Cost, and Internal Use — Units 19–21 done; Units 24–26 (delivery, per-browser ownership, abuse & cost controls) done. Unit 27 (Admin Panel) verified live. Unit 28 (User Authentication) **implemented + verified live** (dev path + live Supabase Google OAuth). **Supabase go-live (2026-07-14):** DB migrated from local Postgres to Supabase Postgres and auth switched to Supabase Google OAuth — both verified end-to-end. Mock remains the default Twitter/LLM provider. (Phase 5 live integrations 16–18 confirmed live end-to-end earlier — see history below.)

## Current Goal

- Unit 41 (Scoring v3 — "audience-honest"): rebuild scoring around "the best creator is the one whose real, engaged audience is most made of the brand's target customers — same rule for everyone, no identity boosts." Per `context/specs/41-scoring-v3-audience-honest.md`. **Phases A + B + C1 + C2 + D shipped + verified** (`pnpm build` 14 projects + `pnpm check` all green; `scoring-v3` 48/48; report UI screenshotted). **A** core purge · **B** expected-reach dial · **C1** geography algorithm · **C2** live OpenAI region + valued-region inference · **D** report surface (reach + geo dials, "matched against" transparency, honest metric regrouping). **Remaining (2 items, both flagged to the user):** (1) the fully *editable* infer→confirm override (brand edits target buckets + valued regions inline) — needs a Prisma migration + form + UX steer, so deferred as its own slice; the transparency half ("matched against" + re-run) shipped. (2) a live paid run to verify C2's real-provider region/valued-region inference end-to-end. Optional polish: feed reach/geo into the LLM narrative prompt. See Completed.

## Completed

- Unit 41 Phase C2 — region / valued-region inference **verified against the live provider** (paid) — 2026-07-20. Closes the last open v3 verification item; this path had shipped mock-verified only. Probed the real `openai` provider (`gpt-5-mini`) directly rather than driving the full pipeline, which isolates the untested layer at a fraction of the cost. **Results, all correct:** (1) `classifyOrgProfile` on a stablecoin-remittance brief returned `valuedRegions: ["subsaharan_africa","latam","southeast_asia"]` — the model reasoned from product economics as the prompt intends, not from the brand's own location; (2) `classifyAudienceAccounts` placed 5/6 accounts correctly (Lagos→`subsaharan_africa`, Buenos Aires→`latam`, Manila→`southeast_asia`, Berlin→`western_europe`, NYC→`north_america`) and correctly returned `unknown` for the account with no location rather than guessing from its bucket, which is the explicit prompt instruction; (3) `geoTiltFactor` on those live values returned **factor 1.025** (coverage 0.83, valuedShare 0.6) — **active, not the neutral 1**, so the geo dial demonstrably moves the score with real data. **The specific risk that made this worth paying for is now retired:** `nullableRegion` in the hand-authored strict schema is an enum containing `null` (12 regions + null), which only the live Structured Outputs endpoint can accept or reject — it accepts it. Pipeline wiring (`org: orgClassification` → `scoreAnalysis` → `input.org.valuedRegions`) re-read and confirmed. One false alarm along the way was the probe's own fault, not the app's: passing `source:'like'` instead of the `REPLY|QUOTE|RETWEET|FOLLOWER` enum made the provider's identity-fields-come-from-input guard reject every row — worth knowing, since the failure surfaces as a generic "Model output failed schema validation after retries".
- Test-data reset to zero — 2026-07-20. Deleted the last remaining analysis (`meshapi_ai/tanmayjain5114`, the one genuine v3 run) at the user's request, backed up to JSON first. DB now holds **0 AnalysisRequest / 0 Report** rows ahead of production traffic. `ProviderUsageLog` (66 rows) survived via `onDelete: SetNull`, and all 1669 `ProviderCache` rows were left untouched.
- Algorithm-aware reuse (`scoringVersion`) + pre-v3 data purge — durable fix for the staleness class of bug — 2026-07-20. Closes Next Up 0 and 0b. **Why:** the 2026-07-18 fix disabled instant reuse (`ANALYSIS_REUSE_WINDOW_SECONDS=0`) to force v3 re-scoring, which was never meant to ship — reuse sits in front of both `checkTierGate` and `checkAnalysisRateLimit` (route.ts:157–181), so at `0` every repeat submit re-ran the full 5–7 min pipeline *and* burned a lifetime analysis (worse after the 12 → 10 drop). Root cause was that `findReusableAnalysis` matched on **inputs only**, with no notion of which algorithm produced the report — so identical inputs kept returning the previous algorithm's score. **Fix:** new `SCORING_VERSION` constant in `@kol-fit/shared` (`constants.ts`, currently **3**), a nullable `Report.scoringVersion` column (`prisma db push` — additive, no migration history in this project), stamped by the worker on every report write (`analysis-run.ts`), and matched by `findReusableAnalysis` alongside `status: COMPLETED`. A scoring change now only needs a `SCORING_VERSION` bump — reuse invalidates itself, **no DB purge and no manual cache bump ever again**. Deliberately NOT coupled to the classification cache `NS`: that layer caches the LLM audience/fit payloads, which a pure re-weighting leaves valid, so coupling would burn an OpenAI re-classify on every calibration tweak — `NS` stays a manual bump for classification **schema/prompt** changes only (the rule is documented at the constant). **Data:** deleted the **25 pre-v3 analyses** (all COMPLETED, 3 owners, all internal test data; backed up to JSON first) and the **152 orphaned `cls:v2`** cache rows; kept the single genuine v3 analysis and backfilled its `scoringVersion` to 3 (it predated the column, so it would otherwise never have been reusable). **All 1669 `tw:v2` rows preserved** — `ProviderCache` has no FK to `AnalysisRequest`, so creator profile/tweet data survives untouched and any re-run costs **zero TwitterAPI calls**. `ProviderUsageLog` is `onDelete: SetNull`, so cost/spend history (66 rows) survived too; `OrgProfile`/`KolProfile` cascade but rebuild free from the Twitter cache. 3 pre-v3 share links died with their reports (they served wrong scores). **Verified:** `pnpm build` (14 projects) + `pnpm check` 20 suites / 350 assertions, 0 failed, incl. 8 new `analysis-reuse` assertions pinning the query shape; and a live DB probe confirming reuse **hits** at version 3 and **misses** at a simulated version 4 (re-runs with the new algorithm). Also corrected a stale purge instruction in this tracker — the Twitter namespace is `tw:v2`, not `tw:v1`.
- Doc/copy consistency sweep after the 12 → 10 tier change — 2026-07-20. The commit that lowered the allowance left "12" in five doc spots, including a **self-contradicting Unit 34 spec** (title said 12, Design section said 10). Fixed the spec title + prose (noting the original 12 and the 2026-07-18 change rather than erasing the historical user direction), `project-overview.md`, `39-funnel-polish.md`, `40-retry-and-fair-quotas.md`, plus three `cls:v2` comments in the check scripts whose assertions already tested `cls:v3`. No runtime code touched.
- Unit 34 Tiered funnel — free account allowance 12 → 10, and tier copy derived from the limits — 2026-07-18. User-requested product change: `TIER_LIMITS.userLifetime` 12 → **10** (anon stays 3). Changed the **code default**, not just `FREE_TIER_USER_LIFETIME` — `.env` is gitignored, so an env-only change would not have reached production. **The number was duplicated in four places** and would have desynced: the constant, the `upgrade_required` API message (`tier-gate.ts`), and two spots in `analysis-form.tsx` — the wall heading ("all 12 included analyses") and a *derived* figure in the login wall ("unlock 9 more" = 12 − 3). Rather than retype the literals, the copy now **reads the server's resolved allowances**: `messageFor(gate, limits)` replaces the static `MESSAGES` map, and the form renders `anonLimit` / `accountLimit` / `signedInLimit`. That also fixes a pre-existing latent bug — the hardcoded copy lied to anyone who set `FREE_TIER_*_LIFETIME`, since those env overrides have always been supported. `GET /api/analyses/quota` gained `signedInLimit` (the authenticated allowance regardless of caller tier) because the anonymous login wall cannot otherwise compute "unlock N more"; `TIER_LIMITS` is now only a fallback for a quota fetch that hasn't landed, with the two walls taking **different** fallbacks so an unresolved quota at the upgrade wall can't claim the anonymous number. Updated `tier-gates.regression.cjs` boundary assertions (11→allowed/12→wall became 9→allowed/10→wall) + the defaults assertion, the Unit 34 spec, the `.env.example` comment, and stale "12 lifetime" comments in `route.ts` / `limits.ts` / `tier-gate.ts`. **Verified:** full `pnpm build` (14 projects, no errors/warnings) + `pnpm check` 20 suites, 0 failed (tier-gates 14/14). **Not verified by driving the UI** — reaching either wall needs an owner seeded with 10 non-FAILED `AnalysisRequest` rows in the shared Supabase DB, so the copy paths are covered by type-check + tests only.
- Unit 41 Scoring v3 — stale-result fix: reuse bypass + `cls:v3` cache namespace bump — 2026-07-18. User reported the app still served **pre-v3 scores** after the v3 ship. Root cause was two independent staleness layers, neither of which the v3 commit accounted for. **(1) Instant reuse (Unit 41 / `ANALYSIS_REUSE_WINDOW_SECONDS=30d`)** short-circuits `POST /api/analyses` at route.ts:163 and returns the saved `Report` row for a matching owner + pair + brief — so the v3 pipeline never ran at all. Set to `0` in `.env` (read per-request, restart-only, no rebuild). **This is a temporary testing state — restore to `2592000` once v3 output is validated**, otherwise every repeat submit re-runs the full 5–7 min pipeline and burns tier quota. **(2) Classification cache namespace was never bumped** — v3 (`5af9cc7`) added `AudienceClassification.region` + `OrgClassification.valuedRegions` but did not touch `packages/cache`. Keys hash *inputs*, not output shape, so pre-v3 payloads still hit; and because both new fields are `.optional()`, the defensive `safeParse` shape-mismatch fallthrough at llm-cache.ts:120 **cannot** catch them — a v2 payload parses clean and is served as a hit with no region data, silently zeroing `geoTiltFactor()` (`placed=0 → factor:1`, metrics.ts:286) and emptying the region dial. Bumped `NS` `cls:v2`→`cls:v3` with a comment recording *why the optional-field case defeats the safeParse guard*. Updated the two checks that assert the namespace (`classification-v2`, `cache-poisoning`) + a stale doc comment. Costs one OpenAI re-classify per handle on first touch; **TwitterAPI.io cost is zero** — all seven pipeline reads are cached 30d (`searchTweets`, the only uncached method, is unused by the pipeline). Orphaned `cls:v2:*` rows left to expire naturally (purgeable via `DELETE FROM "ProviderCache" WHERE key LIKE 'cls:v2:%'` — the `tw:v2:*` rows share the table, so the prefix filter is what protects the Twitter data). **Verified:** full `pnpm build` (14) + `pnpm check` all suites green, 0 failed. **Known gap, not fixed:** `findReusableAnalysis` (route.ts:79) matches on inputs + `COMPLETED` only, with no notion of which algorithm produced the report — so *every* future scoring change repeats this exact bug. See Next Up.
- Unit 41 Scoring v3 — removed the `fitModel` tier + dead-code sweep — 2026-07-18. **fitModel tiering removed:** `assessContentFit` now returns only the informational content-fit rubric (v3), so running it on a separate frontier tier (`LLM_MODEL_FIT`/`fitModel`/`fitClient`) was over-provisioned. Removed `fitModel` from `OpenAiProviderOptions`, the `fitClient` + `readonly fitModel` fields, the constructor branch, the `getUsageStats` merge, and the `assessContentFit` client override (now the main client); dropped the `LLM_MODEL_FIT` read in `factory.ts` and the `.env.example` entry; simplified the content-fit cache key in `llm-cache.ts` to `this.inner.model` (was `fitModel ?? model`). `fastModel` (bulk-audience cheap tier) is KEPT — a live cost optimization, not obsolete. **Dead-code sweep:** wrote a per-symbol unused-export scan (excluding generated Prisma + in-file-used symbols). Found + removed two genuinely-dead items: the **`ScoreMeter`** component (unused — superseded by `ScoreGauge`/`MetricGroups`; its file slimmed to just the still-used `ConfidenceChip` and renamed `score-meter.tsx`→`confidence-chip.tsx`, import updated), and the unused **`PRODUCT_POSITIONING`** constant. Re-ran the scan after: **zero remaining dead value exports** in hand-written source. The other "unused" hits are generated Prisma types, in-file helpers, or conventional `z.infer` type aliases paired with used schemas — not dead code, left as-is. **Verified:** full `pnpm build` (14) + `pnpm check` all green.
- Unit 41 Scoring v3 — removed obsolete pre-v3 scaffolding (dead-code cleanup) — 2026-07-18. User flagged that leftover pre-v3 scripts/code were confusing and wanted only correct/current artifacts to remain. Removed three classes of now-obsolete material: **(1) the calibration harness** — `scripts/calibration/{pairs.json,run-pairs.cjs}` (hand-labeled *identity-based* expected verdicts — the exact philosophy v3 deleted — plus a dead `@ethena_labs` handle) + both calibration docs (`krypto-kol-calibration-pairs-v26.md`, `krypto_kol_calibration_pairs_verified_12.md`); dropped `run-pairs.cjs --selftest` from `pnpm check` and the `pnpm calibrate` script. Correctness is now guaranteed by the `scoring-v3` unit tests (53 assertions), not hand-labeled live pairs. **(2) Vestigial LLM classifications** the model still emitted but v3 scoring never consumed: `KolRelationship` + `relationship`/`relationshipEvidence` (Unit 29F authority) and `audienceIntentOverlap` (Unit 30 intent) — removed from the shared `ContentFitAssessment` schema, the OpenAI `CONTENT_FIT_SCHEMA` + prompt (dropped ~35 lines of relationship/intent instructions), the mock (`classifyRelationshipMock` deleted), and stale comments in the provider/cache. Saves tokens + ends "is this used?" confusion; `assessContentFit` now returns only the content-fit rubric (topicalAdjacency/audienceOverlapPotential/naturalMentionFit/sharedTopics/rationale — informational in v3). **(3) Stale docs:** `code-standards.md` still had the old 35/20/15/15/10/5 blend weights table → replaced with the v3 model (overall == engaged_audience_match, no blend, gates + dials); fixed an `architecture.md` "scoring v2" parenthetical. **Verified:** full `pnpm build` (14) + `pnpm check` all green (classification-v2 28, media-scoring 10, data-enrichment 28, scoring-v3 53, negative-controls 17 — none depended on the removed fields; Zod strips the now-unknown keys from any pre-existing cached ContentFitAssessment payloads, so no cache breakage). Note: `fitModel` (LLM_MODEL_FIT) tiering kept — it now sharpens only the content-fit rubric's borderline adjacency judgments (still a defensible use, and it's optional config, not dead code); comment updated. **Left intentionally:** historical progress-tracker entries still describe the old scoring — they're a dated build LOG (accurate history), not live docs, so not rewritten.
- Unit 41 Scoring v3 — live paid verification + two robustness fixes — 2026-07-18. Ran the live pipeline against real TwitterAPI.io + OpenAI (gpt-5-mini + gpt-5 fit) to verify C2. **C2 confirmed:** the new OpenAI schemas (`valuedRegions` on org, nullable per-account `region`) round-trip live with zero validation errors; per-account **region inference works** (real runs placed 43–46 accounts into macro-regions from X profile locations — realistic global spread N.America/W.Europe/SE-Asia/…); location capture works (`@TheDeFinvestor` → `location:"FREE NEWSLETTER→"`, a joke → unknown); reach works; ~32k/14k tokens per run (cheap). **`@Optimism × @TheDeFinvestor` → GOOD/75/high** with real target buckets (primary developers/infra_research/founders) and a 30%-builder audience — exactly the honest, differentiated verdict v3 exists to produce (not an inflated STRONG). `valuedRegions` came back `[]` on both live orgs (ethena's profile was null; Optimism is a general L2 with no geo-economic skew) — reasonable LLM judgments, not a mechanism failure (it round-trips; the mock populates it; a stablecoin/payments brand would show real regions). **BUG FOUND live (pre-existing, NOT the v3 work — but v3 amplified it):** `@ethena_labs`'s `getUserProfile` returned an **all-null profile** (renamed/suspended/transient — `@TheDeFinvestor` fetched perfectly, so it's not a mapping bug), which starved org classification → empty `targetBuckets` → scoring's generic "any real crypto = target" fallback → a **confident fit 100/STRONG from nothing**. The pipeline guarded empty KOL *posts* but not an empty *org profile*. **Two fixes shipped:** (1) **org-profile guard** (`run-analysis.ts`) — a null org profile with no manual brief now throws (mirrors the KOL-posts guard) instead of scoring garbage; with a brief it still proceeds (the brief defines the target). (2) **generic-target cap** (scoring) — when targets resolve to the generic set, the EAM is capped at `GENERIC_TARGET_MAX_FIT` (= STRONG−1 = 84, so max GOOD, never a confident STRONG) + confidence forced **low** + a "brand's target couldn't be determined, add product/target details" caveat; `overall == engaged_audience_match` invariant preserved (both capped). Tested: `scoring-v3` **53/53** (5 new generic-cap assertions incl. a known-target control that stays uncapped); full `pnpm build` (14) + `pnpm check` all green. Committed on branch `scoring-v3-audience-honest`.
- Unit 41 Scoring v3 — Phase C2 (live geography inference) + Phase D (report surface) — 2026-07-18. **C2 — OpenAI provider now populates geography live:** `ORG_CLASSIFICATION_SCHEMA` gained `valuedRegions` (regionArray, required in strict mode) + a prompt teaching product-economics reasoning (stablecoin/payments → emerging markets; capital-heavy trading → higher-income); `AUDIENCE_BATCH_SCHEMA` gained a nullable `region` per account + a prompt to infer a coarse macro-region from `location=` (now passed per engager) with explicit "use unknown for blank/joke locations, don't guess from bucket/topic" guardrails. Provider maps model `region` at the trust boundary (`"unknown"`/null → omit, matching the mock). Verify needs a paid run (only the mock path is exercised offline). **D — made it all visible** (PDF is gone — removed 2026-07-18 — so the on-screen report is the only surface): new `report/audience-dials.tsx` renders the **Expected reach** card ("~N of your target customers engage per post · X% on-target", with the explicit "separate from the fit score, weigh against price" note) and the **Audience geography** bars (top regions + "% located" coverage, with a "directional, sparse on X" caveat under 25%), placed in the audience panel; a **"Matched against your target: … · valued regions: …"** transparency line (the infer→confirm *transparency* half — the editable override is deferred). **Honesty fixes for bugs the v3 purge introduced in the UI** (flagged to the user): `computeDrivers` no longer credits content/goal/geo as score "drivers" (they don't move the score); `MetricGroups` regrouped from the false "weighted by impact" framing into **The fit score / Context (not scored) / Risk gates** with honest copy; `METRIC_INFO` rewritten for `overall_fit` ("this is the whole score…"), the context metrics ("Context, not part of the score"), and geo (points to the new audience-geography dial). **Verified:** full `pnpm build` (14) + `pnpm check` all green; **screenshotted the rendered report** (throwaway preview route, since removed) — reach card, geo bars, "matched against" line, and the regrouped honest breakdown all render in the mock E2E (`stablepay × cryptokol`: fit 100/STRONG, ~118 target reach, 50% located across Africa/Europe/Asia). **Two cosmetic notes (NOT my bugs, flagged):** the MOCK narrative prints raw bucket keys ("skews toward bots_spam") — mock-only, the real LLM writes prose; and "matched against" shows the org's core target buckets (pre-goal-fold), so under an *awareness* goal (which broadens the effective match to all real crypto) the shown target reads narrower than what scored — accurate for specific goals.
- Unit 41 Scoring v3 — Phase B (expected reach) + Phase C1 (audience-geography algorithm) — 2026-07-18. **Phase B — expected reach (the "how many" dial):** a deterministic `ExpectedReach` computed in `packages/scoring` (`expectedReach()` in metrics.ts), threaded through the pipeline (`avgEngagedPerPost` = mean of reply+quote+retweet over fetched posts, likes/impressions deliberately excluded as vanity) into a new optional `FitReport.expectedReach`. `matchedPerPost = avgEngagedPerPost × matched-target share of ALL classified engagers` (realness baked in — bots/farmers dilute the denominator exactly as they should). Kept rigorously OUT of the fit score — a regression asserts the fit is byte-identical whether reach volume is 1 or 100000 (value = reach ÷ price; only the brand knows price). **Phase C1 — geography algorithm (mock-verifiable; live inference deferred to C2):** new coarse `AudienceRegion` macro-enum + labels (`vocab.ts` — country precision isn't inferable from X's thin free-text `location`); `AudienceAccount.region?`, `OrgClassification.valuedRegions?`, `TwitterUser.location` (+ mapped in the TwitterAPI normalizer), `RegionDistribution` + `FitReport.audienceRegions` dial. Scoring: `geoTiltFactor` applies a **soft, bounded (±`GEO_TILT_MAX`=15%), coverage-scaled** tilt to the audience match — among the MATCHED target accounts, what share sit in the brand's valued regions (Sui/stablecoins value emerging markets; a prediction market values higher-income) — neutral when the brand has no valued regions or nothing's placeable; `regionDistribution` builds the dial. Also folded **goal→target buckets** into `resolveTargets` (developer_adoption now makes developers a target — "goal picks who counts"). Mock LLM gained `classifyRegion` (location→region keywords) + `inferValuedRegions` (product-economics→regions) so mock E2E exercises it; the real higher-recall inference is C2 (OpenAI). **Verified:** full `pnpm build` (14) + `pnpm check` all green; `scoring-v3` now 48/48 (14 new reach + geo + goal assertions, all hand-calc-matched: valued-region audience 82 vs off-region 68, tilt bounded, coverage-scaled, dial coverage=placed/total, dev-goal fold 92 vs 0). All changes additive/optional so older saved reports still validate. **NOT done:** C2 (OpenAI prompt/schema to populate region/valuedRegions live — writable now, verify needs a paid run) and Phase D (rendering the dials + new verdict in FitReportView/PDF/narrative, and the infer→confirm UI). Note the geo tilt DOES move the fit (geography is part of "right audience"), unlike reach which never does.
- Unit 41 Scoring v3 ("audience-honest") — Phase A (core purge) — 2026-07-18. Big design session with the user, who said the scoring algo was "wrong" and gave one north star: **help brands find the creator whose real, engaged audience is most made of their target customers — score everyone the same way, "no extra scoring to anyone."** Decoded the core contradiction: the product promise is "we check who actually listens," but v2 was tuned so **who the creator IS** overrode who listens (founder +6 & GOOD floor, ecosystem-lead lifts, media/adjacent caps, intent floors that rescued non-overlapping audiences — the calibration note literally shows Uniswap×Hayden scored 40/WEAK on pure audience, so the founder machinery was bolted on to force STRONG). User-locked decisions (see `context/specs/41-scoring-v3-audience-honest.md`): **(1)** identity modifiers ALL deleted; **(2)** size counts but as a **separate expected-reach dial**, never in the fit score (value = reach ÷ price, only the brand knows price); **(3)** audience IS the score — content/goal/geo demoted to informational, safety/bots/promo become gates; **(4)** single absolute score (ranking later); **(5)** target = infer + confirm; **(6)** goal reshapes which buckets count; **(7)** follows keep a small 0.25 weight; **(8)** bands 45%+ STRONG / 30% GOOD / 15% OKAY / 5% WEAK / below AVOID; **(9)** audience geography as a soft tilt + dial, valued-regions inferred+confirmed (Sui loves Nigeria/stablecoins, a prediction market doesn't — geography as product-economics fit). **Phase A shipped (this entry):** `packages/scoring` rewritten — `overall_fit == engaged_audience_match` (no weighted blend, no authority boost); deleted `applyAuthorityRules` + the +6 + founder floor + media/adjacent caps + intent damp/floor; `verdict.ts` is now just bands + down-only gates (bot/farm, brand-safety, unrelated-promo); `weights.ts` thresholds reset to the share bands; content/goal/geo still computed as informational components. Retired the obsolete suites (`authority-rules`, `intent-overlap`, `goal-conditional`), renamed `scoring-v2`→`scoring-v3` (34/34, incl. a new `overall==EAM` invariant + the Uniswap benchmark now STRONG **on audience alone, founder relationship present-but-ignored**), updated `negative-controls` (17/17 — the two "promo-heavy → WEAK" cases now correctly AVOID: off-target audiences fall below the 5% floor; the old "awareness value" WEAK floor is gone). **Verified:** full `pnpm build` (14 projects) green + `pnpm check` all suites green (calibration `--selftest` still passes — it tests the verdict-grammar matcher, not the now-obsolete identity labels). `audienceIntentOverlap` left vestigial in the LLM output (no longer consumed; cleanup deferred). **Remaining:** Phase B expected-reach (data confirmed present: `Tweet.replyCount/quoteCount/retweetCount`), Phase C audience geography (needs `location` plumbing: add to `TwitterUser` + normalizer → raw engagers → classifier region inference → region distribution → brand valued-regions; also folds goal→target buckets), Phase D report schema + `FitReportView` + PDF + LLM narrative prompt. project-overview.md Scoring section updated to v3; the old 35/20/15/15/10/5 weights table is gone.
- Perf investigation: slow API reads = Supabase auth round-trips — 2026-07-18. User flagged slow responses (`/api/analyses/quota` 1.7s, `/api/analyses/[id]` 2.6s app-code). **Root cause:** `getClaims()` (the server-side user read) is a **network round-trip to Supabase Auth** because the project key is **HS256 (symmetric)** — the server can't verify the JWT locally. It's called on **every request by the middleware** (session refresh, matches all non-static incl. every poll) AND again in routes; `/api/analyses/quota` resolved the user **twice** (`getCurrentUser` + `getOwnerId`→`getSupabaseUserId`) → ~3 auth hops + the count ≈ 1.7s. The DB queries themselves are fine — indexed (`AnalysisRequest.ownerId`, `AnalysisJob.status`, PK/unique lookups), single round-trips. **Code fix (shipped):** wrapped `readClaims` in React `cache()` (`lib/auth/supabase.ts`) so getClaims runs at most once per request (quota route 2→1 route-level hop; request-scoped, no cross-user leak). **Biggest win (operator, NOT code):** migrate the Supabase project to **asymmetric JWT signing keys** (dashboard → Auth → JWT keys) so `getClaims()` verifies locally with the cached public key — no network — making all auth resolution (middleware + routes + nav) ~instant. **Context:** much of the raw latency is also **dev-server → Tokyo Supabase (`ap-northeast-1`) + cold connections**; a prod instance near the DB region is much faster. The `POST /api/analyses 403`s in the log are the **tier gate** (account used its 12 lifetime) working as designed, not a bug. Also deleted the 7 throwaway test analyses from the hard-proof run (cascade; the user's earlier 08:32/08:35 runs left intact).
- Waiting screen: engagement-first reorder + back buttons + sequential queue — 2026-07-18. User feedback on the running screen (three asks). **(1) Reorder (user-approved "engagement-first"):** header + identity strip + progress bar stay pinned at top; then **Playbook (WaitingTips)** and **Line up your next creator (Queue)** moved UP (so the captive user has something to read/do immediately), then a **"What we're doing"** eyebrow + the staged walkthrough + audience-taking-shape moved BELOW as the proof-of-work — so the wait never reads as a bare spinner. Pure JSX reorder in `RunningExperience` (`analysis-status.tsx`). **(2) Back buttons:** `/detailed` and `/login` had none. New reusable `BackButton` (`components/back-button.tsx`, client) — a proper pill (not a faint text link) that does real browser-back (`router.back()`) with a `fallbackHref` so it never dead-ends; added to both pages. (Report + status pages already had contextual back links — left as-is.) **(3) Sequential queue (don't re-fetch the brand):** the brand's Twitter profile (`getUserProfile`) + LLM org classification (`classifyOrgProfile`) are cached, so a queued same-brand run reuses the brand — *if* the runs don't overlap. **Correction (found via the user's hard-proof run):** the first attempt pinned `boss.work(..., { batchSize: 1 }, ...)` claiming it serializes — **that was WRONG**: pg-boss polls on an interval and invokes the handler for the next job WITHOUT waiting for the previous to resolve, so within one worker jobs can still overlap. Fixed with an **app-level serialization mutex** (handler bodies chained through a `tail` promise; only one `processAnalysisRun` runs at a time). **Hard proof (real paid runs, one worker):** two same-brand runs enqueued simultaneously — caught live as `A=RUNNING / B=QUEUED`, and B.startedAt (09:17:20) > A.completedAt (09:17:19), zero overlap; the `@base` org profile cache had **exactly one row** (fetched 09:16:42 during A, never re-fetched by B). Separately proved brand reuse on `@missiondotfun` (profile fetched once at 09:02:23, unchanged when the second same-brand run ran at 09:03+). **Also found:** the concurrency I first *observed* during testing (two runs overlapping) was actually **duplicate worker processes** accumulated from many local restarts (2 `tsx watch` + 7 pnpm wrappers) — a test-env artifact, NOT a prod issue (prod runs exactly one worker via the single-container `start.mjs`). The mutex is still the correct single-process safety. Caveat: if ever scaled to multiple worker containers, a distributed lock (pg-boss singleton) would be needed. Trade-off: one analysis at a time (fine at this scale). Verified: web + worker build green, regression suites green; engagement-first order + `/detailed` back button confirmed in the browser (throwaway preview, since removed).
- Logo: adopt the finished "overlapx" wordmark + social/OG image — 2026-07-18. User supplied the completed OverlapX logo (PNG only) and asked to use it everywhere. Diagnosis: the **overlap mark was already the app's theme-aware SVG** (`LogoMark` / `icon.svg`) and still matches the finished logo — unchanged. The one genuinely new element was the **lowercase styled wordmark**. Added `<Wordmark>` (`components/wordmark.tsx`) rendering `overlapx` in a new `--font-wordmark` (**Poppins** via `next/font`, self-hosted; a round-geometric *best-match* to the logo font — swap the family in `layout.tsx` for the exact typeface, colour is inherited so it stays theme-aware), replacing the plain `APP_NAME` text in the nav + waiting-tips signature. `APP_NAME="OverlapX"` stays for tab titles/notifications (written name vs. visual wordmark). **Social/share:** wired the logo as the Open Graph + `twitter:summary` image (`public/overlapx-og.jpg`, 2000×2000) with `metadataBase` from `NEXT_PUBLIC_APP_URL` — the app had **no** share preview before. **PNG guidance given:** kept as the master/social asset only; deliberately NOT wired into the nav/favicon (raster, can't recolour per theme, and the file is a dark-bg mockup) — the SVG mark is strictly better there. Verified: web build green + regression suites green; OG tags + `/overlapx-og.jpg` served (200); wordmark renders in both themes (deep-green mark + near-black wordmark on light; lime mark + white wordmark on dark, matching the logo).
- Waiting screen overhaul — real live progress + retention hooks — 2026-07-18. User feedback: customers were **bouncing on the analysis-in-progress page** (screenshot); asked to "improve UI a lot, improve logic, do something different." Diagnosed the bounce: the page set a scary expectation ("Most analyses finish in about 5–7 minutes") *and* invited leaving ("You can leave and come back from History"), the 4 stages were **faked** on a stopwatch (time-estimate `until` thresholds), the bar was a pulsing indeterminate stripe that visibly stalls, and there was nothing to *do*. User picked **Full fix** + hooks **notify-me / line-up-next / richer-tips** (declined payoff-preview). Built two layers. **(A) Real progress (worker→UI):** new `AnalysisProgress` schema in `@kol-fit/shared` (`progress.ts` — stage/stageIndex/updatedAt + report-safe public `ProfileGlimpse` org/kol + folded `AudienceGlimpse[]`; **Unit-33-safe**: public profile facts + audience *shares* only, never raw counts/provider/model). Pipeline (`run-analysis.ts`) gained an `onProgress` option and emits at the 4 **real** stage boundaries (after Twitter fetch → profiles; after engagement collect; after audience classification → folded audience via reused `foldAudienceSegments`; after scoring). Worker (`analysis-run.ts`) merges deltas into one snapshot and persists to a new **`AnalysisJob.progress Json?`** column (additive `prisma db push` applied to Supabase — nullable, "in sync", no data-loss prompt); writes are chained + `.catch`-swallowed (best-effort, never fails the analysis) and reset to `Prisma.DbNull` on QUEUED→RUNNING. `GET /api/analyses/[id]` re-validates + exposes `progress` (added to `AnalysisStatusResponse`). Fire-and-forget throughout: a progress-sink failure can't affect a run. **(B) UI rewrite (`analysis-status.tsx` `RunningExperience`):** stage is now driven by `progress.stageIndex` (falls back to stage 0 while running, −1 queued) not a clock; **forward-only bar** that creeps asymptotically within a stage (never stalls, never hits 100 until COMPLETED); scary "5-7 minutes" replaced by a coarse always-shrinking "…left" phrase; a real **identity strip** (avatar w/ initial fallback + name + verified + follower count) proving the tool is on *their* accounts; a live **"audience is taking shape"** preview (folded shares, low-quality tinted `error`); reframed leave-copy into a safety net.  **Verification finding (fixed):** the teaser's `topAudienceGlimpse` originally did `slice(0, 4)` on the fold, but `foldAudienceSegments` always sorts the merged low-quality slice (bots/farmers — the highest-signal bucket for a "who actually listens" product) LAST, so it got silently dropped whenever ≥4 normal buckets existed; now it keeps top-(max−1) normal + the low-quality slice last whenever present. **Hooks:** `notify-when-ready.tsx` (opt-in browser Notification API — honest scope: browser-only, not Telegram, since we have no user chat id at analysis time — pairs with the existing tab-title flash); `queue-next-analysis.tsx` (inline mini-form that POSTs `/api/analyses`, shows Queued→Open-in-new-tab chips, stays on the current page, handles tier/rate walls inline); richer `WaitingTips` (prev/next + a pausable auto-advance fill bar via new `kf-tip-advance` keyframe, kept the screenshot signature). **Verified:** full `pnpm build` (14 projects) + `pnpm check` (24 suites, 0 failed) green; visually screenshotted every state (queued/reading/measuring/quality/report) in **both themes** via a throwaway `preview-running` route (since removed), console clean (the only hydration warning was the preview harness SSR-ing the elapsed timer — the real component is client-only, never SSR'd). Presentation + progress plumbing only; scoring/report/costs unchanged. **Live end-to-end (real paid run, user-authorized):** ran `@fhenix × @nebula2004` against live TwitterAPI.io + OpenAI (worker restarted on the fresh build). Confirmed via raw SQL + the status API that the worker wrote a real progress snapshot — org Fhenix (35,785 followers, avatar), creator Niharika/@nebula2004 (4,099 followers, avatar), audience `[Creators 21%, Community mgrs 19%, Developers 14%, Low-quality 5% (retained)]` — and the API returns it; report completed (OKAY, 50/100), tab-title flash fired. The run finished in ~90s (provider + classification caches hot for that pair), so the stages advanced faster than a fresh run's ~5-7 min. **Dev gotcha (not a prod issue):** adding the `progress` column to an already-running local dev env made the status API return `progress:null` until `@kol-fit/db` was force-rebuilt (`rm -rf packages/db/dist tsbuildinfo && build`) and the web dev server reloaded the new client — a stale-dist artifact; a clean deploy (`prisma generate` + full build) never hits it. **Operator note: run `prisma db push` (or redeploy the DB) before the worker restarts in prod — the RUNNING transition writes the `progress` column and would otherwise fail the status update.**
- Deployment simplified to one container — 2026-07-18. User questioned the web-on-Vercel + separate-worker-host complexity and asked whether it could be one simple deploy ("all Next.js"). Clarified: frontend+backend are already one Next.js app; the worker is separate only because a 5-7 min analysis can't run in a request/serverless function (documented invariant). Presented options; user chose **two processes in one container** (over the in-process/instrumentation fuse, which needed a ~10-file package extraction since the handlers live in `apps/worker` and are imported by 2 regression tests — flagged the cost, recommended the cheaper path). Added `scripts/start.mjs` (dependency-free Node supervisor: loads `.env` without overriding platform env, spawns `apps/worker/dist/index.js` + `pnpm --filter web start`, and exits if either child dies so the host restarts a clean container — no half-up state) and a root `start` script. **Zero app-code change** — deploy plumbing only; local dev still uses two terminals. Verified: `pnpm start` boots both (worker listening + web serving), and killing one child takes the whole thing down (fail-fast confirmed). Added `DEPLOY.md` (Railway/Render/Fly, build/start commands, env vars, Supabase redirect allowlist, always-on-not-serverless note) and updated the architecture doc's deployment row.
- Auth: Google-only sign-in (removed dev email login) — 2026-07-18. User disliked the passwordless email dev-login and wanted Google OAuth exclusively, with history following the account and user tables tracked. Investigation found it was mostly a config gap: Google OAuth, claim-on-login (anonymous history carryover), and the `User` table already existed (Unit 28) — the email form only showed because local `.env` lacked `NEXT_PUBLIC_SUPABASE_*`. User added those keys (Supabase mode now active locally) and confirmed "Google only, delete email login." **Removed:** the `DevEmailForm` (login is now `<GoogleSignIn>` only, with a `NotConfigured` notice when Supabase env is absent — never the email form); the `POST /api/auth/session` dev-login handler (kept `DELETE` sign-out); `devLoginAllowed` (auth pkg + its 5 regression assertions); the dev-session setters (`setDevSession`/`readDevUserId`), leaving only `clearDevSession` for sign-out cookie hygiene; `current-user.ts` returns null when not in Supabase mode. Kept the tested `signSessionToken`/`verifySessionToken` primitives. **Verified live:** signed in as a real Google account with history showing (claim works, User row mirror-upserted); `/login` renders "Continue with Google" with zero email markup; `POST /api/auth/session` → 405, `DELETE` → 200; build + full `pnpm check` green (auth suite 24→19). `.env.example` updated to Google-only. Note: an existing dev-login `User` row sharing an email with a Google user can collide on the `@unique` email (mirror-upsert fails non-fatally) — a test-data artifact only; clean Google-only prod has no such rows.
- Logo finalized — 2026-07-17. User adopted the overlap mark (two intersecting rings, filled lens = the engaged overlap) as the real logo instead of supplying their own. De-placeholdered `apps/web/app/icon.svg` (favicon). User may still change it later.
- Logo light-contrast fix — 2026-07-18. Pure lime washed out on the near-white nav (~1.3:1). The mark was an `<img src="/logo.svg">`, which is isolated from page CSS and can't be theme-aware (and the app's theme is `data-theme`, not the OS `prefers-color-scheme` an SVG media query sees). Fix: inlined the mark as `<LogoMark>` (`components/logo-mark.tsx`) using `currentColor`, driven by a new `--logo-mark` token — deeper accent-ink green `#5b830a` on light (~4.2:1), exact lime `#bef54b` on dark (unchanged). Rewired the nav and the waiting-tip signature to it; deleted the now-dead `public/logo.svg` (component is the in-app source of truth). `app/icon.svg` favicon stays lime (separate surface, can't read app theme). Verified both themes in the browser.
- Favicon light-contrast — 2026-07-18. Made `app/icon.svg` follow the OS colour scheme via an internal `@media (prefers-color-scheme)` in a `<style>` block: deep green `#5b830a` by default (light tab bars, matching the light theme) and lime `#bef54b` on dark tab bars. A favicon can't read the app's `data-theme`, so OS scheme is the only signal available; the default matches the app's default (light). Same mark + hexes already verified in the nav.
- Waiting-tip brand signature (screenshot marketing surface) — implemented 2026-07-17 (web build + full `pnpm check` green; verified both themes). Tip cards get screenshotted, so `WaitingTips` now carries a subtle footer signature: hairline divider, `/logo.svg` mark + `APP_NAME` on the left, "who actually listens" tagline on the right. Reuses the logo asset so it inherits the real logo automatically once dropped in; kept outside the keyed tip so it stays crisp while tips fade. Turns the ~5-7 min wait into a small marketing surface. (Also committed the user's own edit removing the "with or without @" field hint.)
- Waiting-screen value: "While you wait" creator-marketing tips — implemented 2026-07-17 (web build green; full `pnpm check` green; verified in both themes on a throwaway preview route, since removed). User feedback: the ~5-7 min run left a captive, on-topic audience (a brand about to spend on a creator) staring at a spinner. Confirmed direction with the user: curated (not AI-generated) creator-marketing tips, auto-rotating. Added `apps/web/lib/creator-marketing-tips.ts` (18 hand-written, quotable tips with one-word category tags; neutral for AI + Web3, no "KOL") and `apps/web/components/waiting-tips.tsx` (`WaitingTips`): shuffles once on mount (in an effect, so first paint is deterministic — though the running card is client-only anyway), auto-advances every 8s via a timeout keyed on the index (so a manual advance resets the countdown), pauses on hover/focus, has a manual next control + an "N/18" counter, fades between tips with `animate-in fade-in` gated on `prefers-reduced-motion`. Wired into `RunningExperience` in `analysis-status.tsx` between the pipeline stages and the footer. Presentation-only; no pipeline/cost/latency impact.
- End-to-end real-run verification + copy fixes — 2026-07-17 (14 projects build green; full `pnpm check` green). Ran a REAL analysis (@fhenix vs @nebula2004) against live TwitterAPI.io + OpenAI (gpt-5-mini/gpt-5), worker + web on Supabase, and inspected every surface in the browser. Result: "Okay fit", 50/100, high confidence, rendered correctly. **Handle input:** confirmed `HandleSchema` already normalizes with/without `@` (and mixed case/whitespace);. **Verified working:** full report view in BOTH themes, the folded donut with real data (6 clean segments in light + dark), the PDF (all 3 pages — lime-square + ink wordmark, print-safe green bars, lime callout), and the public share funnel (`/r/[token]`). **Copy bugs found while looking (all fixed):** the report header still labeled the two accounts "Org"/"KOL" (→ "Brand"/"Creator" in `fit-report-view.tsx`); the running-pipeline stage copy said "the org and the KOL"; the empty-field validation messages said "Organization handle"/"KOL handle"; two metric tooltips said "this organization"; and the PDF's Evidence section rendered raw camelCase keys as "KOL POSTS"/"KOL REPLIES" (added an `EVIDENCE_LABELS` display map in `report-pdf.tsx` rather than renaming the internal keys, which are baked into already-stored reports). Also the status page's "come back from Reports" link → "History" to match the renamed nav. Verified the PDF by rendering the stored report to a file and viewing all pages (throwaway preview route + temp files since removed). Note: internal `kolHandle`/`KolProfile`/`sampleSizes` keys remain unchanged by design (never user-visible; renaming is a migration).
- Audience donut: segment folding (the readability fix) — implemented 2026-07-17 (14 projects build green; new 21-assertion regression + full `pnpm check` green; both themes + hover verified visually on a throwaway preview route, since removed). Closes the flagged donut limitation. Per the dataviz skill's own anti-pattern ("donut... ≤ 6 segments" / "past ~7 colour classes adjacent classes blur"), the fix is capping slices, not just recolouring. `foldAudienceSegments` (new, in `packages/shared/src/audience-segments.ts` — pure, so it lives with the other testable logic and out of the React component) folds the 15-bucket taxonomy to ≤ `AUDIENCE_MAX_SEGMENTS` (6): the three low-quality buckets merge into ONE reserved-tone slice (they already shared the error tone, so were never colour-separable), and the categorical tail folds into a neutral "Other". Both folded slices sort last, so the ring's tail is a fixed neutral→red pair, not two share-dependent hues. **Nothing is hidden:** a folded slice carries its `members` (each with its own share, largest first) and renders the breakdown in the tooltip + `title`; the per-bucket table stays in the PDF. Added `--viz-other` (a SECOND neutral, separated from `--viz-neutral` by lightness so "Other" and the `non_crypto` bucket can't collide) in both themes. **Bug fixed along the way:** the floating tooltip was anchored to the donut pointer position but legend-row hovers also set it, rendering it at a stale {0,0} clipped off-screen; it's now donut-only (`tipFor`) and clamped so tall breakdowns stay inside the card. **Also (missed in the earlier de-crypto pass):** user-facing "KOL" survived in `vocab.ts` (`kols_creators` label → "Creators") and in scoring rationales shown in the report (`metrics.ts`, `score.ts`) → "creator"/"brand". `audience-segments.regression.cjs` added to `pnpm check` (share conservation, no-bucket-dropped, cap, all-low-quality, exactly-at/over budget, pathological cap).
- Light/dark theming (light default), hero-field fix, navbar cleanup — implemented 2026-07-17 (14 projects build green; `pnpm check` green; both themes + the toggle + persistence verified visually against a production build). User feedback: the hero field clumped into a permanent blob, the app should be **light first** with a toggle, and the navbar needed work. **Theming:** `globals.css` now carries light (default) + dark raw-value blocks; every mapping below them is written once, so retheming means editing two blocks. Dark is `data-theme="dark"` on `<html>`, set by the new `ThemeToggle`, persisted to localStorage, and replayed pre-paint by an inline script (`lib/theme.ts`) so dark readers get no white flash. OS `prefers-color-scheme` is deliberately NOT auto-applied, per "white first". **Accent split (the load-bearing part):** lime is a LIGHT colour, so it can't be text on white. Tokens are now `--accent-primary` (fill, same both themes), `--accent-contrast` (on the lime), `--accent-ink` (accent as text/icons — darkens to `#5B830A` on light), `--accent-hover` (fill hover). The 28 `text-accent-hover` usages became `text-accent-ink`; `bg-accent-hover` stays a fill hover. **Hero-field bug (the reported blob):** cursor attraction had no falloff as distance → 0 and no repulsion, so particles converged on the exact same point and fused permanently. Added a repulsive CORE (58px) so the crowd forms a ring, plus a hard speed ceiling. The canvas also reads `--field-*` tokens and re-reads them on `data-theme` change (lime is invisible on white → light mode steps engaged down to a deeper green). **Chart:** donut colours moved into `--viz-*` tokens with a separately validated light set (all checks pass; light sits in the contrast relief band, covered by the labels the chart already ships). **Veils/glows** now token-driven (`--field-veil`, `--shell-glow`), so nothing is hardcoded dark-only. **Navbar:** removed the stale "Internal" badge (a build-stage marker from when the tool was internal-only; meaningless now that there's sign-in, quotas, and public share links), renamed "New" → "Analyze" and "Reports" → "History", added the theme toggle. `/analyses` retitled "Your reports" under a "History" eyebrow to match. Open question: "improve the UI" was raised but not specified beyond the navbar and the field — no further redesign attempted.
- Rebrand: acid-lime theme, real logo slot, de-crypto'd copy — implemented 2026-07-17 (all 14 projects build green; `pnpm check` green; verified visually against a production build). User feedback: the Morpho-blue-on-charcoal look read as AI-generated, the copy was weak, and "KOL" is crypto jargon while the product serves **AI and Web3 brands alike**. Reference: Robinhood + Higgsfield (near-black, acid lime, pill controls, tight geometric headlines). **Theme:** palette re-based in `globals.css` to near-black (`#0A0C10`) + acid lime (`#BEF54B`). The lime is a LIGHT accent, so `--accent-contrast` (near-black) is what sits on it; `--primary-foreground`/`--accent-foreground` map to it, otherwise every primary button is white-on-lime and unreadable. `--accent` is now the BRAND lime (app code already read it that way); the two shadcn button variants that wanted a neutral hover say `bg-elevated` explicitly. Buttons are pills (`rounded-full`). **Bug found + fixed (pre-existing):** `--color-accent-primary` was never mapped in the `@theme` block, so all 10 `*-accent-primary` utilities app-wide were silently dead — including the old nav's gradient. **Logo:** now `apps/web/public/logo.svg` + `apps/web/app/icon.svg` (placeholder mark, user is supplying the real asset); nav renders it via `next/image`, so rebranding is a file swap. **Non-token colour** also re-based: hero canvas constants (`audience-field.tsx`, mirrors the lime in JS since canvas can't read tokens), the shell glow, the status veil, and the PDF. **PDF** is light/printable, so lime is a FILL only, never text: wordmark is ink, bars use a print-safe `brandDeep`. **Chart:** the donut's blue-family palette was re-based on a validated categorical set (per the dataviz skill — passes dark lightness band, chroma floor, adjacent-pair CVD 9.4, normal-vision floor 19.4, 3:1 on surface); also fixed a real bug where `traders` and `defi_users` rendered the *same* hex. Known limit documented in-file: 11 hues exceed all-pairs separation, mitigated by the legend/labels/tooltip the chart already ships. **Copy:** hero is now a benefit headline ("Know who actually listens") rather than the bare product name; all `Sparkles` icons removed (the single biggest AI-generated tell) along with the glyph-in-a-gradient-square mark. **Wording:** "KOL" → "creator" and "organization" → "brand" in all user-facing copy (internal `kolHandle`/`KolProfile` identifiers deliberately untouched — invisible to users, renaming them is a migration). A WORDING rule was added to `buildNarrativePrompt` banning "KOL" and the crypto assumption, since the report narrative is model-written and would otherwise reintroduce the jargon at runtime. `ui-context.md` rewritten to match.
- Product rename to OverlapX + copy de-AI-ification — implemented 2026-07-17 (all 14 projects build green; `pnpm check` all suites green; homepage render confirmed serving `<title>OverlapX</title>`). Product name is now **OverlapX** everywhere a user can see it. Renamed via the existing `APP_NAME` constant in `packages/shared/src/constants.ts` (the single source), plus the hardcoded copies: root `metadata.title`, the shared-report page title/banner, the status-page tab-title flash, list/empty states, the delivery email subject + signature, the PDF `Document` title/header/footer, and the emailed PDF filename (`kol-fit-*.pdf` → `overlapx-*.pdf`). `report-pdf`, `report-deliver`, and `analysis-status` now import `APP_NAME` rather than repeating the literal. Scope decisions confirmed with the user: the internal `@kol-fit/*` workspace scope is **deliberately unchanged** (never user-visible; renaming it would touch every import + the lockfile for no user gain), and em dashes were stripped from **user-facing copy only**, not code comments. **Copy:** em dashes removed from all visible prose (rewritten as periods/commas/colons/parens, not mechanically deleted); the `"—"` empty-value placeholders in tables/badges are typographic convention and were intentionally kept. **Prompt-level fix (the durable part):** the report narrative is model-written, so a STYLE rule was added to `buildNarrativePrompt` in `packages/llm/src/openai/prompts.ts` forbidding em dashes and stock LLM phrasing — otherwise every new report would reintroduce them at runtime; the prompt's own em dashes were cleaned so it doesn't model what it forbids. Context docs renamed to match. No behavior/schema/scoring change.
- Unit 40.1: Leads Badge Read-Semantics + Table Fix — implemented 2026-07-17 (web build green; all suites green). Two user-reported admin issues. (1) **Hydration/table bug (mine):** `AdminTable` expects `<th>` elements as `head`; the detailed-requests table passed raw strings → text nodes inside `<tr>` → invalid HTML, hydration error, headers collapsing into a left-side blob. Fixed to proper `<th>` cells (minWidth 960 for the 8 columns). (2) **Chat-style unread badge:** new `DetailedReportRequest.seenAt` (additive push) — opening `/admin/leads` marks all unseen rows seen (best-effort updateMany during render); the nav badge now counts `seenAt: null` instead of status NEW, so it clears once viewed while the NEW/SENT/DISMISSED workflow stays independent.
- Unit 40: Retry Failed Analyses + Fair Quotas — implemented 2026-07-17 (web build green incl. the retry route; all suites green; manual pass pending). Per `context/specs/40-retry-and-fair-quotas.md` (last of the user-approved improvements #2-4). **Fair quotas:** `countLifetimeAnalyses` (single source for the tier gate AND the quota indicator, per the Unit 39 refactor) now excludes FAILED jobs — failed runs no longer consume the 3/12 lifetime allowance. Deliberate distinction: the DAILY abuse caps (Unit 26) still count everything (spend protection — a failing request still burns provider calls). **Retry:** `POST /api/analyses/[id]/retry` — owner-gated (404-for-strangers), FAILED-only (409 otherwise), attempts < 10 safety cap, daily abuse caps still apply; resets the job to QUEUED (clears error/timestamps), re-enqueues the same `{requestId, jobId}` payload, records the new pgBossJobId; enqueue failure reverts to FAILED (mirrors the create route). Complements the worker's existing auto-retry for transient provider errors. **UI:** the failed panel gains a primary "Retry analysis" button (re-enters the queued/running experience via the existing reload mechanism, near-free thanks to caches) + a "failed runs don't count against your analyses" reassurance line; retry API errors surface inline. This completes the approved improvement batch (38 share links, 39 funnel polish + 39.1 leads consolidation/badge, 40 retry/fair quotas).
- Unit 39.1: Admin Leads Consolidation + Unread Badge — implemented 2026-07-17 (web build green). User feedback: the Leads page showed only the dead Unit-24 email-capture rows while real leads (concierge requests) lived on a separate page, and unhandled requests needed chat-style visibility. **Consolidation:** `/admin/leads` is now the concierge detailed-requests queue (Telegram/X/email, status actions) with legacy email captures preserved in a secondary section below; `/admin/detailed` redirects to `/admin/leads`; the duplicate nav entry removed. **Unread badge:** new admin-gated `GET /api/admin/detailed-requests` returns the NEW-status count; `AdminNav` shows it as a chat-style accent badge on the Leads link (fetched on mount/route change + 60s refresh, best-effort). Also (39, hardening): `ShareReport` split minting from clipboard — the minted URL is always shown inline in a copyable input, API errors show their real message, and a blocked clipboard no longer reads as "sharing failed" (the earlier "Try again" symptom; stale-Prisma-client restart also applies).
- Unit 39: Funnel Polish — implemented 2026-07-16 (web build green incl. `/api/analyses/quota`; all suites green; manual pass pending). Per `context/specs/39-funnel-polish.md` (user-approved improvement #3). (1) **Quota indicator:** `GET /api/analyses/quota` (read-only, `getOwnerId` — never sets a cookie on read; lifetime count refactored into `countLifetimeAnalyses()` in tier-gate, the single source for the gate AND the indicator so Unit 40's fair-quota fix hits both); the analysis form shows "N of M analyses left" beside the submit button (hides silently on error). (2) **Ready notification:** the status page flashes the tab title ("✅ Report ready" / "⚠️ Analysis failed") when the run finishes while the tab is hidden, restoring on return — dependency-free, no permission prompts. (3) **Requester acknowledgment:** the concierge success card now nudges the requester to open the delivery bot (`NEXT_PUBLIC_TELEGRAM_BOT_USERNAME`, non-secret, button hidden when unset) and tap Start — Telegram bots can't DM first, so this closes the future delivery loop. `.env.example` updated (user should set `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=kolfitbot`).
- Unit 38: Shareable Report Links — implemented 2026-07-16 (web build green with both new routes; all suites green; manual walk pending: share → incognito open → revoke → 404). Per `context/specs/38-shareable-reports.md` (user-approved improvement #2). **Schema:** `Report.shareToken String? @unique` (additive `db push` — the "data loss" warning was the standard unique-constraint caution on an all-NULL new column; zero risk, accepted). **API:** `POST/DELETE /api/analyses/[id]/share` — owner-only (same 404-for-strangers pattern), POST mints/reuses a 128-bit base64url capability token (COMPLETED reports only, else 409), DELETE revokes (old URL dies). **Public page `/r/[token]`:** force-dynamic, noindex, token-only lookup, 404 on missing/revoked/incomplete; renders the client-redacted `FitReportView` in new `mode="public"` (no owner nav/actions) under a brand banner — "This KOL fit report was shared with you… Analyze your own KOL →" — making every shared report a funnel entry; the concierge CTA stays (shared-report lead-gen). **Owner UI:** Share button next to "All reports" (mints link → clipboard → "Link copied", revoke appears while shared). Token appears in no list/API besides the owner share endpoint. Next per user approval: funnel polish (quota indicator, completion tab-notification, requester acknowledgment), then retry + fair quotas. Org-declared accounts dropped (user call); ROI framing dropped earlier (user call); data-gap handles rechecked 2026-07-16 — still empty upstream.
- Unit 37: Accuracy-Booster Form Section — implemented 2026-07-16 (web build green; visual pass pending). Per `context/specs/37-accuracy-booster-form.md`, user direction: make the optional-context section sell itself instead of building the goal-conditional report surface. The muted "Add optional context" ghost toggle is now a full-width accent card ("Sharpen your results" + Recommended chip + honest benefit copy — the analysis calibrates to YOUR audience/goal instead of generic assumptions); open-state microcopy reframed; outcome-language field hints on website URL / campaign goal ("changes the verdict") / target user ("the audience we match against"). Form-only change; the Unit 32 goal-conditional engine benefits automatically whenever a goal is actually supplied. Goal-conditional REPORT surface (verdict-by-goal strip) remains on the future board.
- Unit 36.1: Report-page CTA cleanup + email on concierge requests — implemented 2026-07-16 (full build green; 23 suites green, detailed-requests check now 17 assertions). User direction: **the on-page report IS the full report** — the Unit 24 "Get the full report" email-capture component contradicted that (and the redaction story), so `get-report.tsx` is REMOVED from the report page and deleted; the single remaining CTA is "Request curated report". Email capture folds into the concierge request instead: `DetailedReportRequest.email` column added (additive `db push` applied) — **required for anonymous requesters** (form shows the field + route enforces), **auto-filled from the account for signed-in users** (no field shown). Shared schema gains optional validated/lowercased `email` (requiredness is auth-dependent, enforced in the route where auth is known). Admin queue + operator Telegram notification now include the email. Historical `ReportDelivery` leads/table remain untouched (old data still visible under /admin/leads). Note: X handle stays required per the original funnel direction — one-line change to relax if wanted.
- Unit 36: Operator Notifications — implemented 2026-07-16 (web build green; 22 suites unchanged-green; live verification = operator sets env + submits a request). Per `context/specs/36-operator-notifications.md`. New `apps/web/lib/notify.ts`: `notifyOperator(text)` — fire-and-forget Telegram Bot API `sendMessage` to the OPERATOR's own chat (`TELEGRAM_BOT_TOKEN` + `TELEGRAM_ADMIN_CHAT_ID`; both unset = silent no-op; 5s timeout; all errors swallowed and never logged since the request URL carries the token) + `buildDetailedRequestNotification()` (pair, handles, 120-char note, admin link via `NEXT_PUBLIC_APP_URL`). Wired into `POST /api/detailed-requests` after a successful create via `void` (never blocks/fails the user's request). One-time operator setup documented in the spec + `.env.example` (@BotFather token, chat id via getUpdates). Closes the "within a day" gap: the concierge promise no longer depends on polling the admin queue.
- Unit 35: Detailed-Report Requests (concierge lead capture) — implemented 2026-07-16 (full build green incl. all 4 new routes; 23 check suites green incl. the new 14-assertion `detailed-requests.regression.cjs`; manual walk pending). Per `context/specs/35-detailed-report-requests.md`, user-clarified scope: **the curated analysis is a standing alternative path available at ANY time**, not only past the 12-report wall. **Schema (applied to Supabase via `prisma db push`, additive):** `DetailedReportRequest` (ownerId, userId?, analysisRequestId?, org/kolHandle?, telegram, xHandle, note?, `DetailedRequestStatus NEW|SENT|DISMISSED`, fulfilledAt?). **Shared:** `DetailedReportRequestInputSchema` with forgiving normalizers (`@`/t.me/x.com/twitter.com URL → bare handle) and strict stored shapes. **API:** public `POST /api/detailed-requests` (owner cookie + optional userId, per-owner cap `MAX_DETAILED_REQUESTS_PER_OWNER_PER_DAY` default 3/24h); admin `PATCH /api/admin/detailed-requests` (status transitions, SENT sets fulfilledAt; zod-free inline validation — apps/web has no direct zod dep). **Entry points:** standalone `/detailed` page (org/KOL optional, prefill via query params); concierge CTA panel on every completed report ("the analyst's cut", prefills pair + analysis id); secondary link on the analysis form; the Unit 34 upgrade wall now has its Request button. **Admin:** `/admin/detailed` queue page + nav entry (pair + report link, t.me/x.com links, note, status pills, Mark sent / Dismiss / Reopen). `project-overview.md` gained the Access Model & Concierge Tier section. Fulfillment is manual by the operator (confirmed).
- Unit 34: Tiered Access Funnel — implemented 2026-07-16 (full build green; `pnpm check` 22 suites green incl. the new 14-assertion `tier-gates.regression.cjs`; manual funnel walk by the user pending). Per `context/specs/34-tiered-access-funnel.md`, user-confirmed model: **3 lifetime analyses per anonymous browser → Google-login wall → 12 lifetime per account → detailed-report concierge tier (Unit 35)**. Shared: `TierLimits`/`resolveTierLimits` (`FREE_TIER_ANON_LIFETIME`/`FREE_TIER_USER_LIFETIME`, default 3/12) + pure `decideTier(count, isAuthenticated, limits)`; `ApiErrorCode` += `login_required`/`upgrade_required` (additive). Web: `lib/tier-gate.ts` counts lifetime `AnalysisRequest` rows per owner (the Unit 28 login claim merges anonymous history into the account, so free-tier usage counts toward the 12 by construction); `POST /api/analyses` runs the tier gate BEFORE the daily abuse limit (funnel copy wins) → 401/403 with friendly messages; `analysis-form.tsx` renders code-aware walls (accent-styled panels, not error styling): login wall with a Sign-in CTA to `/login`, upgrade wall with the concierge pitch (the request CTA itself ships with Unit 35). Daily abuse caps (Unit 26) unchanged on top. Anonymous counting is cookie-based (incognito-resettable — accepted v1 leak, user-confirmed). No schema change.
- Unit 33: Client-Facing Methodology Redaction — implemented 2026-07-16 (web build green; source-level leak sweep clean; user visual pass pending). Per `context/specs/33-client-methodology-redaction.md`, user direction: the public UI must not reveal the "secret sauce" or internal artifacts. **Nothing persisted changed** — scores/reasons/evidence/samples stay in the DB (internal intelligence + admin); only the render is trimmed. Changes: **waiting page** (`analysis-status.tsx`) — request id removed from header + running panel; staged walkthrough rewritten to outcome language (no post/API/account counts, no "AI batches", no "deterministic scoring across nine metrics"); **report** (`fit-report-view.tsx`) — "Evidence & sample" section removed (sample grid + notes that named providers/models/ingestion/scoring approach) → slim footer with confidence + generated date; hero sample chips removed; "What works / what to watch" no longer quotes raw scoring `reasons` (methodology strings) — neutral value-based notes instead; **audience donut** — center shows "Engaged audience / by type" instead of the classified count (revealed the classification cap); **metric bars** (`metric-groups.tsx`) — weight chips removed (were also stale pre-29E values); **ⓘ explainers** (`metric-info.ts`) — rewritten to what-it-tells-you language, no baselines/curves/thresholds/exclusions/weights. Kept client-visible: verdict, scores, confidence level, audience shares, LLM narrative, takeaways, recommendations, failed-state error codes. `project-overview.md` success criterion 8 updated (evidence shown internally only). The report-delivery CTA still receives the request id as a functional prop (not displayed).
- Hotfix: pg-boss session-pooler exhaustion (user-reported 2026-07-15: worker log flooded with `ECONNRESET` then `EMAXCONNSESSION max clients reached in session mode - pool_size: 15`) — implemented + verified (full build green; `pnpm check` 21 suites green incl. the new 7-assertion `pgboss-pool.regression.cjs`). **Root cause:** pg-boss connects via `DIRECT_URL` (Supabase session pooler, 15-client cap) and its default pg Pool is 10 per process; the web app (enqueue-only) + the worker together demand up to 20, so after a pooler idle-drop (`ECONNRESET`, normal) the reconnect storm hit the cap. **Fix (`packages/queue/src/boss.ts`):** new pure `resolvePgBossOptions(env)` — enqueue-only processes (default role) get `max: 2` and `supervise/schedule: false` (no background loops for a process that only sends jobs); the worker (identified by `PGBOSS_ROLE=worker`, set in `apps/worker/src/env.ts` before queue use) gets `max: 5` with supervision/scheduling intact; `PGBOSS_POOL_MAX` overrides. Totals: web 2 + worker 5 = 7, with tsx-watch restart-overlap headroom (12) under the 15 cap. `.env.example` documents `PGBOSS_POOL_MAX`. **User action: restart both dev processes** (stale pools clear after the pooler's idle timeout).
- Unit 32: Goal-Conditional Verdicts (v1) + Official Ecosystem Lead — implemented + live-validated 2026-07-15 (builds green; `pnpm check` 20 suites / 376 assertions green incl. the 18-assertion `goal-conditional.regression.cjs`; **live calibration 16/17 valid pairs exact-band** — from 13 after Unit 30, 9 after Unit 29, ~5 under v1). Per `context/specs/32-goal-conditional-verdicts.md` (v26 rule 3 + the hierarchy's missing class). **New relationship class `official_ecosystem_lead`** (additive enum): publicly-known creator/lead/official operator — and publicly announced advisors — of the org's platform; founder-grade floor always, founder-grade +6 boost except under a retail `awareness` goal (v26 pair-3: "STRONG for builders, GOOD for broad awareness"). **Adjacent authority capped at GOOD** under the default product-relevant campaign; `awareness`/`investor_credibility` goals lift the cap (the structural Vitalik fix, safe now that jesse has his true class). **Media WEAK tier softens to OKAY under an awareness goal** (WatcherGuru's own label note). `applyAuthorityRules` gains `goalKey` (from the existing `normalizeGoal`; absent = default campaign). **Definition refinements from the gpt-5 judge** (each anchored in v26): media_or_news = BRAND accounts incl. community/event brands; advisors count as official leads; specialization contrast in the intent rubric (generalists ≠ deep-niche specialists). **Fit tier proven:** `LLM_MODEL_FIT=gpt-5` fixes every knowledge-bound judgment (meow founder, jesse official lead, Nate advisor) — **PRODUCTION MUST ADD `LLM_MODEL_FIT=gpt-5` to `.env`** or worker fit judgments stay on gpt-5-mini. Remaining: 1 jitter miss (Vitalik intent 2↔3 — the opposite-labeled Aave×hayden sits on the same rating, so constants can't separate them; documented), 3 TwitterAPI.io data-gap handles (upstream, error loudly).
- Unit 31: Media Scoring — implemented + live-validated 2026-07-15 (builds green; `pnpm check` 19 suites / 358 assertions green incl. the new 10-assertion `media-scoring.regression.cjs`; mock E2E deterministic; live re-score byte-identical on all 17 calibration verdicts/scores — no regressions, the change adds detection without disturbing non-exhibiting pairs). Per `context/specs/31-media-scoring.md` — makes the 29B vision labels count. (1) **Visual-only promos join promo saturation** (`paidPromoRisk` v3): a post whose image is labeled `promo_graphic` but whose text wasn't flagged is a visual shill and counts toward saturation (no double-count with text promos; doesn't inflate `unrelatedShare` — gates stay evidence-driven); regression shows 4 hidden visual promos moving risk 4→17 with an explicit reason. (2) **Deterministic media profile** appended to `content_fit` reasons ("Visual content across N labeled image(s): X% charts/data/analysis, Y% memes, Z% promo graphics") — substantive share deliberately informational-only (no calibration label demands a numeric effect; knob documented in the spec). (3) `DEFAULT_MEDIA_IMAGE_LIMIT` 12→16 (live data: ~40% of sampled posts carry media). Calibration standing: 13/17 exact-band (unchanged, as intended).
- Unit 30: Audience Intent Overlap — implemented + live-validated 2026-07-15 (full builds green; `pnpm check` 18 suites / 348 assertions green incl. the 31-assertion `intent-overlap.regression.cjs`; two live validation iterations). Per `context/specs/30-audience-intent-overlap.md` (v26 rule 4: category ≠ intent). **Calibration: 13/17 valid pairs exact-band, up from 9** — fixed Immutable×shroud (STRONG 86→OKAY 61), Uniswap×WatcherGuru (OKAY→WEAK 60), Ledger×CoinDesk (GOOD→WEAK 61), fhenix×CoinDesk (OKAY→WEAK 56), Polymarket×Nate (OKAY 61→GOOD 71 via the intent floor) while holding all previous passes. **Design:** `ContentFitAssessmentSchema.audienceIntentOverlap` (0-5, additive optional) judged in the pair-specific fit call with the v26 rule-4 contrasts + an ANTI-HEDGE rule (iteration 1 showed the model defaulting to 3 for exactly the ≤2 cases); EAM v3 damps category matches on intent ≤2 (`INTENT_DAMP` .3/.4/.5) and floors on intent ≥4 (`INTENT_FLOOR` 55/70), 3/unknown neutral; media cap intent-tiered (≤2→WEAK, GOOD needs EAM≥75 AND ≥4, else OKAY). **Stability work:** identity-first relationship instruction; fit call runs `reasoningEffort: "low"` (per-call override added to the OpenAI client); new `LLM_MODEL_FIT` env — optional stronger model for the single judgment call (fit cache keys on the judging model via `provider.fitModel`). **Residual misses (4, documented in the spec):** Vitalik (structural — goal-conditional unit), meow (model-knowledge: gpt-5-mini doesn't reliably know the pseudonymous founder — `LLM_MODEL_FIT` or org-declared official accounts fix it), Aave×hayden at exactly 65/GOOD-bound, fhenix×ETHIndia intent 3↔4 flicker (Medium-confidence label). Tuning stopped deliberately at the jitter floor.
- Unit 29E (tuning half): Live Calibration + Tuning Pass — completed 2026-07-14 against real TwitterAPI.io + OpenAI (gpt-5-mini) data; full per-pair table + analysis recorded in `context/specs/29e-calibration-harness.md`. **Result: 9/17 valid pairs exact-band — all 5 STRONG founder/ecosystem pairs pass (Uniswap×hayden 94, chainlink×Sergey 83, base×jesse 85, Jupiter×meow 87, eigenlayer×sreeram 100), and the GOOD band passes wherever the authority/media machinery applies (zachxbt 82, TheDeFinvestor 80, both ETHIndia pairs media-capped to GOOD).** The v1 benchmark (Uniswap×hayden 40/WEAK) closes at 94/STRONG/high on live data; live latency confirmed ~2 min cold / ~45s cache-warm (was ~6.5 min pre-29D). **Tuned constants:** STRONG threshold 80→83; new `AUTHORITY_OVERALL_BOOST_FOUNDER` +6 (v26 rule 1 "very large positive modifier AND floor"); media cap two-tier (`MEDIA_CAP_GOOD` when EAM≥75 audience proof — media earns GOOD, never STRONG — else `MEDIA_CAP_OKAY`). **Prompt fixes (cache purged accordingly):** brand-safety — investigating/reporting scams is NOT a flag (zachxbt was false-gated WEAK by his own scam investigations); relationship — pseudonymous/publicly-known founders count (meow was `none`, now founder). **Pipeline guard:** 0 fetched posts → loud failure (was: confident garbage AVOID-28 when TwitterAPI credits soft-failed to empty envelopes). All 17 check suites / 317 assertions green (authority-rules updated for the two-tier media cap + boost). **The 8 remaining misses cluster exactly into the recorded future units, not anchor problems:** (1) intent-overlap taxonomy (v26 rule 4; 6 misses — `defi_users` conflates DEX/lending intent, mainstream gamers→`nft_gaming`, news readers→trader buckets inflating media EAM); (2) goal-conditional verdicts (rule 3; Vitalik×Uniswap STRONG-vs-GOOD — jessepollak is also `adjacent` yet EXPECTED STRONG, so no blanket adjacent cap can separate them); (3) domain-authority lift for specialists (Nate×Polymarket OKAY-61-vs-GOOD). Further global-anchor bending would overfit and break the 9 passes. **Open data issue:** `aeyakovenko` + `BanklessHQ` tweet fetches return empty from TwitterAPI.io even with working credits (endpoint investigation needed; the guard fails them loudly).
- Hotfix: cross-provider cache poisoning (found by the first live calibration run, 2026-07-14) — implemented + verified (new 7-assertion `cache-poisoning.regression.cjs` in `pnpm check`, 17 suites green; 693 poisoned `ProviderCache` rows purged from Supabase). **Incident:** the first `--live` calibration run returned identical mock-shaped results for all 20 pairs (overall 82, posts=12, engaged=16 — the mock fixture sizes). Root cause: cache keys carried no provider KIND — `tw:v2:tweets:<handle>:<limit>` is the same key for mock and live, and the LLM `cls:` keys include the model, which the mock provider echoes from `LLM_MODEL` (`gpt-5-mini`) — so earlier mock-mode calibrate runs (which used the Prisma store against the now-shared Supabase DB) poisoned every key the live run read. Cost impact of the bad run ≈ only the ~20 uncached narrative calls. **Fix (packages/cache):** both decorators now namespace keys by provider kind — `tw:v2:<kind>:…` / `cls:v2:<kind>:…` — resolved from an explicit constructor/factory param or defaulting to the same env the provider factories use (`TWITTER_PROVIDER`/`LLM_PROVIDER`); the calibration runner additionally uses an in-memory store for mock runs (mock data never persists to the shared DB — belt + suspenders). Worker needs no change (env-derived defaults match what it builds). The incident scenario itself is a regression test: a live provider behind a mock-poisoned shared store bypasses the junk and calls through.
- Unit 29G: Severity-Tiered Risk Caps + Synthetic Negative Controls — implemented + verified 2026-07-14 (full `pnpm build` green; `pnpm check` 16 suites / 303 assertions green incl. the new 17-assertion `negative-controls.regression.cjs`; mock calibrate run executes the 20 real pairs and skips the 7 synthetics). Per `context/specs/29g-severity-tiered-risk-caps.md`, motivated by the user's **v26 calibration set** (saved to `context/krypto-kol-calibration-pairs-v26.md`; original doc also at `context/krypto_kol_calibration_pairs_verified_12.md`). **pairs.json upgraded to 27 pairs** (20 real incl. the retained Vitalik pair + 7 `synthetic: true` negative controls with `labelConfidence` per pair); the runner now SKIPS synthetic pairs (patterns, not real accounts) — they are encoded as scoring fixtures instead. **Gates (rule 6 severity tiers):** `RiskGateInput` += `brandSafety`; new `BOT_GATE_AVOID` (97 — overwhelming fake/farmed engagement caps AVOID), `PROMO_GATE_WEAK` (95 + unrelated>0.5 → WEAK; promo saturation alone still never AVOID, rule 14), `BRAND_GATE_WEAK` (<40 → WEAK) / `BRAND_GATE_AVOID` (<20 → AVOID — casino mismatch, deceptive claims, phishing, impersonation); caps combine as minimum; 29F founder floor already yields to any fired gate (severe risk beats authority). All 7 synthetic patterns verified as fixtures: AirdropFarmHub/EngagementRaidBot/MemeGiveawayHost/CasinoBonusFeed/UnverifiedSportsTips → AVOID (the last despite CF 85 — category overlap must not defeat caps), CryptoMacroSponsor/AltcoinDealsDesk → WEAK not AVOID (promo-heavy retains awareness value), founder+severe-risk → AVOID. **Recorded future units (not in scope, from v26 rules):** campaign-goal-conditional verdicts (rule 3: goal-specific sub-scores; the "verdict assumes a normal product-relevant campaign" note covers today), independence-vs-controllability fields (rule 11), prediction/gambling compliance gate beyond the legal_or_regulatory flag (rule 13).
- Unit 29F: Relationship-Aware Verdicts (authority fit) — implemented + verified 2026-07-14 (full `pnpm build` green; `pnpm check` 15 suites / 286 assertions green incl. the new 27-assertion `authority-rules.regression.cjs`; mock calibration run executes all 12 pairs end-to-end). Per `context/specs/29f-relationship-aware-verdicts.md`, motivated by the user's verified 12-pair set (`context/krypto_kol_calibration_pairs_verified_12.md`) — **all 12 pairs loaded into `scripts/calibration/pairs.json`** with the runner's expected-verdict grammar. Authority is a **verdict floor/cap modifier, not a weighted metric** (9 slugs unchanged). **LLM (29B call extended):** `ContentFitAssessmentSchema` += optional `relationship` (`founder_or_core_team | adjacent_ecosystem_authority | independent_specialist | media_or_news | none`) + `relationshipEvidence`; `AssessContentFitInput.kol` += `profile` (the bio is the evidence — "inventor of Uniswap Protocol"); OpenAI strict schema + prompt define the five categories precisely (org-specific founder vs chain-founder distinction, weaker-category-when-unsure); mock rules deterministic (founder terms + org mention → founder; founder terms alone → adjacent; news/media → media; analyst/investigator → specialist); fit cache key now profile-sensitive (id + bio). **Scoring:** `applyAuthorityRules(verdict, ctx)` after the risk gates — founder floor `AUTHORITY_FLOOR_FOUNDER` (GOOD) applied only when no risk gate fired AND brand_safety ≥ `AUTHORITY_MIN_BRAND_SAFETY` (60) — noisy engagement must not collapse a founder pair, severe risk still wins; media/news cap `MEDIA_CAP` (OKAY) unless `EAM ≥ MEDIA_CAP_EAM_EXEMPT` (75) — reach ≠ fit, audience proof exempts; adjacent authority + specialists earn verdicts through metrics (no floor/cap); relationship + any floor/cap surfaced in overall reasons. Pipeline passes `kolProfile` into `assessContentFit`. Constants all in `weights.ts` for 29E tuning. Next: user-approved `--live` calibration run over the 12 pairs → tune anchors/gates against the labels.
- Unit 29E (runner half): Calibration Harness — implemented + verified 2026-07-14 (offline: matcher selftest 11/11; mock-mode end-to-end run PASS + `last-run.json` written; live-guard aborts exit 2 without `--live`; MISS path exits 1; filter path works; `pairs.json` restored after the MISS test). Per `context/specs/29e-calibration-harness.md`. New `scripts/calibration/run-pairs.cjs` + root `pnpm calibrate`: loads root `.env` (tiny built-in parser, no new deps), builds providers exactly like the worker (env-selected Twitter/LLM wrapped in `withTwitterCache`/`withLlmCache` over `PrismaCacheStore`, miss-safe) so **cache-warm re-runs re-score without re-spending** — the tuning loop is edit `weights.ts` → `pnpm build` → `pnpm calibrate`. Expected-verdict grammar: exact (`STRONG`), at-least (`GOOD+`), at-most (`WEAK-`), set (`GOOD|STRONG`). **Live-spend guard**: aborts (exit 2) when providers resolve live unless `--live` passed; mock mode runs freely behind a loud "not meaningful" banner. Prints per pair: PASS/MISS, verdict/overall/confidence, all 8 components, sample sizes, wall-time (doubles as the 29D latency confirmation on live runs); writes structured `scripts/calibration/last-run.json` (gitignored) for tuning diffs; substring filter (`pnpm calibrate -- uniswap`); exit 1 on any miss (NOT in `pnpm check` — non-hermetic; only the pure `--selftest` is wired into the check chain, now 14 suites / 259 assertions). **Tuning half of 29E remains blocked on the user's ground-truth pairs** (only Uniswap × haydenzadams = STRONG recorded).
- Unit 29D: Pipeline Parallelism + Latency — implemented + verified 2026-07-14 (full `pnpm build` green; `pnpm check` 13 suites / 248 assertions green incl. the new 9-assertion `pipeline-latency.regression.cjs`). Per `context/specs/29d-pipeline-latency.md`. **Pipeline (`run-analysis.ts`):** (1) per-post engagement fetches now run with **bounded concurrency** (`DEFAULT_ENGAGEMENT_FETCH_CONCURRENCY` 6 posts in flight, each still firing replies∥quotes∥retweeters; override via `RunAnalysisOptions.engagementConcurrency` or `ANALYSIS_ENGAGEMENT_FETCH_CONCURRENCY`) — was 20 sequential batches, the dominant Twitter cost; index-ordered `mapConcurrent` keeps group order identical so dedupe/appearances/output are **byte-identical** to the sequential version (regression-proven); (2) website/docs ingestion promise starts first and is awaited only at org classification (overlaps the whole Twitter phase); (3) profiles+posts+replies collapsed into one 4-way `Promise.all` (was two rounds); (4) org ∥ KOL-content classifications now `Promise.all` (were serial). With 29B's 3-concurrent audience batches + 29C's audience∥fit parallelism, the live critical path drops from ~20+2+2 serial LLM/Twitter rounds toward ~4 engagement rounds + 2 LLM stages → targets ~2–2.5 min p50 (live timing to be confirmed on the next approved live run). **Caps rebalance:** `retweetersPerPost` 100→50 (weakest signal — scoring v2 weights retweets 0.5 — and the most numerous fetch; env override unchanged), synced in `architecture.md` caps table. `.env.example` += `ANALYSIS_ENGAGEMENT_FETCH_CONCURRENCY`. No provider/schema/scoring/worker/API changes.
- Unit 29C: Scoring v2 — implemented + verified 2026-07-14 (full `pnpm build` green; `pnpm check` 12 suites / 239 assertions green incl. the new 33-assertion `scoring-v2.regression.cjs`; mock pipeline E2E deterministic). Per `context/specs/29c-scoring-v2.md`. **THE BENCHMARK PASSES: a Uniswap × haydenzadams-shaped fixture (the saved 2026-07-10 live distribution, 300 classified) scores 92/STRONG/high — v1 scored the real run 40/WEAK.** The 9 `ScoreMetric` slugs kept; every tunable in `weights.ts` for 29E calibration. **Core changes (packages/scoring):** `curve(x, anchors)` piecewise-linear calibration (raw shares are NOT scores) with `EAM/CF/PROMO/BOT_RISK/GEO_ANCHORS`; **EAM v2** — targets from 29B `org.targetBuckets` (primary 1.0/secondary 0.5; keyword fallback), computed over HUMAN accounts only (bots/giveaway excluded — triple-punishment ended), source-weighted (reply/quote 1.0, retweet 0.5, follower 0.25), 30% share≈75/45%≈88; **AQ v2** — baseline-adjusted (first 10% junk free, `AQ_SLOPE` 180 past it) + repeat-engager bonus (29A `appearances`, max +8); `avgBotScore` excludes null botScores (missing data no longer deflates risk); **bot risk v2** — excess-above-baseline through `BOT_RISK_ANCHORS` + avg-bot nudge; **promo risk v2** — saturation×quality from 29B `postLabels` (`curve(promoShare) × (0.4 + 0.6·max(unrelatedShare, lowQualityShare))` — in-domain decent promos ≈ free), legacy pattern heuristic softened (10×, cap 60, low conf) as fallback, returns `unrelatedShare` for the gate; **brand safety v2** — 29B flags only (high 35/medium 15/low 5, floor 0; no flags → 100; promo leakage + meme penalty GONE); **content fit v2** — 29B rubric `curve(.3·adjacency+.4·overlap+.3·natural)` (3/5≈70, cap retired), token-overlap fallback at low conf; **CGF v2** — fuzzy goal normalization ("Developer Adoption"→`developer_adoption`), human-weighted coverage of union(goal buckets, primary targets), explicit unrecognized-goal reason; **geo v2** — real `Tweet.lang` share vs `REGION_LANGS` table through `GEO_ANCHORS` (neutral 85/medium when no region; legacy stubs only when no lang data); **weights v2** EAM .40/AQ .15/CF .15/CGF .15/BS .10/GLF .05; **gates v2** — blunt 70→WEAK gone; bot ≥85→cap OKAY, ≥95→cap WEAK; promo gates ONLY at ≥85 AND unrelatedShare>0.5→cap OKAY (`RiskGateInput` gains `promoUnrelatedShare`); **confidence v2** — keys off `engagedAccountsClassified` (≥100→2/≥30→1) + posts + web ingest + `hasEngagementText` (≥5 high/≥3 medium). **Types:** `ScoringSampleMeta` += `engagedAccountsClassified?`/`repeatEngagerShare?`; `ScoringEvidence` += `hasEngagementText?`; `ScoringInput` += `contentFitAssessment?`/`kolPostLangs?`; barrel exports anchors/gates/`curve`. **Pipeline:** `run-analysis.ts` runs `llm.assessContentFit` in PARALLEL with audience classification (failure degrades to token fallback, never fails the analysis — Invariant 8), computes `repeatEngagerShare`, threads langs/classified-count/text-presence. **Web:** `lib/metric-info.ts` ⓘ explainers rewritten to v2 semantics (baselines, saturation, flags, adjacency). Mock pipeline now 82/STRONG (was 57/OKAY) — correct for the well-matched mock scenario.
- Unit 29B: LLM Classification v2 — implemented + verified 2026-07-14 (full `pnpm build` green; `pnpm check` 11 suites / 206 assertions green incl. the new 28-assertion `classification-v2.regression.cjs`; mock pipeline E2E: valid deterministic report with 12 postLabels + 4 mediaLabels riding in the saved content classification). Per `context/specs/29b-llm-classification-v2.md`. The LLM still never emits scores — only labels + bounded 0-5 rubric ratings. **Shared (additive optional):** `OrgClassification.targetBuckets {primary, secondary}` (the org's wanted audience, LLM-inferred); `KolContentClassification.postLabels` (per-post `{postId,isPromo,promoRelated?,promoQuality?}` → 29C computes promo *saturation*), `.brandSafetyFlags` (8-flag enum + severity + post evidence), `.mediaLabels` (per-attached-image kind); new `ContentFitAssessmentSchema` (three 0-5 integer rubric dimensions + sharedTopics + rationale). **Interface:** `LlmProvider.assessContentFit(org, kolContent)` added (pair-specific semantic rubric, kept OUT of classifyKolContent so the per-KOL cache stays pair-independent). **OpenAI:** strict schemas extended + new `CONTENT_FIT_SCHEMA`; prompts — org asks for target buckets from the bucket enum (never bots/giveaway), KOL content lists posts with `[postId]` + per-post label/flag instructions, audience rows now carry 29A `said="…"` reply text + follower/following/tweets/since/verified stats with explicit junk-vs-substantive guidance, new content-fit rubric prompt ("adjacent counts ≥3", forbids 0-100 scores); **multimodal** — `classifyKolContent` attaches up to `OPENAI_MEDIA_IMAGE_LIMIT` (default 12) post images (photo url / video thumbnail) as `input_image` parts from the same sampled posts the prompt lists (client `respond()` gained an `images` param); **model tiering** — optional `LLM_MODEL_FAST` (factory) / `fastModel` option → second client used for bulk audience batches, usage stats merged; **audience batches now run with bounded concurrency 3** (was sequential), output deterministically ordered (sampler order); kolContent max tokens 2000→6000 (per-post labels need room). **Mock:** deterministic equivalents (targetBuckets keyword rules, per-post promo scan, rug/scam flag scan, media-URL-hint labels, token-overlap 0-5 rubric) — all schema-validated. **Cache:** namespace `cls:v1`→`cls:v2`; audience keys now include per-account engagement text (it affects classification); `assessContentFit` cached under new `fit` kind (content-addressed org+content+model, reuses content TTL — no config change); stats gain `fit`. `.env.example` += `LLM_MODEL_FAST`, `OPENAI_MEDIA_IMAGE_LIMIT`. Pipeline/scoring untouched — 29C wires consumption (`assessContentFit` is exercised by checks, not yet called by the pipeline).
- Unit 29A: Provider Data Enrichment — implemented + verified 2026-07-14 (full `pnpm build` green; `pnpm check` all 10 suites / 178 assertions green incl. the new 28-assertion `data-enrichment.regression.cjs`; offline mock-pipeline E2E: valid deterministic report, unchanged scoring). First implementation sub-unit of the Unit 29 accuracy overhaul (`context/specs/29a-provider-data-enrichment.md`). **Shared (additive optional only):** `TweetMediaSchema` + `Tweet.media` (photo `url` / video+gif `previewUrl` thumbnail); `EngagedAccountRaw.text` (reply/quote body, 500-char bound, lone-surrogate-stripped — Postgres jsonb + OpenAI both reject lone surrogates) + `EngagedAccountRaw.appearances` (int ≥1). Old payloads still validate; no Prisma change. **TwitterAPI.io provider:** `normalizeTweet` maps `extendedEntities.media` (fallback `entities.media`, unknown/garbage items skipped); `normalizeEngaged` gains a sanitized `text` param; `getTweetReplies`/`getTweetQuotes` now pass the engagement tweet's text (previously discarded — the P0 roadmap gap); retweeters unchanged (no text exists). **Mock:** every pool account carries a bucket-appropriate deterministic `replyText` (substantive dev/founder/DeFi/trader/VC replies vs "wen airdrop ser 🪂"/giveaway-claim/"gm" junk) surfaced on REPLY/QUOTE (not RETWEET); post templates gain photo-chart/meme/video media fixtures; new `selectPool()` (`selectAccounts` kept as its user-only wrapper). **Pipeline:** `collectEngagedAccounts` counts `appearances` = distinct analyzed posts per account (same-post duplicates don't inflate; counting continues past the `maxUnique` cap so repeat evidence isn't order-truncated; dedupe/cap/first-occurrence semantics unchanged). **Cache:** twitter cache namespace `tw:v1`→`tw:v2` (pre-enrichment cached payloads must not starve an enriched analysis); LLM `cls:` namespace bump deferred to 29B when classification inputs actually change. Consumers (LLM prompts/scoring) intentionally untouched — 29B/29C wire the new signals. Also: `scripts/calibration/pairs.json` seeded with ground-truth pair #1 (Uniswap × haydenzadams = STRONG, user-confirmed; v1 scored it 40/WEAK).
- Supabase go-live (2026-07-14): migrated off local Postgres onto the Supabase project and activated real Google OAuth — both verified end-to-end. **DB:** `.env` `DATABASE_URL` → Supabase **transaction pooler** (6543, `?pgbouncer=true`, app runtime), `DIRECT_URL` → Supabase **session pooler** (5432, pg-boss + Prisma migrations); `prisma db push` synced the full schema (incl. `User`) to Supabase; a mock analysis ran QUEUED→RUNNING→COMPLETED against Supabase (report saved, then cleaned up), proving app + pg-boss + worker all connect (session pooler supports pg-boss's LISTEN/NOTIFY + advisory locks). **Auth:** wired the client-side "Continue with Google" (`components/auth/login-form.tsx` — `signInWithOAuth({provider:'google', redirectTo: <origin>/auth/callback})`, `@supabase/ssr` browser client dynamic-imported so it stays off the dev bundle; committed + pushed). With `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` set, `resolveAuthMode`→supabase; drove the full flow in Chrome (login → Google consent → Supabase `/auth/v1/callback` → our `/auth/callback` PKCE `exchangeCodeForSession` → session) and confirmed a Supabase `User` row (`id` = Supabase UUID, `email`, `lastLoginAt`) + the nav showing the signed-in email. **Handover notes:** `.env` is gitignored (coworker needs the secrets shared out-of-band); the shared Supabase DB means no re-migration; a tester's Google account must be added under the OAuth consent screen's **Test users** and the app run on **port 3000** (the whitelisted redirect); run the worker (`pnpm dev:worker`) alongside the web app or analyses sit in QUEUED. Known cosmetic: Next 16 logs a `middleware`→`proxy` deprecation warning (works; rename later). `.env` never committed.
- Unit 28: User Authentication — implemented on 2026-07-13 (full `pnpm build` across all 14 projects + `pnpm check` 9 suites green, incl. the new 24-assertion `auth.regression.cjs`; live dev E2E is the orchestrator's step). Replaces the per-browser `kolfit_owner` cookie as the *sole* notion of ownership with **real user accounts** via a small **auth seam** (like the provider abstractions). **New `packages/auth` (`@kol-fit/auth`)** — the pure, framework-agnostic security core (no Next/Prisma/Supabase/zod; only `node:crypto`; `types:["node"]`): `signSessionToken`/`verifySessionToken` (stateless `${userId}.${base64url(hmacSHA256(userId, secret))}`, constant-time `timingSafeEqual`, null on any tamper/malformed/secret-mismatch, never throws), `resolveAuthMode` (→`supabase` iff both `NEXT_PUBLIC_SUPABASE_URL`+`NEXT_PUBLIC_SUPABASE_ANON_KEY` set, else `dev`), `devLoginAllowed` (fail-closed: no passwordless login in prod unless `AUTH_DEV_LOGIN=true`), `AuthUser`/`AuthMode` types. **Prisma:** additive `User` model (`id` cuid dev / Supabase UUID prod, unique `email`, `createdAt`, `lastLoginAt`); `AnalysisRequest.ownerId` stays a **nullable free string** (cookie id OR `User.id`) — deliberately **no FK** (existing cookie rows would violate it); `prisma generate` picks it up on build (`prisma.user` delegate confirmed). **Web glue `apps/web/lib/auth/`**: `session.ts` (dev `kolfit_session` httpOnly HMAC cookie, 30d, secret from `AUTH_SESSION_SECRET`; unset secret ⇒ sessions disabled/logged-out), `supabase.ts` (isolated adapter — `createServerClient({cookies:{getAll,setAll}})` per the current official `@supabase/ssr` App-Router guide, reads the user via **`getClaims()` never `getSession()`**, mirror-upserts the `User`; **@supabase/ssr reached only via dynamic `import()`** so the dev runtime never loads it; marked "activate + verify at deploy — not yet live-verified"), `current-user.ts` (the seam: `getCurrentUserId`/`getCurrentUser`; supabase branch dynamic-imported; `unstable_rethrow` so Next's dynamic-render bailout isn't swallowed), `claim.ts` (`claimAnonymousReports` re-assigns `AnalysisRequest.ownerId` cookie→user, best-effort, logs count only), `index.ts` barrel. **Reworked `owner.ts`** (same `getOwnerId`/`ensureOwnerId` names) → signed-in user id when present, else the anon cookie — so **all six existing call sites get per-user scoping + per-user rate limiting automatically** (ownership id *becomes* the user id). **Routes:** `POST /api/auth/session` (dev login: guard `devLoginAllowed`+dev mode else 404, validate email, upsert `User`, set session, claim, `ok({id,email})`), `DELETE` (sign out + supabase signOut in supabase mode), `GET/POST /auth/callback` (supabase-only: `exchangeCodeForSession`→mirror-upsert→claim→redirect home; isolated, unverified). **Middleware** `apps/web/middleware.ts`: **no-op in dev**; in supabase mode refreshes the session per the official `@supabase/ssr` pattern (getClaims + cookie passthrough, matcher excluding static assets); the mode check is inlined (not importing `@kol-fit/auth`) to keep `node:crypto` out of the Edge bundle, and @supabase/ssr is dynamic-imported. **UI:** `/login` page + `components/auth/login-form.tsx` (branch on server-provided mode: dev email field → `/analyses`; supabase minimal magic-link/OAuth entry) + `components/auth/user-menu.tsx` (email + Sign out, or Sign in link) slotted into `top-nav.tsx`; the shell resolves the user server-side so the root layout is now `force-dynamic` (otherwise `/` would serve a stale build-time logged-out nav). **Deps:** `@supabase/ssr`+`@supabase/supabase-js`+`@kol-fit/auth` on `apps/web` (supabase kept off the dev runtime path via dynamic import); none added to `packages/auth`. **Regression** `scripts/checks/auth.regression.cjs` (24 assertions: token round-trip, tamper/wrong-secret/malformed→null, constant-time valid-length-wrong-sig→null, never-throws, `resolveAuthMode` both/either/neither, `devLoginAllowed` prod/flag/non-prod) wired into `pnpm check` + `check:auth`. `.env.example` (`AUTH_SESSION_SECRET`, commented `AUTH_DEV_LOGIN`/`NEXT_PUBLIC_SUPABASE_*`) + architecture.md auth section updated. Non-goals honored: no workspaces/roles, no password reset/verification/MFA, app not hard-gated, admin (Unit 27) auth unchanged. **Live dev E2E verified (2026-07-13)** against a throwaway Postgres via the real `next dev` server + curl cookie-jar: (1) anonymous create → `ownerId` = the `kolfit_owner` cookie id; (2) dev login (`Me@Example.com ` → normalized `me@example.com`) → `kolfit_session` set, `User` row created, and the anonymous report's `ownerId` re-assigned to the user id (log `claimed 1 anonymous report(s)`); (3) logged-in create → `ownerId` = user id; (4) the list returns both (2) for the user; (5) with `MAX_ANALYSES_PER_OWNER_PER_DAY=2` a 3rd create returns **429 `rate_limited`** — proving the cap now keys on the user id and counts the claimed report; (6) sign out → session cleared, anonymous list back to 0. No server errors. Non-breaking rollout: with no `AUTH_SESSION_SECRET` set, dev sessions are disabled and the app runs anonymously exactly as before — enabling login locally needs `AUTH_SESSION_SECRET` + `prisma db push` (the additive `User` model). The Supabase adapter remains deploy-activated + not yet live-verified.

- Unit 27: Admin Panel — implemented + verified live on 2026-07-13 (full `pnpm build` + `pnpm check` 8 suites green). A **read-only** operator surface at `/admin`, gated by a single shared password. **No Prisma schema change, no new deps, no new data collection** — it only projects existing rows (`AnalysisRequest`, `AnalysisJob`, `Report`, `ReportDelivery`, `ProviderUsageLog`) and performs **no mutations** (no delete/re-run/export). Auth (`apps/web/lib/admin/auth.ts`): `ADMIN_PASSWORD` compared with `timingSafeEqual`; `POST/DELETE /api/admin/session` sets/clears an httpOnly `kolfit_admin` cookie = `sha256("kolfit-admin-v1:" + ADMIN_PASSWORD)`; every page calls `requireAdmin()` (redirects to `/admin/login`). **Fail-closed:** unset password ⇒ panel disabled (pages render "not configured", session route 404s) — never open by default; rotating the password logs everyone out. Data layer (`apps/web/lib/admin/queries.ts`, never owner-scoped): `getAdminOverview()` (rolling 24h/7d/all-time counts, job-status mix, distinct browsers, leads + distinct emails/Telegram handles, provider spend/tokens, Unit-26 cap headroom, verdict mix, top KOLs/orgs, recent activity), `listAdminAnalyses({q})` (search over org/KOL handle + ownerId), `listAdminLeads()`, `getAdminUsage()` (totals per provider + recent calls) — cursor pagination reusing the `analyses-list.ts` idiom. Pages `app/admin/{,analyses,leads,usage,login}` + `components/admin/*` (existing theme tokens/primitives only; plain tables + stat cards, no chart lib). **One shared-package change:** `"unauthorized"` added to `ApiErrorCodeSchema` (`packages/shared/src/api.ts`) for the 401 — additive; error-taxonomy suite still green. **One justified relaxation:** a valid admin session bypasses Unit 25 owner scoping in `GET /api/analyses/[id]` (documented exception; a non-admin non-owner still gets 404). `.env.example` + architecture.md auth section updated. **Verified live** against a throwaway Postgres seeded with 3 distinct owner cookies / 7 analyses / mixed job states / 3 leads / 8 usage logs: no cookie ⇒ 307 → login; wrong password ⇒ 401 + no cookie; correct ⇒ httpOnly cookie + all four pages rendering **DB-exact** numbers (analyses 6/24h vs 7 all-time — window math correct; 3 browsers; jobs 1/1/4/1; spend $0.41/24h = 3×$0.1353 and $0.54 all-time; 168 provider requests = 4×42; openai $0.38 / twitterapi $0.16; distinct contacts 2 emails + 2 Telegram; verdict mix; top KOL cobie ×4); `?q=aave` and a case-insensitive `?q=OWNER-BRAVO` both filter correctly; admin opens another browser's report (200) while a stranger still 404s; after logout ⇒ 404 + redirect; with `ADMIN_PASSWORD` unset a **previously valid cookie** renders only the "not configured" notice with **zero seeded values leaked**. Not linked from the public `TopNav` (reachable at `/admin` directly) — deliberate. Real accounts/roles remain the future auth unit. See `context/specs/27-admin-panel.md`.
- Unit 26: Abuse & cost controls (denial-of-wallet mitigation) — implemented + verified on 2026-07-13 (full `pnpm build` across all projects + `pnpm check` 8 suites green, incl. the new 43-assertion suite). Bounds worst-case third-party spend on the public, unauthenticated `POST /api/analyses` without new infra (no Redis; DB-backed). **No Prisma schema change, no new deps.** New `packages/shared/src/limits.ts` (exported from index): `AbuseLimits`/`ABUSE_LIMITS` (perOwnerPerDay 10, globalPerDay 200, maxDailySpendUsd 0=disabled)/`ABUSE_LIMIT_ENV_VARS` + pure `resolveAbuseLimits(env)` (posInt for count caps, non-neg for spend; env `{}` default so the shared package needs no Node types). New `apps/web/lib/rate-limit.ts`: `checkAnalysisRateLimit(ownerId)` — rolling 24h window, per-owner + global `AnalysisRequest.count()` reads, plus (when `MAX_DAILY_SPEND_USD>0`) a `ProviderUsageLog.costUsd` sum over its `createdAt` (column confirmed present). Wired into `POST /api/analyses` (after `ensureOwnerId`, before create): refusal → **HTTP 429** `rate_limited`, kept inside the existing `try` so DB errors fall to the 500 path. Worker retry: `RETRYABLE_CODES`/`isRetryable`/`decideRetry` in `apps/worker/src/errors.ts` (six transient codes: twitter/llm rate_limited/timeout/unavailable; auth/config/not_found/invalid_output/analysis_failed terminal); `enqueueAnalysisRun(payload, {startAfterSeconds})` maps a positive value to pg-boss `startAfter`; the `analysis-run.ts` catch now, on a retryable code with attempts left (`ANALYSIS_MAX_ATTEMPTS` default 3), sets the job QUEUED + re-enqueues with linear backoff (`ANALYSIS_RETRY_DELAY_SECONDS` default 60 × attempt) and acks — never re-throws (batch isolation + idempotency preserved). The QUEUED→RUNNING update now captures the incremented `attempts` onto the in-memory job so the retry decision sees the post-increment count (first run = 1). Worker `index.ts` logs a LIVE-providers startup warning when `TWITTER_PROVIDER=twitterapi`/`LLM_PROVIDER=openai`. `.env.example` gained the five new vars with a public-endpoint warning. New `scripts/checks/abuse-and-retry.regression.cjs` (defaults/overrides/invalid-env for `resolveAbuseLimits`; every retryable/terminal code + attempts-budget for `decideRetry`; exact `RETRYABLE_CODES` set) wired into `pnpm check` + a `check:abuse-and-retry` alias. architecture.md gained an "Abuse & Cost Controls (Unit 26)" subsection. Real per-user auth + per-IP limiting stay the future fix (`ownerId` maps onto a real user id then). **Retry loop verified live end-to-end (2026-07-13)** against a throwaway local Postgres + real pg-boss + the real worker binary, faults injected only via env/stubs (no product code changed): (A) a retryable failure (dead endpoint → `twitter_unavailable`) exhausts to `FAILED` at `ANALYSIS_MAX_ATTEMPTS`, the retry pg-boss job re-enqueued at exactly the linear backoff (`start_after − created_on` = 1.00s) and **no** report written; (B) a terminal `twitter_auth` (401 stub) fails immediately at attempt 1 with **no** retry job enqueued; (C) a transient `twitter_rate_limited` (429-then-200 stub) recovers via delayed re-enqueue to `COMPLETED` with exactly **one** saved report (idempotent, no duplicates). Closes the earlier "verified by reasoning only" gap; no defect found.
- Unit 25: Per-browser report ownership (pre-auth scoping) — implemented + verified (full `pnpm build` + `pnpm check` 7 suites green; DB isolation check). "See only your own reports" without auth (deferred): new nullable `AnalysisRequest.ownerId` (+ `@@index([ownerId, createdAt])`, pushed to DB) set from a private `kolfit_owner` cookie (`apps/web/lib/owner.ts`: `getOwnerId` read-only + `ensureOwnerId` create/set httpOnly, sameSite lax, 1y). Wired: `POST /api/analyses` tags the request with the owner (cookie set on first submit); `listAnalyses({ownerId})` filters (no owner → empty); `GET /api/analyses` + the `/analyses` page pass the owner; `GET /api/analyses/[id]` and `POST …/deliver` 404 for non-owners (404 not 403 so existence isn't leaked). Caches (`ProviderCache`) stay shared/un-scoped (handle/input-keyed, public-account data). Limits documented (per-browser; cleared cookies/other devices lose visibility; pre-Unit-25 null-owner reports hidden) — maps onto a real user id when auth lands. Verified owner isolation at the DB level (owner A sees 2, owner B sees 1). architecture.md auth section updated.
- Unit 24: Report delivery & lead capture (email PDF) — implemented + verified (full `pnpm build` all 12 projects + `pnpm check` 83/83; disk-light online delivery run). The report stays fully visible; a "Get the full report" card captures **email and/or Telegram**. New Prisma `ReportDelivery` model + `DeliveryStatus` enum (leads table, per-channel status) pushed to local DB. New **`packages/mail`**: `MailProvider` abstraction — **mock** (logs, default, no creds) + **resend** (real, lazy-`import`ed so it's not a build dep; needs `RESEND_API_KEY`+`MAIL_FROM`) via `createMailProvider()`. New **`packages/report-pdf`** (ESM; `@react-pdf/renderer` + React 19): `renderReportPdf(fitReport, scores, meta)` → a light, printable, Morpho-blue-accented PDF (verdict/score, key takeaways, colored metric bars, audience table with low-quality flags, recommendation, narratives, evidence, page numbers) — no headless browser. Queue: `report.deliver` queue + `ReportDeliverPayloadSchema` + `enqueueReportDeliver` + `boss.createQueue`. Worker: `processReportDelivery` (renders PDF, sends via mail provider, marks SENT/FAILED; idempotent, isolated, safe logging) registered on a second `boss.work`; report-pdf loaded via dynamic `import()` (ESM-from-CJS, like pg-boss). API: thin `POST /api/analyses/[id]/deliver` (email→PENDING+enqueue, telegram→captured/SKIPPED). UI: `components/report/get-report.tsx` slotted under the report hero. `ReportDeliverInputSchema` added to `@kol-fit/shared` (≥1 channel, email format, `@`-stripped telegram). New `scripts/checks/report-delivery.regression.cjs` (10 assertions: schema variants, mock mail, PDF `%PDF` render) wired into `pnpm check`. `.env.example` (`MAIL_PROVIDER`/`RESEND_*`) + architecture.md updated. Online proof: created a ReportDelivery, handler rendered an 11KB PDF, mock-sent it, row → SENT (cleaned up). **No real email without `MAIL_PROVIDER=resend` + key.** Telegram capture-only (delivery deferred — bot-start constraint).
- Enhancement: immersive in-progress experience (`RunningExperience` in `analysis-status.tsx`). Replaces the bare QUEUED/RUNNING card with a live **elapsed timer** (1s ticker off `job.startedAt`), an honest estimate ("~5–7 min", with a "taking longer than usual" message past ~7m), and a **staged walkthrough of the real pipeline** (read profiles → measure real engagement → classify audience with AI → score & write report) where each stage's copy explains *why it takes time* — framed around the product's differentiator (measuring who actually engages, not follower counts). The likely-active stage is estimated from elapsed time (the timer is the truthful anchor; no fake percentage — honors ui-context "no fake progress"). The `AudienceField` runs quietly behind the panel (the audience being sifted) under a readability veil. `pnpm build` green. (A future option: real per-stage progress events from the worker for exact stage tracking.)
- Enhancement: interactive landing hero (`components/audience-field.tsx`) on `/`. A canvas particle field embodying the product thesis — a drifting crowd of faint gray accounts with a ~34% subset lit electric-blue (the genuinely *engaged* audience), networked with links; the cursor acts like the analyzer, energizing + attracting nearby accounts (crowd→engaged) and linking them to itself. No dependencies (canvas + rAF), DPR-scaled, `ResizeObserver`-sized, `prefers-reduced-motion` → static frame, `aria-hidden`, cleaned up on unmount. Tuned bolder (denser/brighter/larger particles, stronger cursor pull) after first pass read too subtle. Landing rebuilt so the **form is the focal point lifted onto the field** (blue halo, light veil), not a card buried below it — with cursor able to reach the field in the margins (content `pointer-events-none`, form re-enabled). `pnpm build` green.
- Enhancement (post-Unit-23): Morpho-inspired retheme of the whole web surface. Adopted Morpho's design language (brand.morpho.org) — dark **warm-charcoal** neutrals (`#15181A`→`#222529`→`#383B3E`) replacing the cold blue-black, and a single **electric-blue** brand accent `#2973FF`/`#5792FF` replacing the violet; softer/diffuse shadows, rounder radii (base `--radius` 0.5→0.625rem), more air. Token-driven: swapped the palette + accent + borders in `globals.css` so the whole app flips at once; repurposed `--shadow-card` into a soft box-shadow exposed as a `shadow-card` Tailwind utility and replaced the heavy inline shadows. Fixed the few hardcoded accents (nav logo gradient, app-shell ambient glow, audience-donut bucket palette → Morpho blues). Semantic score colors (green/amber/red) kept **separate** from the blue accent for data legibility. Design language only — no Morpho identity assets (our own mark/gradients). Verified: full `pnpm build` + `pnpm check` (73) green; `shadow-card` utility confirmed in the compiled CSS. `ui-context.md` palette/theme section updated. (Type stays self-hosted Geist for now; Morpho uses FK Grotesk — a switch is a future option.)
- Unit 23: Cross-analysis classification cache (LLM reuse) — implemented + verified (full `pnpm build` + `pnpm check` 73/73; disk-light online proof). Reuses the expensive LLM classifications across analyses so re-pairings don't recompute them. New `packages/cache/src/llm-cache.ts` `withLlmCache(inner, store, config)` / `CachingLlmProvider`: caches `classifyOrgProfile`/`classifyKolContent`/`classifyAudienceAccounts`, **content-addressed** (sha-256 of the call's actual inputs + model under a versioned `cls:v1:` namespace — content: handle + sorted post/reply ids; audience: sorted engaged-account ids+sources + `OPENAI_AUDIENCE_CLASSIFICATION_LIMIT`; org: handle + brief + website hash), so a cached result is served only for identical inputs (no staleness, no cross-pair leakage). Re-validates cached payloads against their Zod schema on read (miss on drift); miss-safe; **`generateFitReport` never cached** (pair-specific); forwards inner `model`/`getUsageStats` + per-method hit/miss stats. `resolveClassificationCacheConfig()` (`CLASSIFICATION_CACHE_ENABLED`, base TTL 14d + per-kind overrides). Reuses the existing `ProviderCache` KV table (`provider: "llm"`) — **no schema change, no LLM-interface change, no pipeline change**. Worker `buildProviders()` wraps the LLM provider with `withLlmCache(createLlmProvider(), new PrismaCacheStore("llm"), …)`; `logProviderUsage` records classification cache hits/misses in the LLM `meta`. New `scripts/checks/llm-cache.regression.cjs` (12 assertions: identical→hit, reorder-independent, changed input→miss, report-never-cached, miss-safe, re-validate bad payload, disabled pass-through) wired into `pnpm check`. `.env.example` + architecture.md updated. Online proof (local DB, mock): orgA×kolB then orgD×kolB → run 2 reuses kolB content+audience (hits) and recomputes only the orgD classification (miss). Pipeline/providers stay `@kol-fit/db`-free.
- Enhancement (post-Unit-22): profile pics + Geist typography premium pass. Switched the web font from Inter/JetBrains → **Geist Sans/Mono** (self-hosted via the `geist` package; `--font-sans`/`--font-mono` mapped to `--font-geist-*` in globals.css). New `components/ui/avatar.tsx`: org/KOL avatar that tries the provider `avatarUrl`, then an avatar-by-handle lookup (`unavatar.io/twitter/<handle>` — verified to return real X pics for existing report handles), then a deterministic gradient monogram; optional verified badge. Threaded profile data end-to-end: new `ProfileSnapshotSchema` + optional additive `profiles: {org, kol}` on `FitReportSchema` (no migration, schema version unchanged); pipeline (`run-analysis.ts`) builds snapshots from the fetched Twitter profiles into the parsed report (rides the existing `report` JSON — no worker/DTO change). Report hero rebuilt with an org-vs-KOL avatar row (name + follower count + verified); reports list rows use real avatars. Existing reports get real pics via the by-handle fallback; new runs store the provider URL. Full `pnpm build` + `pnpm check` (61) green.
- Unit 22: UI/UX overhaul (client-facing polish) — implemented across the whole web surface (full `pnpm build` + `pnpm check` 61/61 green). Per `context/specs/22-ui-ux-overhaul.md`. Direction: show everything (no hiding), hierarchy-driven polish, summary as points, ⓘ metric explainers, client-facing. **Report page** (`fit-report-view.tsx`) rebuilt as an analyst brief: verdict hero band with a radial `ScoreGauge` + one-line takeaway + confidence/sample chips; **key takeaways** as bullets (authored `keyTakeaways` if present, else sentence-split `summary`); computed **Why** section (what works / what to watch, derived from the scored metrics, not hardcoded); **audience donut** (`AudienceDonut`, all 15 buckets ranked, low-quality flagged) as the visual centerpiece; **grouped score bars** (`MetricGroups`, Fit vs Risk, weights) each with a click **`InfoHint` ⓘ** popover sourced from `lib/metric-info.ts` (authored copy for all 9 metrics); recommendation callout, content/engagement, risk cards, evidence footer. New components: `ui/info-hint.tsx` (custom accessible popover, no new deps), `report/score-gauge.tsx`, `report/audience-donut.tsx`, `report/metric-groups.tsx`. Removed dead `report/score-matrix.tsx`/`audience-bars.tsx`/`report-section.tsx`. **Reports list** (`analyses-list.tsx`) polished (avatars, colored verdict/score, status pills). **Nav** (`top-nav.tsx`, now client) with active-route state; **home** hero refresh; **app-shell** ambient glow; **status** running state gets an indeterminate bar + realistic "a few minutes" expectation (globals.css keyframes, reduced-motion safe). **Data-model:** added optional additive `keyTakeaways: string[]` to `FitReportSchema` + OpenAI schema/prompt/normalize + mock (no migration, `REPORT_SCHEMA_VERSION` unchanged; old reports fall back to summary-split bullets). No scoring/pipeline/provider/queue/API-shape change. Design reviewed first via a live Artifact mockup (real getflipx × nebula2004 data) before porting.
- Enhancement (post-plan): Executive summary paragraph on the report. Added optional `summary: string` to the spine of `FitReportSchema` (`packages/shared/src/report.ts`) — a 3-5 sentence readable prose conclusion tying the metrics into a takeaway; optional so old saved reports (no `summary`) still validate and `REPORT_SCHEMA_VERSION` is unchanged. OpenAI provider: `summary` added to `REPORT_NARRATIVE_SCHEMA` (properties + required), `buildReportPrompt` instructs a skimmable exec summary that references the verdict qualitatively but invents no numbers, `assembleFitReport` maps `summary` (empty→undefined). Mock provider: deterministic summary paragraph (verdict phrase + themes + top audience bucket + a go/caution closer). Renderer: new "Summary" card in `fit-report-view.tsx` directly under the hero (degrades gracefully when absent). Full `pnpm build` + `pnpm check` green; mock run prints a coherent paragraph. No schema/migration change; no scoring change (numbers stay deterministic).
- Unit 21: Error Handling & Reliability Pass — implemented and verified (full `pnpm build` + `pnpm check` green, 61 assertions; disk-light online DB check 5/5). **No schema change** (`AnalysisJob.attempts`/`errorCode`/`errorMessage` already existed). New `apps/worker/src/errors.ts`: `AnalysisErrorCode` (stable slugs: `twitter_auth|rate_limited|not_found|timeout|unavailable`, `llm_auth|rate_limited|invalid_output|timeout|config|unavailable`, `analysis_failed`) + `classifyAnalysisError(error)` mapping `TwitterApiError`/`OpenAiError` by `.code` (with a structural cross-realm fallback) to a slug + a **fixed, safe, user-friendly message** — raw provider/exception text never persisted/logged/shown. Worker handler (`analysis-run.ts`): increments `attempts` on QUEUED→RUNNING; catch block now stores the classified `code`/`message` (was a generic `worker_error`) and logs only `code` + a bounded message (no raw error object). Status DTO/route: added `job.attempts` to `AnalysisStatusResponse` + `/api/analyses/[id]`. UI (`analysis-status.tsx`): richer `FailedBody` (friendly message + error code + attempt count + `failedAt` + "start a new analysis"/"back to reports" actions); replaced the leftover duplicate `VERDICT_TONE`/inline badge in `CompletedBody` with the shared `VerdictBadge` (Unit 20). New `scripts/checks/error-taxonomy.regression.cjs` (22 assertions: every provider code mapping, unknown→fallback, non-Error/null, no-secret-leakage, structural fallback) wired into `pnpm check`. Architecture.md gained an "Error Handling & Failure Taxonomy" note. No pg-boss retry/delivery change (per-job isolation + idempotency preserved; auto-retry deferred to a future `analysis.retry`); no provider/scoring/LLM logic change. DB check simulated a `TwitterApiError("not_found")` failure → job FAILED, `errorCode: twitter_not_found`, `attempts=1`, `failedAt` set, stored message safe (no raw 404 body) and human-friendly (seed row cleaned up).
- Unit 20: Reports List & Saved Report History — implemented and verified (full `pnpm build` incl. `apps/web` + `pnpm check` green; disk-light online DB check 6/6). Read-only UI + read API; **no schema change** (all fields + indexes already existed on `AnalysisRequest`/`Report`). New `apps/web/lib/analyses-list.ts`: `AnalysisListItem`/`AnalysisListResponse` DTO + `listAnalyses({limit,cursor})` (Prisma `analysisRequest.findMany`, newest-first `createdAt desc`, **summary-select only** — never loads `report.report`/`report.scores` blobs — `take: limit+1` sentinel for cursor pagination) + `clampLimit` (1..100, default 25). New `GET /api/analyses` handler added to the existing route (thin; `force-dynamic`; parses `limit`/`cursor`, clamps noise instead of 400ing, `ok`/`err` + 500 on DB error, no leak) — `POST` untouched. New `/analyses` server-component page (`apps/web/app/analyses/page.tsx`, `force-dynamic`, reads first page directly via `listAnalyses()` — no self-fetch — with a graceful "couldn't load" fallback). New `components/analyses-list.tsx`: responsive semantic table (`overflow-x-auto`), each row links to `/analyses/[id]`, columns KOL→Org / Status / Verdict / Score / Created, mono score+date, `Intl.DateTimeFormat` (no date lib), "—" for non-completed verdict/score, cursor-based "Load more" (`?cursor=` server-rendered link), and an empty state ("No reports yet" → link to `/`). Extracted shared `components/report/verdict-badge.tsx` (`VERDICT_TONE` + `VerdictBadge`, null→"—") and refactored `fit-report-view.tsx` to reuse it (dropped now-unused `cn`/`ReportVerdict` imports). `top-nav.tsx`: added a "Reports" link to `/analyses` + logo links home (removed the stale "no routes yet" comment). No `components/ui/*` primitive rewritten; no worker/pipeline/provider/scoring change. DB check seeded a COMPLETED+report / RUNNING / FAILED trio → verified newest-first order, correct verdict/score/status mapping, `report:null` for non-completed rows, and cursor pagination with no page overlap (seed rows cleaned up).
- Unit 19: Caching & Cost Controls — implemented and verified (full `pnpm build` + `pnpm check` green; offline + disk-light online DB checks pass). **Caching:** new `packages/cache` (`@kol-fit/cache`). New Prisma `ProviderCache` model (generic KV: `key` PK, `provider`, `payload` Json, `fetchedAt`, `expiresAt`, `@@index([expiresAt])`) pushed to local DB + client regenerated. `CacheStore` interface with `InMemoryCacheStore` (injectable clock, for tests) and `PrismaCacheStore` (upsert-based, lazy-deletes expired rows on read miss). `withTwitterCache(inner, store, config)` decorator (`CachingTwitterProvider implements TwitterProvider`) wraps the 8 read methods with versioned keys (`tw:v1:profile:<handle>`, `tw:v1:tweets|replies:<handle>:<limit>`, `tw:v1:tweetReplies|tweetQuotes|retweeters:<tweetId>:<limit>`, `tw:v1:followers:<handle>:<limit>`), stores normalized shared-type output (not raw), normalizes handle case, is **miss-safe** (store errors → miss/no-op, never fail an analysis), tracks `cacheStats {hits,misses}`, passes `searchTweets` through uncached, and forwards inner `getUsageStats()` merged with cache counts. `resolveCacheConfig()` reads `CACHE_ENABLED` (default true), `CACHE_TTL_PROFILE_SECONDS` (24h), `CACHE_TTL_SECONDS`/`CACHE_TTL_TWEETS_SECONDS` (6h). **Caps:** `packages/shared/caps.ts` refactored — `AnalysisCaps` is now an explicit numeric interface (was `typeof ANALYSIS_CAPS` literals) + exported `CAP_ENV_VARS`; `resolveCaps(overrides?, env?)` added in `packages/analysis` applies `ANALYSIS_*` env overrides (invalid/non-positive → default; in-process overrides win). **Representative sampling:** `packages/llm/openai/sampling.ts` `sampleAudienceAccounts(accounts, limit)` replaces the first-N slice in `classifyAudienceAccounts` — proportional-by-source (largest-remainder) + evenly-spaced within each stable-sorted group; fully deterministic. **Worker wiring:** `apps/worker/src/providers.ts` (`buildProviders()` builds cached Twitter provider via `PrismaCacheStore` + LLM provider; `logProviderUsage()` writes `ProviderUsageLog` rows from provider `getUsageStats()` — raw request/token counts always, cache hit/miss in `meta`, `costUsd` only when `LLM_INPUT/OUTPUT_COST_PER_MTOK` set; best-effort, never fails the job). `analysis-run.ts` now calls `buildProviders()` + `resolveCaps()` → `runAnalysis(req, {twitter, llm, caps})` and logs usage after report upsert. New regression check `scripts/checks/cache-and-caps.regression.cjs` (18 assertions: cache hit/miss/TTL/miss-safe/disabled, caps overrides, sampling determinism+proportionality) wired into `pnpm check`. Verified online (disk-light, local DB): `PrismaCacheStore` round-trip (set/get/expiry/lazy-delete) and a two-run mock pipeline (run1 40 misses → run2 40 hits, mock writes 0 usage rows). Pipeline stays pure + db-free (caching/usage live worker-side).
- Product direction clarified: known KOL fit analysis for crypto startups/orgs.
- Core value clarified: engaged audience match is more important than content similarity.
- First data provider selected: TwitterAPI.io through provider abstraction.
- First LLM provider selected: OpenAI through provider abstraction.
- Architecture direction selected: serious but lean TypeScript system with web app + worker.
- Queue decision selected: pg-boss on Postgres first, Redis/BullMQ later only if needed.
- Database decision selected: Supabase Postgres.
- ORM decision selected: Prisma for AI-coding friendliness.
- Initial context files created.
- Unit 01: Repository Scaffold and Tooling. pnpm workspace (`apps/web`, `apps/worker`, `packages/{shared,db,twitter,llm,analysis,scoring}`) scaffolded; git initialized with `.gitignore` (no commits made); root `tsconfig.base.json` added (TypeScript strict mode; `module`/`moduleResolution` set to `Node16` after `Node10` proved deprecated in the resolved TypeScript 6.0.3); `.env.example` added with `DATABASE_URL`, `DIRECT_URL`, `TWITTERAPI_IO_KEY`, `OPENAI_API_KEY`, `LLM_MODEL`, `NEXT_PUBLIC_APP_URL`. `pnpm install` and `pnpm build` both pass across all 8 packages/apps. `apps/web` dev server verified to render the `@kol-fit/shared` `APP_NAME` constant; `apps/worker` verified (both built output and `tsx`) to log a "worker booted" message including it. No Tailwind/shadcn, Prisma, pg-boss, Zod, or provider logic added, per scope. See `context/specs/01-project-scaffold.md`.
- Unit 02 spec created at `context/specs/02-ui-theme-and-app-shell.md`.
- Unit 02: UI Theme and App Shell — implemented and verified. `apps/web` now has the dark analyst-dashboard visual system from `ui-context.md`, a reusable app shell (top nav + centered content), and a static placeholder landing page. Details: Tailwind v4 (CSS-first; `tailwindcss` + `@tailwindcss/postcss` resolved to 4.3.2, so v4's `@import "tailwindcss"` + `@theme inline` was used, not v3 config files) wired via `apps/web/postcss.config.mjs` and `apps/web/app/globals.css`. All 16 `ui-context.md` colors defined as raw `:root` CSS variables with their exact hex/rgba values; mapped through `@theme inline` to both shadcn semantic tokens (so generated primitives render on the palette, untouched) and ui-context-named utilities (`bg-surface`, `bg-elevated`, `text-success`, `border-default`, `border-strong`, etc.). No component hardcodes a hex (verified by grep). Dark-only, no toggle (tokens live directly in `:root`, `color-scheme: dark`). Fonts via `next/font/google` Inter (`--font-sans`) + JetBrains Mono (`--font-mono`) — no extra font package. shadcn/ui set up with `components.json` + `cn()` in `apps/web/lib/utils.ts` + only the four in-scope primitives in `apps/web/components/ui/` (`button`, `card`, `badge`, `separator`); peer deps `class-variance-authority`, `clsx`, `tailwind-merge`, `tw-animate-css`, `@radix-ui/react-slot`, `@radix-ui/react-separator`, plus `lucide-react` for icons. App shell in `apps/web/components/{app-shell,top-nav}.tsx` (product name from `@kol-fit/shared` `APP_NAME`, no dead nav links). Static landing page at `apps/web/app/page.tsx`: product name + positioning line + a non-interactive "No reports yet" / "Create your first KOL fit analysis" empty-state card (rendered HTML contains zero `<button>`/`<a>` elements). `apps/web/tsconfig.json` gained `@/*` path alias. `pnpm build` passes across all 8 packages/apps; dev server renders `/` with 200 and the compiled CSS contains all 15 token hex values. No API routes, DB/Prisma, worker changes, or provider/scoring logic added. See `context/specs/02-ui-theme-and-app-shell.md`.

- Unit 03: Prisma Database Schema — implemented and verified. Prisma set up **only** in `packages/db`. `prisma/schema.prisma` defines the initial Postgres schema: models `Workspace`, `AnalysisRequest`, `AnalysisJob`, `OrgProfile`, `KolProfile`, `Report`, `ReportEvidence`, `EngagedAccountSample`, `ProviderUsageLog`; enums `JobStatus`, `ReportStatus`, `ReportVerdict`, `EngagementSource`. Workspace-ready (nullable `workspaceId` on `AnalysisRequest` + `Report`), reports persisted from day one, deterministic `Report.scores` kept separate from LLM `Report.report`, compact evidence in `ReportEvidence`, no raw-payload columns, indexes for workspace/history + job status + org/KOL handle + `createdAt`. Client singleton in `packages/db/src/client.ts`, package barrel in `packages/db/src/index.ts` re-exporting `prisma` + all generated types/enums under `@kol-fit/db`. Verified offline: `prisma validate` passes, `prisma format` is idempotent, `prisma generate` succeeds, a runtime export smoke test loads the client (with adapter) and reads all four enums, and `pnpm build` passes across all 8 packages/apps. Resolved to **Prisma 7.8.0**, which required adapting away from the spec's v6-shaped assumptions (see Architecture Decisions + the Implementation Note in the spec). No queue/pg-boss, worker, analysis, provider, LLM, scoring, or API/UI logic added. See `context/specs/03-prisma-database-schema.md`.

- Unit 04: Shared Schemas and Types — implemented and verified. `packages/shared` now holds the contract layer: `zod` added (resolved 4.4.3) and a per-concern module set under `src/` (`constants`, `caps`, `enums`, `vocab`, `handle`, `request`, `scores`, `audience`, `twitter`, `llm`, `report`, `api`) re-exported via the barrel `index.ts`. Defines: `AnalysisRequestInputSchema` (1:1 with the nullable `AnalysisRequest` columns; handle normalization strips `@`/lowercases; `workspaceId` is not user input), the 15-section `FitReportSchema` (versioned via `REPORT_SCHEMA_VERSION`), `ScoreBreakdownSchema` + `ScoreMetricSchema` (9 metrics, 0–100 + confidence; **no weights** — those stay in `packages/scoring`), `AudienceBucketSchema` (15 buckets) + `AudienceDistributionSchema`, status/verdict/source enums mirrored from the Unit 03 Prisma enums (drift-checked, shared stays a dependency-free leaf — no `@kol-fit/db` import), provider-neutral Twitter/X types (`TwitterUserSchema`/`TweetSchema`/`EngagedAccountRawSchema`), provider-neutral LLM output types (`OrgClassificationSchema`/`KolContentClassificationSchema`/`AudienceClassificationSchema` + type-only `FitReport` re-export to avoid a module cycle), `ANALYSIS_CAPS`, and `ApiResponse`/`ApiErrorCode` + `ok()`/`err()`. `APP_NAME` moved to `constants.ts` and still exported (web + worker unaffected). Verified: `pnpm build` passes across all 8 packages/apps; a 14-assertion offline parse smoke test passes (valid/invalid request + report fixtures, handle normalization, score bounds); enum values match the Prisma enums exactly; `ANALYSIS_CAPS` matches the architecture table (100/50/20/50/30/100/1500); request keys are 1:1 with the DB columns. Zod 4 adaptations: `z.url()` (not `z.string().url()`), `z.partialRecord` for partial enum-keyed records. No DB/API/queue/worker/provider/LLM/scoring logic added. See `context/specs/04-shared-schemas-and-types.md`.

- Unit 05: Analysis Request API — implemented and verified. Added `POST /api/analyses` at `apps/web/app/api/analyses/route.ts` (Node runtime). Validates the body with `AnalysisRequestInputSchema`, then atomically creates one `AnalysisRequest` + its `AnalysisJob` (QUEUED) via a Prisma nested create, and returns the shared `ApiResponse` shape: 201 `ok({id,jobId,status,createdAt})`, 400 `err("validation_error", …)` (flattened Zod issues / "Invalid JSON body"), 500 `err("internal_error", …)` (guarded DB write; no stack/secret leak). `apps/web` gained the `@kol-fit/db` workspace dependency; **no** `serverExternalPackages` change was needed (Next 16/Turbopack bundled Prisma + pg cleanly). Per spec: no `Report` row (Unit 07), no pg-boss enqueue (Unit 06), `workspaceId` null, and the Prisma schema was not modified. Verified offline (dev server): invalid body → 400, malformed JSON → 400, valid body with no DB → clean 500 (no leak), wrong method → 405. Verified online against a throwaway local Postgres 14 (initdb in scratchpad, `prisma db push` to materialize tables): valid POST → 201; DB shows exactly one `AnalysisRequest` (normalized handles `myorg`/`coolkol`, context stored, `workspaceId` null) + one `AnalysisJob` (QUEUED, attempts 0, `pgBossJobId` null) linked by `requestId`, and zero `Report`/`OrgProfile`/`KolProfile` rows; temp DB torn down after. `pnpm build` passes across all 8 packages/apps. See `context/specs/05-analysis-request-api.md`.

- Unit 06: pg-boss Queue Setup — implemented and verified. New `packages/queue` (`@kol-fit/queue`) owns all pg-boss specifics: `QUEUE_NAMES.ANALYSIS_RUN` (`constants.ts`), `AnalysisRunPayloadSchema` (`payloads.ts`), a lazy globalThis-cached pg-boss singleton (`boss.ts`) that connects via `DIRECT_URL ?? DATABASE_URL` and only constructs/starts on first use, and `enqueueAnalysisRun()` (`enqueue.ts`). `POST /api/analyses` now enqueues `analysis.run` after the DB write and stores the id on `AnalysisJob.pgBossJobId`; enqueue failure marks the job `FAILED`/`errorCode="enqueue_failed"`/`failedAt` and returns 500; a post-enqueue `pgBossJobId` update failure is non-fatal (still 201). Deps: `pg-boss` 12.25.1 + `zod` + `@types/node` in `packages/queue`; `apps/web` gained `@kol-fit/queue`. Implementation notes: pg-boss 12 is ESM-only, loaded via dynamic `import("pg-boss")` from the CJS package (avoids a `resolution-mode` static type import; the boss type is derived from the dynamic import); queue `tsconfig.json` sets `types: ["node"]` (repo convention). No `serverExternalPackages` needed (lazy import keeps pg-boss out of Next's static graph). **No Prisma schema change** (`pgBossJobId` already existed). Verified offline (dev server: 400/400/clean-500) and online against a throwaway local Postgres 14 (`prisma db push`): valid POST → 201; pg-boss auto-created its `pgboss` schema on first enqueue; a `pgboss.job` row exists for `analysis.run` with payload `{requestId,jobId}`; `AnalysisJob.pgBossJobId` non-null and equal to the pg-boss job id; job still QUEUED; no `Report`/profile rows. Also verified the enqueue-failure path (good `DATABASE_URL`, bad `DIRECT_URL`): 500 + job marked `FAILED`/`enqueue_failed`, request row preserved. `pnpm build` passes across all 9 workspace projects. `architecture.md` updated to add `packages/queue` (Repository Structure + System Boundaries). See `context/specs/06-pg-boss-queue-setup.md`.

- Unit 07: Worker Process Skeleton and Placeholder Report — implemented and verified. `apps/worker` is now a standalone pg-boss consumer. Modules: `env.ts` (loads repo-root `.env` via `dotenv`, imported first so Prisma/pg-boss see env), `index.ts` (boot → `getBoss()` → `boss.work(analysis.run, handler)` iterating pg-boss's batch array, graceful SIGINT/SIGTERM shutdown via `stopBoss()`+`$disconnect`, boot-failure `exit(1)`, unhandled-rejection/exception logging), `handlers/analysis-run.ts` (`processAnalysisRun`: validate payload with `AnalysisRunPayloadSchema` → load `AnalysisJob`+`request` → guard missing/mismatch/already-COMPLETED → RUNNING → upsert Report → COMPLETED; per-job try/catch → FAILED `errorCode="worker_error"` with sanitized message), `placeholder-report.ts` (`buildPlaceholderReport` returns a **valid** `FitReport` parsed by `FitReportSchema`, marked placeholder: overallScore 0 / confidence low / `evidence.notes` "PLACEHOLDER…"). Duplicate prevention via `Report.requestId @unique` upsert + `status==="COMPLETED"` short-circuit. New deps: `@kol-fit/db` + `@kol-fit/queue` (workspace) + `dotenv` on `apps/worker`. **No Prisma schema change.** Verified: `pnpm build` green; offline (no DB) worker boots, logs a clear "failed to start", exits 1 (no hang/unhandled rejection); online E2E against throwaway local Postgres — POST → job QUEUED→RUNNING→COMPLETED (startedAt+completedAt set), exactly one `Report` (status COMPLETED, `report` validates against `FitReportSchema`, clearly marked placeholder); idempotency (re-enqueue same payload → "already completed; skipping", still 1 report); missing records (bogus payload → "not found; acking", no report, worker stays alive). The forced RUNNING→FAILED path is implemented + type-checked but its optional fault-injection check was not run (hard to trigger without injecting an error). No provider/LLM/scoring/pipeline/ingestion/UI/API-route changes. See `context/specs/07-worker-skeleton-placeholder-report.md`.

- Unit 08: Analysis Form UI — implemented and verified. Home route `/` now renders the real analysis form. `app/page.tsx` (Server) keeps the hero and renders the reusable `"use client"` `components/analysis-form.tsx`, which has required `orgHandle`/`kolHandle` (Input) plus an "Add optional context" toggle revealing `websiteUrl`/`docsUrl` (url Input), `productCategory`/`region` (Input), `campaignGoal`/`stage` (Select from shared `CAMPAIGN_GOAL_LABELS`/`PRODUCT_STAGE_LABELS`, with a "__none__" sentinel → omitted), and `targetUser` (Textarea). Client validation reuses `AnalysisRequestInputSchema` from `@kol-fit/shared` (empty optionals omitted from payload, Zod issues mapped to inline per-field errors, friendly "required" messages for empty handles), submits to `POST /api/analyses`, branches on `ApiResponse.ok`, and on success `router.push('/analyses/[id]')`. Loading (disabled inputs + `Loader2` spinner + "Analyzing…"), form-level API/network error panel (token-based `border-error/bg-error`). Added shadcn primitives `input`/`textarea`/`label`/`select` (peer deps `@radix-ui/react-label`, `@radix-ui/react-select`) — `components/ui/` now has exactly 8, no hardcoded hex. Temporary placeholder `app/analyses/[id]/page.tsx` (Server, awaits `params`) confirms "Analysis queued" + the request id — no polling/fetch/report render (Unit 09 replaces it). Verified: `pnpm build` green; home renders form (optionals collapsed); **headless-Chrome (puppeteer-core) drive** — empty submit → inline required errors + no network; invalid handle + bad URL → field errors + no network; valid submit with no DB → API 500 → form-level error panel + button re-enabled + no redirect (10/10 interactive assertions); online (throwaway Postgres) valid submit → 201 → redirect to `/analyses/<id>` placeholder showing the id, with the `AnalysisRequest` row created (handles normalized). No schema/worker/queue/provider/LLM/scoring/API-route changes; no auth/reports-list/share/export. See `context/specs/08-analysis-form-ui.md`.

- Unit 09: Report Status Page — implemented and verified. Added read-only `GET /api/analyses/[id]` (`apps/web/app/api/analyses/[id]/route.ts`, `runtime=nodejs`, `dynamic=force-dynamic`): `findUnique` with `{job, report}`, maps to `AnalysisStatusResponse` (dates→ISO, `report.report` re-validated with `FitReportSchema`→null on failure), returns shared `ApiResponse` — 200 / 404 `not_found` / 500 `internal_error` (no leak). DTO type in web-only `apps/web/lib/analysis-status.ts` (imported by route + client; no `packages/shared` change). `/analyses/[id]` page (Server) now renders the `"use client"` `components/analysis-status.tsx`, which fetches with `cache:"no-store"`, polls via recursive `setTimeout` (2500ms) while `job.status` is QUEUED/RUNNING, stops on COMPLETED/FAILED/404, tolerates transient poll errors (effect-local `haveData`/`lastStatus` avoid stale-closure bugs), and renders header (org vs KOL, status badge, request id) + per-state bodies: loading, queued/running (Activity + "updates automatically", **no fake %**), completed (compact placeholder card: verdict badge, `overallScore /100`, confidence, the "PLACEHOLDER…" note — saved values only, not the Unit 15 renderer), failed (saved `errorMessage`/`errorCode`), not-found, and initial load-error (Try again). No client recompute; DB is the source of truth. No new deps or UI primitives (still 8), no hardcoded hex. Verified: `pnpm build` green; offline (no DB) route → clean 500, page SSRs the loading shell; online (throwaway Postgres) — 13 API assertions across all five states + full POST→worker→COMPLETED loop with placeholder report; headless-Chrome (7 checks) confirmed the completed page renders the report and **polling stops**, and the queued page keeps polling. No schema/worker/POST-route/provider/LLM/scoring changes; no auth/reports-list/share/export. See `context/specs/09-report-status-page.md`.

- Unit 10: Twitter Provider Interface and Mock Provider — implemented and verified. `packages/twitter` now has: `src/provider.ts` (`TwitterProvider` interface — 8 methods returning shared normalized types; `getUserProfile`→`TwitterUser|null`, tweets/replies/search→`Tweet[]`, tweet-engagement→`EngagedAccountRaw[]` with matching `EngagementSource`, followers→`TwitterUser[]` — plus `TwitterProviderKind`); `src/mock/fixtures.ts` (deterministic FNV-1a-seeded fixtures: known org `acmeprotocol` + KOL `cryptowhale` + hash-derived fallback for any handle; varied KOL posts/replies; a 16-account engaged pool spanning developers/founders/DeFi/traders/airdrop-farmers/meme/bots-spam/investor signals; fixed timestamps, no `Math.random`/`Date.now`); `src/mock/provider.ts` (`MockTwitterProvider`, `limit`-respecting, deep-equal per args); `src/factory.ts` (`createTwitterProvider({kind})` — resolution `options.kind`→`TWITTER_PROVIDER` env→`mock`; `twitterapi` throws "not implemented (Unit 16)"). Deps: `@kol-fit/shared` (workspace) + `@types/node` (dev); tsconfig `types:["node"]`. `.env.example` gained optional `TWITTER_PROVIDER=mock`. No new shared types (reused `TwitterUser`/`Tweet`/`EngagedAccountRaw`). Verified fully offline/disk-light: `pnpm build` green + a 27-assertion `node -e` run — all outputs validate against the shared Zod schemas, determinism holds, `limit` respected, provider selection works (mock default/env; twitterapi throws), and the engaged pool covers all required buckets incl. an empty-bio bot. No worker/API/UI/schema/LLM/scoring/pipeline changes; no live TwitterAPI.io; no discovery/search overbuild. See `context/specs/10-twitter-provider-interface-and-mock.md`.

- Unit 11: LLM Provider Interface and Mock Provider — implemented and verified. `packages/llm` now has: `src/provider.ts` (`LlmProvider` interface — 4 architecture methods → shared LLM schemas, `readonly model`; `LlmProviderKind`; LLM-call input types `ClassifyOrgInput`/`ClassifyKolContentInput`/`ClassifyAudienceInput`/`GenerateFitReportInput` composed from shared types); `src/mock/classify.ts` (deterministic, pure helpers: bio/handle→`AudienceBucket` first-match keyword rules, `toAudienceAccount`, `buildDistribution`, `extractKolContent` keyword/ticker scan, `inferOrgClassification` that echoes any `manualBrief` field verbatim — Invariant 7); `src/mock/provider.ts` (`MockLlmProvider`, validates every output with the shared Zod schema before returning); `src/factory.ts` (`createLlmProvider({kind,model})` — kind `options`→`LLM_PROVIDER` env→`mock`; model `options`→`LLM_MODEL` env→`"mock"` sentinel; `openai` throws "not implemented (Unit 17)"). Deps: `@kol-fit/shared` (workspace) + `@types/node` (dev); tsconfig `types:["node"]`. `.env.example` gained optional `LLM_PROVIDER=mock`. **Scoring boundary honored:** `generateFitReport` takes deterministic `scores`/`verdict` as input and passes them through into the `FitReport`; without them it emits marked placeholders (`overallScore.value 0`, `confidence low`, note "pending deterministic scoring (Unit 14)") — the LLM never computes numeric scores. No new shared types (reused the Unit 04 LLM/report schemas). Verified fully offline/disk-light: `pnpm build` green + a ~22-assertion `node -e` run (fed by the Unit 10 Twitter mock) — provider selection (mock default/env; openai throws), model plumbing (options/env/sentinel), all 4 outputs validate against shared schemas, determinism, manual-brief override, audience distribution spanning 8 buckets with correct sampleSize and shares∈[0,1], and the score pass-through-vs-placeholder boundary (echoes value 73/verdict STRONG/components when provided; placeholders + note when absent). No real model name hardcoded. No worker/API/UI/schema/scoring/pipeline/OpenAI/TwitterAPI.io changes. See `context/specs/11-llm-provider-interface-and-mock.md`.

- Unit 12: Website/Docs Content Ingestion — implemented and verified. `packages/analysis/src/ingestion/`: `types.ts` (`SourceContent`/`OrgContext`/`IngestOptions` + `SourceKind`/`SourceStatus`/7 `IngestErrorCode`s + defaults `timeoutMs 5000`/`maxBytes 1_000_000`/`maxTextChars 8000`); `extract.ts` (pure regex HTML→text — strips script/style/noscript/template/comments, block tags→newlines, tag strip, `decodeEntities` named+numeric, `collapseWhitespace`, `extractTitle` capped 300; plus a plain/markdown path; no cheerio/jsdom); `fetch-content.ts` (`fetchUrlContent(url, kind, options)` — URL parse + http/https scheme guard + `isBlockedHost` SSRF guard (localhost/`.local`/loopback/private/link-local IPv4 + bracket-stripped IPv6 fc/fd/fe80/::1) before any request; `AbortController` timeout; Content-Type allowlist (html for both, plain/markdown for docs; missing CT lenient→html); two size gates (Content-Length + streamed reader vs `maxBytes`→`too_large`); decode UTF-8→extract→truncate to `maxTextChars`; try/catch maps to the 7 codes; never throws); `org-context.ts` (`ingestOrgContext({websiteUrl?, docsUrl?}, options)` — independent per-source fetch, `skipped` when a URL is absent, `combinedText` = bounded concat of fetched text → feeds `ClassifyOrgInput.websiteText`; never throws → analysis never aborts on ingest failure, Invariant 8). Injectable `fetchImpl`/`now` for offline determinism. No runtime deps (global `fetch`/`AbortController`/`TextDecoder`); `@types/node` dev + tsconfig `types:["node"]`; no zod/shared dep. Verified fully offline/disk-light: `pnpm build` green + a ~33-assertion `node -e` run with injected mock Responses + fixed clock — happy path (title decode, script/style stripped, charCount, fetchedAt), plain-text path, entity/whitespace, truncation, both `too_large` gates, `timeout` (AbortError), `unsupported_content_type` (incl. website rejecting text/plain), `http_error`+httpStatus, `network_error`, `invalid_url`/`ftp` and all `blocked_host` forms **with fetch asserted not called**, public IPv4/IPv6 allowed, and aggregate skipped/partial-failure/combinedText/determinism. **Caught+fixed a real bug during verification:** `URL.hostname` keeps brackets on IPv6 literals (`[::1]`), so the SSRF guard now strips them before matching (re-verified across `::1`/`fe80`/`fc00`/`fd12`, public IPv6 still allowed). No worker/API/UI/schema/TwitterAPI.io/OpenAI/scoring changes; no crawler abstraction. See `context/specs/12-website-docs-content-fetch.md`.

- Unit 13: Analysis Pipeline Skeleton (with Mocks) — implemented and verified. `packages/analysis/src/pipeline/`: `types.ts` (`AnalysisRequestData`/`RunAnalysisOptions`/`PipelineEvidence`/`AnalysisResult`); `select-posts.ts` (`selectTopPosts` deterministic by engagement, id tiebreaker); `collect-engagement.ts` (`collectEngagedAccounts` dedupe-by-`user.id` + cap); `placeholder-scores.ts` (`buildPlaceholderScores()` — all 9 components 0/low + "Unit 14" reasons, validated; **the single seam Unit 14 replaces**); `run-analysis.ts` (`runAnalysis(request, options)` — factory-default mock Twitter/LLM, optional ingestion (off by default → `ingest({})`, no fetch), caps-bounded fetch, LLM classifications w/ manual-brief + optional `websiteText`, placeholder scores + verdict OKAY passed to `generateFitReport`, then evidence-annotate (web/docs charCounts + placeholder/provider/ingestion notes) and re-validate with `FitReportSchema`). Depends only on provider interfaces/factories (grep-confirmed no `Mock*`), **never imports `@kol-fit/db`** (persistence stays worker-side). Worker `analysis-run.ts`: `buildPlaceholderReport` removed (`apps/worker/src/placeholder-report.ts` deleted), now maps `job.request`→`AnalysisRequestData`, calls `runAnalysis`, and maps `AnalysisResult`→existing `Report` columns (`overallScore`/`verdict`/`scores`/`report`/`audienceSummary`/`confidence`/`sampleSize`/`reportSchemaVersion`/`llmModel`/`promptVersion=null`), preserving Unit 07 QUEUED→RUNNING→COMPLETED/FAILED transitions + unique-`requestId` upsert idempotency. Deps: `packages/analysis` += shared/twitter/llm; `apps/worker` += analysis; no new npm packages. **No Prisma schema change** (all columns pre-existed), no API/UI change. Verified: `pnpm build` green; offline pipeline `node -e` (Part A 23 + Part B 9 = 32 assertions: report/scores validate, evidence complete, sampleSizes match, placeholder scoring marked, caps + dedupe, determinism, manual-brief region surfaces, ingestion on/off with injected fetch asserted not-called-when-off, provider decoupling) + disk-light online worker E2E (throwaway Postgres, 15 assertions: full lifecycle, all persisted columns populated, placeholder banner, idempotency) — all offline except the DB E2E; no TwitterAPI.io/OpenAI/live-network. See `context/specs/13-analysis-pipeline-skeleton-with-mocks.md`.

- Unit 14: Deterministic Scoring Module — implemented and verified. `packages/scoring` (pure, no env/IO): `weights.ts` (`OVERALL_WEIGHTS` EAM .35/AQ .20/CF .15/CGF .15/BS .10/GLF .05, `VERDICT_THRESHOLDS` 80/65/50/35, `RISK_GATE_THRESHOLD` 70, `CONTENT_FIT_CAP` 90, `GOAL_BUCKETS`/`KEYWORD_BUCKETS`/`GENERIC_TARGET_BUCKETS`); `types.ts` (`ScoringInput`/`ScoringSampleMeta`/`ScoringEvidence`(booleans)/`ScoringBrief`/`ScoringResult` — defined here, composed from shared value types, **no `packages/analysis` import**); `confidence.ts` (`confidenceFromEvidence` points→low/med/high, `minConfidence`); `verdict.ts` (`verdictFromScore` + risk gate caps at WEAK when either risk ≥70, `riskGateApplied`); `metrics.ts` (pure per-metric fns → `ScoreValue` w/ non-empty reasons: EAM by target-bucket share, AQ=100−bot/farm penalty, content_fit token-overlap capped 90, CGF by goal buckets, geo heuristic low-conf, brand_safety=100−0.7·promo−meme, paid_promo_risk & bot_farm_risk HIGHER=WORSE; `deriveTargetBuckets`/`resolveGoal`/`share`/`avgBotScore`); `score.ts` (`scoreAnalysis` computes risks→components→weighted overall→verdict→confidence, `ScoreBreakdownSchema.parse`s output; `overall` separate, `components`=the other 8). Pipeline `run-analysis.ts`: replaced `buildPlaceholderScores()`/`verdict="OKAY"` with `scoreAnalysis({org,content,audience,sample,evidence,brief})`, deleted `placeholder-scores.ts` (+ barrel export), swapped the "SCORES ARE PLACEHOLDERS" note for a "deterministic scoring" note; real `scores`/`verdict` passed to `generateFitReport` (LLM still invents no numbers — verified). Deps: `packages/scoring` += shared; `packages/analysis` += scoring. **No Prisma schema/API/UI change; worker `analysis-run.ts` untouched** (persistence path identical to Unit 13). Verified: `pnpm build` green; offline scoring `node -e` (27 assertions: schema validity, 8 components, weights arithmetic, core-metric dominance, risk direction, verdict thresholds + risk-gate, confidence tiers, geo always low, determinism, empty-audience edge) + pipeline integration `node -e` (11: real scores flow through, no placeholder text/note, `report.verdict===verdictFromScore(...)`, section scores echo components, determinism, `buildPlaceholderScores` gone) + disk-light online worker E2E (10: real scores/verdict/confidence persist, idempotency; sample persisted overall 46→WEAK/medium). Scoring independent of analysis + LLM computes no scores (grep-confirmed; one llm hit is a doc comment). See `context/specs/14-deterministic-scoring-module.md`.

- Unit 15: Final Report Renderer — implemented and verified. Additive read-only DTO change: `apps/web/lib/analysis-status.ts` gains `report.scores: ScoreBreakdown | null`; the `[id]` route parses `report.scores` with `ScoreBreakdownSchema` (defensive→null), mirroring the `fitReport` handling (no Prisma/shared change — `Report.scores` column already existed). New `apps/web/components/report/`: `score-meter.tsx` (`<ScoreMeter kind="fit"|"risk">` — CSS bar, value→width only, risk mode inverts color + "higher is worse" caption; `ConfidenceChip`), `report-section.tsx` (`ReportSection`/`BulletList`/`ChipRow`), `score-matrix.tsx` (all 9 metrics from `scores` + local `SCORE_METRIC_LABELS`; missing→"Not scored"), `audience-bars.tsx` (per-bucket bars via `AUDIENCE_BUCKET_LABELS`, low-quality buckets flagged), `fit-report-view.tsx` (wide `max-w-4xl` layout: hero verdict/overall/confidence/reasons → score matrix → recommendation → audience → content/engagement → risk&safety → evidence; `collectMetrics` merges `scores` with the 6 metrics embedded in `FitReport` as fallback — no recompute). `analysis-status.tsx`: COMPLETED+`fitReport` → early-return `<FitReportView>`; `CompletedBody` reduced to the `fitReport`-null fallback; removed the old placeholder-note banner. No new `ui/` primitives (stays 8), no chart lib, no hardcoded hex, dark theme tokens only; renders saved DB state only (no LLM/scoring/provider calls, no score recompute). Graceful missing/partial-data (spine always renders; optional sections omitted; `scores` null → overall + 6 embedded metrics, other 3 "unavailable"). Verified: `pnpm build` green (ui stays 8); DTO curl (scores present, 8 components + overall; malformed→null no 500); disk-light online headless-Chrome drive — 41/41 substantive (all 9 metrics render = saved DB values incl. overall 46/EAM 38/… , verdict WEAK + medium confidence match DB, risk legend + "higher is worse" captions, all 15 sections, audience bars + sampleSize, evidence deterministic-scoring note not placeholder banner; the only console 404 is Next-dev `favicon.ico`) + spine-only/`scores`-null degradation drive (13/13: spine renders, "breakdown unavailable" + "Not scored" for missing metrics, optional sections omitted, no null/undefined leak, crash-free). Only `apps/web` + context changed; no scoring/pipeline/worker/Prisma/shared changes; no reports-list/auth/share/export. See `context/specs/15-final-report-schema-and-renderer.md`.

- Unit 16: Live TwitterAPI.io Provider — implemented and verified. New `packages/twitter/src/twitterapi/`: `endpoints.ts` (base `https://api.twitterapi.io` w/ `TWITTERAPI_IO_BASE_URL` override; the 8 confirmed paths; `DEFAULT_TIMEOUT_MS 15000`; `DEFAULT_MAX_PAGES 200`); `errors.ts` (`TwitterApiError` + `TwitterApiErrorCode` auth/rate_limited/not_found/provider_error/timeout/network_error/invalid_response; messages never contain the key); `client.ts` (`TwitterApiClient` — `X-API-Key` header, `AbortController` timeout, HTTP→code mapping, tolerant envelope: array via caller `arrayField`, `status`, `message`/`msg`, pagination `has_next_page`/`next_cursor`; `collect()` pages up to `limit` and stops at `has_next_page:false`/empty-or-repeated cursor/`maxPages`; continues when `has_next_page` absent but `next_cursor` present (followers); in-memory usage counters + `getUsageStats()`); `normalize.ts` (`normalizeUser`/`normalizeTweet`/`normalizeEngaged` — defensive field mapping userName→handle, description→bio, isBlueVerified→verified, statusesCount→tweetCount, followers/following, author→engaged user, string/number coercion, per-item shared-schema `safeParse` → skip-on-invalid, Invariant 8/9/12); `provider.ts` (`TwitterApiProvider implements TwitterProvider` wiring the 8 methods per the mapping table; `getUserProfile` catches `not_found`→null and maps `status:"error"`/absent data→null; `getUserTweets`/`getUserReplies` split on `isReply` via `includeReplies`; retweeters from `users[]`, replies/quotes engaged users from `tweet.author`; `getUsageStats()`). Factory `"twitterapi"` branch reads `TWITTERAPI_IO_KEY` (fail-fast `auth_error` if missing — no silent mock fallback); **mock unchanged + still default**; interface unchanged. Barrel exports `TwitterApiProvider`/`createTwitterApiProvider`/`TwitterApiError`/`UsageStats`. `.env.example` gained optional `TWITTERAPI_IO_BASE_URL`/`TWITTERAPI_IO_TIMEOUT_MS`. **No new npm deps** (global fetch/AbortController; shared schemas for validation). Verified fully offline (injected `fetchImpl`, doc-shaped JSON, no network): `pnpm build` green + a 32-assertion `node -e` — all 8 methods send correct path/params/`X-API-Key` and return schema-valid normalized output; pagination up to limit + stop conditions + maxPages guard + followers-continue-on-cursor; envelope tolerance (`data`/`msg`, `followers`, `users`, `tweets`); graceful malformed-item skip + profile `status:"error"`→null; error mapping 401/429/404/500/AbortError/non-JSON→correct codes; **API key never in any thrown message** (grep-confirmed: key only read from env in factory, only set as header, no logging); factory selection (key→`TwitterApiProvider`, no-key→`auth_error`, mock default); determinism. Live check skipped (no key present — correct; runs only when `TWITTERAPI_IO_KEY` set, never CI). All TwitterAPI.io logic confined to `packages/twitter`; no worker/UI/API/Prisma/LLM/scoring changes; no caching/discovery (Unit 19 owns caching/cost). See `context/specs/16-twitterapi-provider.md`.

- Unit 17: Live OpenAI LLM Provider — implemented and verified. New `packages/llm/src/openai/`: `errors.ts` (`OpenAiError` + codes auth/rate_limited/provider_error/timeout/network_error/invalid_response/refusal/config_error; no key in messages); `client.ts` (`OpenAiClient` — POST `${OPENAI_BASE_URL||https://api.openai.com/v1}/responses`, `Authorization: Bearer` + `Content-Type`, `AbortController` timeout 60s, HTTP→code mapping, body `{model,input:[system,user],text:{format:{type:"json_schema",name,strict:true,schema}},max_output_tokens}`, extracts `output_text` or walks `output[].content[]` (+ `refusal`), accumulates `usage.{input,output,total}_tokens`, injectable `fetchImpl`, `getUsageStats()`); `schemas.ts` (hand-authored strict JSON schemas per method — enum values pulled from shared `AudienceBucketSchema`/`EngagementSourceSchema`/`ConfidenceLevelSchema`; nullable optionals; `additionalProperties:false`; **report-narrative schema has NO score/verdict fields**); `prompts.ts` (`SYSTEM_PROMPT` + compact-evidence builders, truncated post/account samples not raw payloads, org manual-brief-override note, **report prompt states scores/verdict are FINAL — narrative only**; `repairNote`); `normalize.ts` (`deepNullToUndefined`, `buildAudienceDistribution` deterministic, `assembleFitReport` injecting input scores/verdict/audience/content — model supplies only prose); `provider.ts` (`OpenAiLlmProvider implements LlmProvider` — 4 methods via `requestValidated` = respond→JSON.parse→coerce→assemble→shared `safeParse`→bounded repair-retry→typed `invalid_response`/`refusal`; `readonly model`; `getUsageStats()`; **audience cap `OPENAI_AUDIENCE_CLASSIFICATION_LIMIT` (default 300) → first deterministic slice, ≤100/batch, identity fields taken from INPUT not model, distribution aggregated in code**). Factory `"openai"` branch reads `OPENAI_API_KEY`+`LLM_MODEL` (fail-fast `config_error` if either missing — no hardcoded model, no silent fallback); **mock unchanged + still default**; interface unchanged. Barrel exports `OpenAiLlmProvider`/`createOpenAiLlmProvider`/`OpenAiError`/`LlmUsageStats`. One justified pipeline touch: `run-analysis.ts` records `engagedAccountsClassified` (= `audience.distribution.sampleSize`) alongside total `engagedAccounts` in `sampleSizes` (additive; harmless for mock where classified==total — **scoring/confidence unchanged per user; deferred to Unit 19**). `.env.example` gained optional `OPENAI_BASE_URL`/`OPENAI_TIMEOUT_MS`/`OPENAI_AUDIENCE_CLASSIFICATION_LIMIT=300`. **No new npm deps** (raw fetch; shared schemas validate; `ZodError` typed structurally to avoid a zod dep). Verified fully offline (injected `fetchImpl`, canned Responses JSON, no key/network): `pnpm build` green + a 34-assertion `node -e` — all 4 methods send `/responses`+Bearer+`json_schema` strict+model-from-env and return schema-valid output; **scoring boundary** (report numbers = input scores 73/verdict STRONG/components; narrative from model; report schema has no score fields); audience cap (first 250 of 1000, `ceil/100`=3 batches ≤100 each, identity from input); null→undefined coercion; retry (invalid→valid succeeds; invalid×2→`invalid_response`; refusal→`refusal`); error mapping 401/429/500/AbortError/non-JSON; **key never in any message** (grep-confirmed: only env→Authorization, no logging); factory selection (key+model→provider, missing→`config_error`, mock default); determinism — plus a 4-assertion mock-pipeline check (still valid report, both counts recorded). Live check correctly skipped (no key/model; runs only when both set in `.env`, never CI). All OpenAI logic confined to `packages/llm`; no worker/UI/API/Prisma/TwitterAPI.io/scoring changes; Unit 19 owns cost + better audience sampling. See `context/specs/17-openai-provider.md`.

## In Progress

- None. Unit 18 complete, verified live end-to-end, and **committed + pushed** (details in Session Notes 2026-07-10).

<details><summary>Unit 18 historical in-progress log (superseded by completion)</summary>

- Unit 18: Live End-to-End Report — **first live run surfaced a base-URL bug, since fixed along with 5 more.** Offline results (earlier): `pnpm build` + 11-assertion `node -e` (mock default; live-factory construction with dummy env; fail-fast on missing key/model) + no-secret-leak grep. Pre-run DB/boot checks: DB reachable but schema not applied → ran `prisma db push` (local `kolfit` @ localhost:5432); found web didn't load root `.env` (Next only reads `apps/web/.env*`) → added git-ignored symlink `apps/web/.env → ../../.env`; re-verified worker boots + `GET /api/analyses/<id>` → 404. **Live run bug + fix:** the run died on the first TwitterAPI.io call — `fetch` threw "Failed to parse URL from /twitter/user/info?..." (relative URL). Root cause: `resolveBaseUrl` used `??`, so an **empty** `TWITTERAPI_IO_BASE_URL` from `.env` (shipped empty in `.env.example`) overrode the default → empty base → relative URLs (same latent bug in the OpenAI client's `OPENAI_BASE_URL`). No billable call actually left the machine (fetch failed pre-send; only no-key connectivity probes hit the APIs → 401/421, confirming reachability). Fix (provider-only, per "fix provider not pipeline"): both `resolveBaseUrl` now treat empty/whitespace override+env as unset via trimmed `||` → default. Added regression: `scripts/checks/base-url.regression.cjs` + `pnpm check:base-url` (3/3: empty/whitespace base-url env → absolute URLs for both providers, injected fetch, no network/keys). `pnpm build` green. Changed files: `packages/twitter/src/twitterapi/endpoints.ts`, `packages/llm/src/openai/client.ts`, `package.json` (script), `scripts/` (new). `.env`/schema/UI/pipeline/scoring unchanged. Per `context/specs/18-full-live-end-to-end-report.md`.

</details>

## Next Up

~~0. Restore `ANALYSIS_REUSE_WINDOW_SECONDS`.~~ **Done 2026-07-20** — back to
   `2592000` (30d) in `.env`. The workaround is no longer load-bearing: item 0b
   below now blocks stale reuse structurally, so reuse can stay ON.

~~0b. Make reuse algorithm-aware.~~ **Done 2026-07-20** — see Completed.

1. **Production deploy — single RackNerd VPS, single Supabase DB (planned 2026-07-19).**
   Follow `DEPLOY.md` → *Deploying on a VPS (systemd)* and the post-deploy
   checklist. Highest-risk step is the **Supabase Auth redirect allowlist**
   (Site URL + `<app-url>/auth/callback`): Google sign-in fails silently
   without it. Also purge dev/test rows before real traffic, and confirm the
   systemd supervision actually restarts the app (kill the worker child and
   watch it come back) rather than assuming it.
2. **Unit 41 (Scoring v3) — 2 remaining items**, both already flagged to the user;
   phases A–D are shipped and verified:
   - The fully **editable** infer→confirm override (brand edits target buckets +
     valued regions inline). Needs a schema change (`db push`) + form + UX steer,
     so it is its own slice. The transparency half ("matched against" + re-run)
     already shipped.
   - ~~A live paid run to verify C2's real-provider region / valued-region
     inference.~~ **Done 2026-07-20 (live, paid)** — see Completed. No bug: the
     path works end-to-end against gpt-5-mini.
   - Optional polish: feed reach/geo into the LLM narrative prompt.

3. **`ProviderCache` has no eviction — expired rows accumulate forever.**
   Found 2026-07-20: 1416 of 1669 `tw:v2` rows are already expired (early rows
   were written under a ~6h TTL, before `CACHE_TTL_*` was raised to 30d).
   **Not a correctness bug** — `PrismaCacheStore.get` checks `expiresAt` on read
   (store.ts:55) so stale data is never served — but nothing ever DELETEs them,
   so the table grows without bound in production. Wants a periodic
   `DELETE FROM "ProviderCache" WHERE "expiresAt" <= now()` (pg-boss schedule or
   a Supabase cron). **Cost note:** because most rows are expired, re-running a
   previously-analyzed pair is NOT free any more — it refetches from
   TwitterAPI.io. Only 253 rows are live (80 each of retweeters/tweetQuotes/
   tweetReplies, 5 profiles, 4 tweets, 4 replies).

Unit 19 (Caching and Cost Controls) is **done**, not next — `packages/cache`
ships `CacheStore` + `PrismaCacheStore`/`InMemoryCacheStore`, the `ProviderCache`
table exists, and `.env.example` carries the full `CACHE_*` set. Its spec stays
at `context/specs/19-caching-and-cost-controls.md` as the design record.

## Open Questions

- **Architecture doc naming reconciliation (still open, non-blocking):** the Unit 03 schema uses `OrgProfile`/`KolProfile`/`Report`/`EngagedAccountSample` and folds `ScoreBreakdown`/`AudienceClassificationSummary` into `Report` JSON columns + `ReportEvidence`, whereas `architecture.md` → *Suggested Database Models* still lists the older `OrgSnapshot`/`KolSnapshot`/`AnalysisReport`/`EngagedAudienceSample` names. This is a naming refinement only (no change to system boundaries or invariants), so `architecture.md` was intentionally left untouched during Unit 03's tight scope. Align that section's names when convenient (or in a docs pass). Mapping is documented in `context/specs/03-prisma-database-schema.md` → *Reconciliation with architecture.md*.
- **No baseline migration (still true, now documented):** `packages/db/prisma/`
  holds only `schema.prisma` — `migrations/` has never been generated, and every
  schema change to date was applied with `prisma db push`. Consequence:
  `prisma migrate deploy` is a no-op, so it must not be used to materialize a
  fresh database (`DEPLOY.md` step 3 now says `db push` explicitly, and the
  `db:deploy` script in `packages/db/package.json` is unused until a baseline
  exists). Generating an `init` migration is worth doing if incremental prod
  schema changes ever need to be safe/reviewable; `db push` is adequate while
  there is a single operator and a single database.
- **Post-MVP enhancement roadmap (captured 2026-07-09, partly delivered):** the
  prioritized list lives in `context/project-overview.md` → *Post-MVP
  Enhancement Roadmap*. Since captured, several items shipped: reply/quote
  **text** enrichment (the Unit 10 normalization gap, closed by Unit 29a),
  representative audience sampling (P1, see below), media/visual analysis
  (Unit 31), and export/share (Unit 38 shareable reports). **Still open** from
  that list: KOL shortlisting/ranking, ROI/cost framing, cross-KOL audience
  overlap, promo track record, engagement trend.

### Recently resolved (kept briefly for traceability)

- ~~Scoring v2 calibration pairs~~ — moot: Unit 29E (the calibration harness the
  pairs were for) was removed in `73d379d`; scoring is covered by the v3
  regression suites. Model tiering shipped (`LLM_MODEL_FAST`), and latency has a
  dedicated `pipeline-latency` regression check.
- ~~Live-DB verification deferred~~ — resolved: Supabase went live 2026-07-14 and
  was verified end-to-end. The migration half of that note is still true and has
  been split out into its own entry above.
- ~~Unit 19 follow-up (audience sampling)~~ — resolved: the blunt first-N slice is
  gone. `OpenAiProvider.classifyAudienceAccounts` now calls
  `sampleAudienceAccounts` (`packages/llm/src/openai/provider.ts:287`), a
  representative deterministic sample proportional by engagement source and
  evenly spread within each.
- ~~Model-choice foresight for the `LLM_MODEL` pick~~ — resolved: the model is
  chosen and read from env, with a cheap tier wired via `LLM_MODEL_FAST`. The
  provider remains OpenAI-only; adding another is still a small `packages/llm`
  addition if ever wanted.

## Architecture Decisions

- Use a modular TypeScript architecture with `apps/web`, `apps/worker`, and shared `packages/*` boundaries.
- Use Next.js for UI and lightweight route handlers.
- Use worker jobs for deep analysis instead of API route execution.
- Use Supabase Postgres as the primary database.
- Use Prisma as ORM.
- Use pg-boss for background jobs to avoid Redis cost/complexity at the start.
- Use TwitterAPI.io as the first X/Twitter provider.
- Use provider abstraction for Twitter/X providers.
- Use OpenAI first for LLM analysis, behind an LLM provider abstraction.
- Save reports and evidence/sample-size metadata for future KOL intelligence.
- Package manager is pnpm with pnpm workspaces; all `npm` references in docs/scripts are replaced with `pnpm`.
- Git is initialized during Unit 01 with a standard `.gitignore`. No commits are made unless explicitly requested.
- Auth is out of scope for the first build entirely (not even a simple internal gate). Assume a single internal workspace. Reports/requests carry a nullable `workspaceId` from the first schema onward so real workspaces can be added later without a breaking migration.
- Reports are persisted in the database from the first unit that has a DB (Unit 03 schema, first real save in Unit 07's placeholder report). A saved-reports list/history UI is deferred to Unit 20.
- The LLM model is never hardcoded; it is read from the `LLM_MODEL` environment variable and chosen concretely during Unit 17 (live OpenAI provider).
- Analysis depth/cost caps are centralized config constants (see `architecture.md` — Analysis Depth and Cost Controls), adjustable later in Unit 19: 100 KOL posts, 50 KOL replies, top 20 posts for deep analysis, 50 replies/post, 30 quotes/post, 100 retweeters/post, 1,500 unique engaged accounts max per report.
- Raw provider payloads (Twitter/X and website/docs fetches) are not stored indefinitely. Only normalized entities, sample-size metadata, and compact evidence JSON are persisted.
- Website/docs URLs get lightweight single-page fetch + parse (strict size/timeout limits, no crawler), implemented as a module inside `packages/analysis` (new Unit 12), not a separate provider package.
- AGENTS.md and CLAUDE.md are kept aligned; CLAUDE.md is the active entry file for Claude Code. Reviewed AGENTS.md for conflicts — none found beyond a stale `npm run build` reference, now fixed.
- TypeScript 6.0.3 (resolved via `"latest"`) deprecates `moduleResolution: "node10"` and requires `module`/`moduleResolution` to match when using `Node16`. Root `tsconfig.base.json` uses `module: "Node16"`, `moduleResolution: "Node16"` for all backend packages/apps/worker; `apps/web`'s own `tsconfig.json` overrides `module`/`moduleResolution`/`jsx` etc. as required by Next.js (16.2.10 resolved via `"latest"`), extending the base only for strict-mode options.
- Prisma resolved to **7.8.0** (`latest`), a major with breaking setup changes vs. the Unit 03 spec's v6 assumptions. Adaptations (consistent with the repo's use-`latest`-and-adapt approach from Units 01–02): (a) datasource block carries only `provider` — connection URLs moved to `packages/db/prisma.config.ts` (`defineConfig`, loads root `.env` via `dotenv`, reads `DIRECT_URL`/`DATABASE_URL` with an empty fallback so offline `validate`/`generate` don't throw); (b) generator is `prisma-client` (not `prisma-client-js`), requires an explicit `output` and emits TypeScript to `packages/db/src/generated/prisma` (gitignored) with `moduleFormat = "cjs"` — CJS is mandatory because the package is CommonJS and the default ESM output uses `import.meta.url`, which breaks once tsc compiles it to CJS; (c) the runtime client connects through a driver adapter — `new PrismaClient({ adapter: new PrismaPg(DATABASE_URL) })` — adding `@prisma/adapter-pg` + `pg`; (d) imports come from the generated output (`./generated/prisma/client.js`, Node16 `.js` specifiers), re-exported from `@kol-fit/db`; (e) `dotenv` (not `dotenv-cli`) loads env inside the config file. The spec's Implementation Note documents this as authoritative.
- This background job's worktree-isolation guard could not be satisfied because Unit 01 requires an initial `git init` with zero commits, so `EnterWorktree` had no ref to branch from (unborn HEAD). Per explicit user approval, `.claude/settings.json` was set to `{"worktree": {"bgIsolation": "none"}}` to work directly in the checkout instead. That setting did not take effect mid-session (isolation state appears cached at session start), so all Unit 01 file creation/edits were done via Bash (heredocs) rather than the Write/Edit tools, which remained blocked for the rest of this session.

## Session Notes
- 2026-07-15 (latest): Implemented + live-validated Unit 32 (Goal-Conditional Verdicts + official_ecosystem_lead) — **calibration 16/17 exact-band**, the practical ceiling (1 documented jitter miss, 3 upstream data gaps). Also closed the empty-fetch investigation (TwitterAPI.io has no data for aeyakovenko/BanklessHQ via last_tweets OR advanced_search — upstream gap, guard behaves correctly) and proved the `LLM_MODEL_FIT=gpt-5` tier (user must add to `.env`). Uncommitted. Next up (user to pick): KOL shortlisting/ranking (recommended), ROI/cost framing, goal-specific sub-scores UI surface, org-declared official accounts.
- 2026-07-15 (later): Implemented + live-validated Unit 31 (Media Scoring) — see Completed. Uncommitted. Remaining known work: goal-conditional verdicts (v26 rule 3 — Vitalik miss + campaign-goal product depth), `LLM_MODEL_FIT` experiment for the meow pseudonymous-founder miss, aeyakovenko/BanklessHQ empty-fetch provider investigation, then the product P0s (KOL shortlisting/ranking, ROI framing).
- 2026-07-15: Implemented + live-validated Unit 30 (Audience Intent Overlap) — calibration 9→13/17 exact. See Completed. Uncommitted. Next per user instruction: Unit 31 (media scoring — consume the 29B mediaLabels in scoring: promo-graphic share → promo risk, substantive share → content evidence, deterministic mediaProfile in the report). Optional experiment available: set `LLM_MODEL_FIT=gpt-5` (or similar) in `.env` + purge fit cache to likely resolve the meow pseudonymous-founder miss. Data issue still open: aeyakovenko/BanklessHQ tweet fetches return empty from TwitterAPI.io.
- 2026-07-14 (tuning complete): Unit 29 (accuracy overhaul, 29A–29G + calibration) is DONE — see the 29E tuning entry in Completed. Uncommitted work: cache-poisoning hotfix + 29E tuning batch (weights/prompts/guard/checks/specs). Next units, justified by the miss analysis: intent-overlap taxonomy (v26 rule 4, fixes 6 of 8 misses), goal-conditional verdicts (rule 3), then product P0s (shortlisting/ranking, ROI framing). Also investigate the aeyakovenko/BanklessHQ empty-tweets provider issue.
- 2026-07-14 (live calibration): User approved the live run. First attempt was poisoned by kind-less cache keys (see Hotfix in Completed) — fixed, purged, re-launched on a clean cache. **First clean result: Uniswap × haydenzadams (live, 100 posts / 1500 engaged / 300 classified) = STRONG, overall 88, HIGH confidence, in 120.5s** — the v1 benchmark (40/WEAK) is fixed on real data, and the 29D latency target (~2 min, was ~6.5 min) is confirmed. Remaining 19 pairs in flight; 29E tuning pass follows against `scripts/calibration/last-run.json`. Hotfix not yet committed (remind: commit as its own unit before the tuning commit).
- 2026-07-14 (later): User expanded the calibration set to 26 pairs incl. 7 synthetic negative controls (saved to `context/krypto-kol-calibration-pairs-v26.md`) + 14 algorithm rules. Implemented + verified Unit 29G (severity-tiered risk caps: AVOID tier for overwhelming fake engagement + brand-safety gates; synthetics as fixtures in `negative-controls.regression.cjs`; runner skips them live). pairs.json now 27 (20 real + 7 synthetic). 29F committed+pushed by the user earlier. Remaining: user-approved `--live` calibration run over the 20 real pairs (billable) → 29E tuning pass; future units recorded for v26 rules 3 (goal-conditional verdicts), 11 (controllability fields), 13 (compliance gate). Not committed — awaiting user's commit-per-unit go-ahead.
- 2026-07-14: User supplied the verified 12-pair calibration set (`context/krypto_kol_calibration_pairs_verified_12.md`, may add more). Loaded all 12 into `scripts/calibration/pairs.json` and implemented + verified Unit 29F (Relationship-Aware Verdicts) — the set's central lesson (founder ≠ adjacent ≠ media authority) needed a new signal the algorithm lacked. See Completed. 29E runner committed+pushed by the user earlier. Remaining: user-approved `--live` calibration run over the 12 pairs (billable; ~12 analyses, cache-warms everything), then the 29E anchor/gate tuning pass against `last-run.json`. Not committed — awaiting user's commit-per-unit go-ahead.
- 2026-07-14: Built + verified the Unit 29E calibration runner (`pnpm calibrate`) per `context/specs/29e-calibration-harness.md` — see Completed. 29D committed+pushed by the user earlier. Remaining in Unit 29: the 29E tuning pass, blocked on the user's ground-truth pairs (they're gathering them); first live run after 29D should also confirm the latency win. Not committed — awaiting user's commit-per-unit go-ahead.
- 2026-07-14: Implemented + verified Unit 29D (Pipeline Parallelism + Latency) per `context/specs/29d-pipeline-latency.md` — see Completed. 29C committed+pushed by the user earlier. Next: 29E (calibration harness + tuning) — needs the user's remaining ground-truth pairs (only Uniswap × haydenzadams = STRONG recorded so far in `scripts/calibration/pairs.json`); a live timed run would also confirm the latency win. Not committed — awaiting user's commit-per-unit go-ahead.
- 2026-07-14: Implemented + verified Unit 29C (Scoring v2) per `context/specs/29c-scoring-v2.md` — see Completed. The ground-truth benchmark (Uniswap × haydenzadams shape) flips 40/WEAK → 92/STRONG. 29B committed+pushed by the user earlier. Next: 29D (pipeline parallelism/latency: bounded-concurrency per-post engagement fetches, caps rebalance) and 29E (calibration harness + tuning once the user supplies more ground-truth pairs). Not committed — awaiting user's commit-per-unit go-ahead.
- 2026-07-14: Implemented + verified Unit 29B (LLM Classification v2) per `context/specs/29b-llm-classification-v2.md` — see Completed. 29A was committed+pushed by the user earlier today. Next: 29C (Scoring v2 — calibration curves, human-only EAM via targetBuckets, baseline-adjusted bot/quality, saturation-based promo risk, flags-based brand safety, rubric-based content fit, geo v2, revised gates; wires assessContentFit into the pipeline). Not committed — awaiting user's commit-per-unit go-ahead.
- 2026-07-14: Implemented + verified Unit 29A (Provider Data Enrichment) per `context/specs/29a-provider-data-enrichment.md` — see Completed. Note: the accuracy-overhaul umbrella + sub-unit specs were originally created as "Unit 24" before discovering Units 24–28 already exist (delivery/ownership/abuse/admin/auth); renumbered to **Unit 29** (files + all references). User confirmed ground-truth pair #1: Uniswap × haydenzadams should be STRONG (seeded in `scripts/calibration/pairs.json`). Next: 29B (LLM classification v2). Not committed — awaiting user's commit-per-unit go-ahead.
- 2026-07-13: Created `context/specs/29-analysis-accuracy-overhaul.md` (umbrella design spec, NOT implemented) after the user reported the analysis/scoring is inaccurate: scores too harsh (live @Uniswap × @haydenzadams → 40/WEAK, obviously wrong), paid-promo over-punitive (it's the KOL business model), bot penalties too heavy (bots are endemic — only excess should hurt), brand safety senseless (it's just 0.7×promo), content fit structurally low (exact-token overlap), no media analysis (X is media-first), and analysis latency (~5–7 min) too slow. Decided architecture: LLM emits rich labels + bounded 0–5 rubric ratings; deterministic math with piecewise-linear **calibration curves** (anchor tables in `weights.ts`) produces every number — reproducible via the Unit 23 cache, tunable against user ground-truth pairs. Key moves: curve-mapped human-only source-weighted EAM with LLM-derived org targetBuckets; baseline-adjusted audience-quality/bot-risk (10% junk is free); promo risk = promo *saturation* × unrelatedness (not pattern counts), no longer feeding brand safety; brand safety from explicit severity-weighted safety flags; semantic content-fit rubric via a small pair-specific `assessContentFit` call (kept out of the per-KOL cache); media via `Tweet.media` + multimodal classify; reply/quote text + profile stats into audience classification; parallelized pipeline + model tiering for ≤2.5 min target. All schema changes additive; 9 ScoreMetric slugs kept; cache namespace bumps to `cls:v2:`. Sub-units 29A–29E defined. Awaiting user review + calibration pairs.
- 2026-07-07: Created `context/specs/02-ui-theme-and-app-shell.md` per explicit request. Spec scopes Tailwind setup, the `ui-context.md` dark token system, app shell (top nav + centered content), and a static placeholder landing page only — no analysis form, API routes, DB, worker changes, or provider/scoring logic. Not yet implemented.

- The first version should be real and deep, not a toy MVP.
- The system should still avoid unnecessary infra cost because it will not have hundreds of concurrent users at the start.
- Do not build KOL discovery yet.
- Do not build campaign CRM yet.
- Do not run full analysis inside a Next.js API route.
- Prioritize architecture that can grow into an internal agency tool and later a SaaS product.
- 2026-07-07: Resolved all prior open questions (package manager, git init timing, auth scope, report persistence timing, LLM model configurability, analysis depth/cost caps, raw payload retention, website/docs ingestion scope, AGENTS.md/CLAUDE.md alignment). Updated `architecture.md`, `code-standards.md`, `ai-workflow-rules.md`, `project-overview.md`, `AGENTS.md`, and `context/specs/00-build-plan.md` accordingly. Build plan gained a new Unit 12 (Website/Docs Content Fetch Module); all units from the former Unit 12 onward shifted by +1 (now Units 13-21). Created `context/specs/01-project-scaffold.md`.
- 2026-07-07: Implemented and verified Unit 01 (Repository Scaffold and Tooling). All verification checklist items in `context/specs/01-project-scaffold.md` pass. No commits were made to the new git repository, per instruction.
- 2026-07-08: Implemented and verified Unit 02 (UI Theme and App Shell). See the Completed entry above; `context/specs/02-ui-theme-and-app-shell.md`.
- 2026-07-09: Created `context/specs/17-openai-provider.md` per explicit request (spec only, not implemented). Grounded in the official OpenAI docs (fetched via WebSearch/WebFetch, not guessed): Responses API `POST /v1/responses`, Structured Outputs via `text.format={type:"json_schema",name,strict:true,schema}`, output at `output_text`/`output[].content[]` (+ `refusal`), `usage.{input,output,total}_tokens`, `Authorization: Bearer`, strict-schema rules (all-required, additionalProperties:false, nullable optionals, enum/min/max supported, no open records). Scopes `OpenAiLlmProvider` under `packages/llm/src/openai/` behind the unchanged interface, selected by `LLM_PROVIDER=openai`, key from `OPENAI_API_KEY` + model from `LLM_MODEL` (fail-fast, never hardcoded/logged; mock stays default). Key design decisions: hand-authored strict JSON schemas per method with the **shared Zod schema as the trust boundary** (parse→null→undefined coerce→assemble→safeParse→repair-retry→typed error, never accept invalid JSON — Invariant 12); `classifyAudienceAccounts` returns per-account bucket assignments (batched ≤100 to respect caps/tokens) and the provider aggregates the `AudienceDistribution` deterministically, avoiding the record that strict mode can't express; **`generateFitReport` schema excludes all numeric/score/verdict fields** and the provider injects the deterministic input scores/verdict + audience distribution + kol content, so the LLM is structurally unable to invent scores (Unit 14 boundary, also stated in the prompt); prompts confined to `packages/llm`, fed compact evidence not raw payloads; typed `OpenAiError` (auth/rate_limited/provider_error/timeout/network_error/invalid_response/refusal/config_error); `AbortController` timeout (60s default); in-memory token usage via concrete-only `getUsageStats()` (no interface/DTO/schema change — Unit 19 owns cost). Injectable `fetchImpl` for offline verification (no key needed); optional live check only when `OPENAI_API_KEY`+`LLM_MODEL` set in local `.env` (spec says to tell the user which env vars to add, never paste keys in chat, never CI). No new npm deps (raw fetch, no openai SDK). No worker/UI/API/Prisma/TwitterAPI.io/scoring changes. Sources cited.
- 2026-07-10: Post-Unit-18 cleanup done + committed by user (worker log, stale comments, `TWITTERAPI_IO_TIMEOUT_MS` wiring, `pnpm check`, **typescript pinned `latest`→`^5.9.3`** fixing a broken `next build`). Then created `context/specs/19-caching-and-cost-controls.md` (spec only, not implemented) — DB-backed TwitterAPI.io caching via a new `packages/cache` (worker-side decorator so the pipeline/providers stay db-free), env-overridable `ANALYSIS_CAPS` via `resolveCaps()`, representative audience sampling (replacing Unit 17 first-N), and `ProviderUsageLog` population from `getUsageStats()`. One justified schema addition (`ProviderCache` KV table) flagged to confirm before implementation. Design decisions surfaced as open questions.
- 2026-07-10: **Unit 18 COMPLETE — live end-to-end confirmed.** Final run (@Uniswap vs @haydenzadams, live twitterapi + openai gpt-5-mini, ~6.5 min) COMPLETED with real data: kolPosts 100, kolReplies 50, topPostsAnalyzed 20, engagedAccounts 1500, **engagedAccountsClassified 300**, real 15-bucket audience distribution (non_crypto 40, developers 31, traders 28, bots_spam 27, …), overall **40 / WEAK / high confidence**, FitReportSchema-valid, non-placeholder, genuinely useful narrative (AMM design / pool launches / TVL milestones). Headless-Chrome UI render check 11/11 (hero verdict/overall/confidence, all 9 metrics, risk "higher is worse", audience bars, best use cases, recommended angle, evidence). Six real issues were found+fixed by live testing, each provider-only (never pipeline/scoring/schema/UI), each with an offline regression check (`scripts/checks/*`, 21 assertions total, all green): (1) base-URL empty-env `??` → relative URL [twitter+openai `resolveBaseUrl` trimmed `||`]; (2) gpt-5-mini reasoning tokens consumed `max_output_tokens` → empty output [added `reasoning:{effort}` default minimal via `OPENAI_REASONING_EFFORT`, raised MAX_TOKENS, detect `status:incomplete`]; (3) live TwitterAPI.io wraps arrays under `data` [tolerant `arrayField`, confirmed via one approved diagnostic call]; (4) 100-account audience batch exceeded 60s timeout [batch 100→40, wired `OPENAI_TIMEOUT_MS`, default 60s→120s]; (5) OpenAI 400 with no detail [capture+surface sanitized error body — immediately revealed #6]; (6) `truncate()` `slice()` split an emoji → unpaired UTF-16 surrogate → OpenAI 400 [strip lone surrogates]. (Also removed unsupported `minimum`/`maximum` from the audience strict schema + clamp botScore — belt-and-suspenders.) Pre-run prep: `prisma db push` to local `kolfit`; git-ignored `apps/web/.env → ../../.env` symlink (Next doesn't load monorepo-root `.env`). `.env` never modified by me — **reminder: it's still set to live providers, so future runs are billable; set TWITTER_PROVIDER/LLM_PROVIDER back to `mock` for offline dev.** Known cosmetic leftover: worker still logs "(placeholder report saved)" though the saved report is real. No commits made.
- 2026-07-10: Unit 18 third live run (approved) COMPLETED end-to-end (base-URL + reasoning fixes validated: Twitter connected, OpenAI `gpt-5-mini` produced a valid `FitReportSchema` report with deterministic scores), BUT all sample sizes were 0 — the live Twitter provider extracted no posts/accounts. One approved diagnostic raw `last_tweets` call (structure only, no key/content printed) confirmed the real envelope wraps arrays under **`data`**: `{status,code,msg,data:{pin_tweet,tweets:[...]},has_next_page,next_cursor}` — the tweet/author field names matched, only the array location was wrong (my `collect` read top-level `body.tweets`). Provider-only fix: `arrayField` in `packages/twitter/.../provider.ts` now tolerates `data.<key>`, `data` as a bare array, AND top-level (backward compat). Added `scripts/checks/twitterapi-envelope.regression.cjs` + `pnpm check:twitterapi-envelope` (6/6, using the real data-wrapped shape for tweets/users/followers/user-info + top-level fallback). `pnpm build` + all 3 regression checks green (envelope 6, base-url 3, reasoning 5). No pipeline/scoring/schema/UI change; user `.env` untouched. Final confirming live re-run pending user re-approval. Note: worker completion log still prints the stale Unit 07 "(placeholder report saved)" string though the saved report is the real one — cosmetic, tidy when next touching the worker.
- 2026-07-10: Unit 18 second live run (approved, `@Uniswap`/`@haydenzadams`) got past Twitter (base-URL fix validated — reached RUNNING, full Twitter fetch OK) but FAILED at the first OpenAI call (`classifyOrgProfile`) with `invalid_response` "Model output was not valid JSON". Root cause: `gpt-5-mini` is a reasoning model — the Responses API counts reasoning tokens against `max_output_tokens`, so the low budgets (org/kol 700) were spent on reasoning → response `incomplete` with empty `output_text` → JSON.parse fail. Provider-only fix (packages/llm): send `reasoning: { effort }` (default "minimal", from `OPENAI_REASONING_EFFORT`, `"off"`/`"none"` omits it for non-reasoning models like gpt-4o-mini; option override supported), raised `MAX_TOKENS` (org/kol 2000, audience 8000, report 4000 — caps, billed only for used), and detect `status:"incomplete"` → clear typed error instead of "not valid JSON". Added `scripts/checks/openai-reasoning.regression.cjs` + `pnpm check:openai-reasoning` (5/5). `.env.example` documents `OPENAI_REASONING_EFFORT` (user `.env` needs no change — defaults to minimal). `pnpm build` + both regression checks green. No pipeline/scoring/schema/UI change; user `.env` untouched. Live re-run pending user re-approval.
- 2026-07-10: Unit 18 first live run (approved, one submission, `@Uniswap`/`@haydenzadams`) FAILED fast on a real base-URL bug — `resolveBaseUrl` used `??`, so an empty `TWITTERAPI_IO_BASE_URL`/`OPENAI_BASE_URL` from `.env` (empty in `.env.example`) produced relative request URLs → `fetch` "Failed to parse URL". No billable call left the machine. Fixed both providers (trimmed `||` → default), added `scripts/checks/base-url.regression.cjs` + `pnpm check:base-url` (passes), `pnpm build` green. Pre-run prep also applied: `prisma db push` (local `kolfit` DB) + git-ignored `apps/web/.env → ../../.env` symlink so Next loads env. Live re-run pending user re-approval. No `.env`/schema/scoring/UI/pipeline changes.
- 2026-07-09: Captured a post-MVP product roadmap (from a product-strategy discussion) into `context/project-overview.md` → *Post-MVP Enhancement Roadmap* (P0/P1/P2 + self-rating), with pointers + the LLM model-choice/multimodal foresight added to Open Questions here. No code/scope change to the current build; these are future enhancements to work on after Unit 18. Key P0 flag: engagement-quality depth needs reply/quote **text** (dropped in the Unit 10 provider normalization). Docs-only change.
- 2026-07-09: Created `context/specs/18-full-live-end-to-end-report.md` per explicit request (spec only, not implemented). Verified by reading the wiring that Unit 18 needs **no code changes**: worker loads root `.env` first, `run-analysis.ts` defaults to env-driven provider factories, worker calls `runAnalysis` with no provider options → live path is pure config (`TWITTER_PROVIDER=twitterapi`/`LLM_PROVIDER=openai` + keys + `LLM_MODEL`; mock default). Spec is a connect-and-verify plan: offline verification stays CI-safe (mock default + build + factory-construction-with-dummy-keys + no-secret-leak grep); a gated manual live procedure requires explicit approval and one controlled run of a single low-risk pair, with prominent billable-cost warnings and "never run automatically". Documents failure handling (provider errors → worker FAILED + generic status message + typed cause in server logs with no key leak; richer per-code UI deferred to Unit 21), report validation (FitReportSchema-valid, deterministic scores from packages/scoring, OpenAI narrative-only), and the exception that any needed fix goes into provider normalization not the pipeline (else stop and explain). No new deps; no scoring/schema/UI changes. Never paste keys in chat / never log keys.
- 2026-07-09: Implemented and verified Unit 17 (Live OpenAI LLM Provider) in `packages/llm/src/openai/` + factory `"openai"` branch + one justified `run-analysis.ts` `sampleSizes` touch (`engagedAccountsClassified`). Fully offline verification (`pnpm build` + 34-assertion `node -e` with injected `fetchImpl` on canned Responses JSON: request shape, scoring boundary, audience cap+batching, null coercion, retry/refusal, error mapping, key-never-leaks, factory selection, determinism; + 4-assertion mock-pipeline check) — no key/network. Per user: confidence/scoring left unchanged (classified-count recorded for a future Unit 19 pass). Mock unchanged + still default; interface/schema unchanged; key only env→Authorization (grep-confirmed); no hardcoded model. Live check correctly skipped (no key/model). Scratch cleaned. No commits made.
- 2026-07-09: Updated `context/specs/17-openai-provider.md` per user-confirmed audience design. Per-account classification kept, but capped by `OPENAI_AUDIENCE_CLASSIFICATION_LIMIT` (default 300): LLM labels only the first deterministic capped slice (≤100/batch); provider aggregates the distribution deterministically (LLM never emits percentages/scores); `distribution.sampleSize` = classified count. Records both counts clearly — total via existing `evidence.sampleSizes.engagedAccounts`, and a new `engagedAccountsClassified` (= `distribution.sampleSize`) added in `run-analysis.ts` (the single justified, additive pipeline touch — no type/scoring change) — so confidence can reflect the capped sample (wiring confidence to it deferred). Added env var + verification checks. Flagged Unit 19 to improve audience sampling (representative, not first-N) + cost controls. Spec-only; no implementation.
- 2026-07-09: Implemented and verified Unit 16 (Live TwitterAPI.io Provider) in `packages/twitter/src/twitterapi/` + factory `"twitterapi"` branch. Fully offline verification (`pnpm build` + 32-assertion `node -e` with injected `fetchImpl` on doc-shaped JSON — all 8 methods, pagination/caps/maxPages, envelope tolerance, graceful skips, error-code mapping, key-never-leaks, factory selection, determinism); no network, no key required. Mock unchanged + still default; interface/schema unchanged; API key only read from env in factory and only sent as `X-API-Key` header (grep-confirmed, no logging). Live check correctly skipped (no key). Scratch cleaned. No commits made.
- 2026-07-09: Created `context/specs/16-twitterapi-provider.md` per explicit request (spec only, not implemented). Per the user's instruction to fetch rather than guess API shapes, I asked how to source them (they chose "I fetch the official docs"), then pulled the real endpoint paths + response shapes from docs.twitterapi.io via WebFetch/WebSearch and grounded the spec in them (Confirmed API Reference block + Sources). Scopes `TwitterApiProvider` under `packages/twitter/src/twitterapi/` behind the unchanged interface, selected by `TWITTER_PROVIDER=twitterapi`, key from `TWITTERAPI_IO_KEY` (never hardcoded/logged; fail-fast if missing; mock stays default). Documented: 8-method→endpoint mapping, tolerant normalizer for the inconsistent envelopes (`data`/`tweets`/`followers`/`users`, `msg`/`message`), field mapping to shared types with per-item Zod validation + skip-on-invalid (graceful, Invariant 8), caps-bounded cursor pagination with `maxPages` guard, typed `TwitterApiError` codes, `AbortController` timeout, injectable `fetchImpl` for offline verification, optional key-gated live check (never CI), and minimal in-memory usage counters via a concrete-only `getUsageStats()` (no interface/DTO/schema change — Unit 19 owns real caching/cost). No new npm deps. No worker/UI/API/Prisma/LLM/scoring changes.
- 2026-07-09: Updated `context/specs/16-twitterapi-provider.md` — user confirmed the quotations endpoint path `GET /twitter/tweet/quotes` (docs title "Get Tweet Quotations", same base/`X-API-Key`/cursor pattern, ~20 quotes/page). Removed the "confirm at implementation" TBD flags; all eight endpoint paths are now confirmed. Spec-only; no implementation.
- 2026-07-09: Implemented and verified Unit 15 (Final Report Renderer). Web-only: additive `report.scores` DTO field + defensive parse in the `[id]` route; new `apps/web/components/report/` (fit-report-view, score-meter, score-matrix, audience-bars, report-section); `analysis-status.tsx` renders `<FitReportView>` for completed reports (compact card now a fallback). Verified `pnpm build` (ui stays 8) + DTO curl + disk-light online headless-Chrome drives: full report 41/41 (all 9 metrics = saved DB values, all 15 sections, risk direction obvious) and spine-only/scores-null degradation 13/13. Only console 404 is Next-dev favicon (benign). No scoring/pipeline/worker/Prisma/shared change; renders saved state only. Disk healthy (~12Gi free); all scratch (Postgres/puppeteer/logs) torn down. No commits made.
- 2026-07-09: Created `context/specs/15-final-report-schema-and-renderer.md` per explicit request (spec only, not implemented). Scopes a web-only fit-report renderer: replace the Unit 09 compact completed card in `analysis-status.tsx` with `<FitReportView>` (wider `max-w-4xl`) rendering the saved `FitReport` (15 sections) + full `ScoreBreakdown` (9 metrics) for COMPLETED analyses on `/analyses/[id]`. **Key design finding + required DTO change:** the `FitReport` embeds only 6 of the 9 metrics (overall + engaged_audience_match/paid_promo_risk/bot_farm_risk/brand_safety/geo_language_fit); the other 3 (content_fit, audience_quality, campaign_goal_fit) live only in `Report.scores`, so the DTO must add `scores: ScoreBreakdown | null` — parsed defensively with `ScoreBreakdownSchema` in `apps/web/app/api/analyses/[id]/route.ts`, added to `apps/web/lib/analysis-status.ts` (additive, read-only, backward-compatible; **no Prisma schema change** — `Report.scores` column exists since Unit 03; no shared change — `ScoreBreakdown` already exported). New `components/report/`: `fit-report-view.tsx`, reusable `score-meter.tsx` (`<ScoreMeter kind="fit"|"risk">` — risk mode = inverted color + "higher is worse" caption so `paid_promo_risk`/`bot_farm_risk` direction is unmistakable), `score-matrix.tsx` (9 metrics from `scores` + local `SCORE_METRIC_LABELS`), `audience-bars.tsx` (per-bucket CSS bars via `AUDIENCE_BUCKET_LABELS`, low-quality buckets flagged), `report-section.tsx`. CSS bars only (no chart lib/accordion), dark analyst-dashboard tokens, no hardcoded hex, no new `ui/` primitives (stays 8). Renders saved DB state only — no score recompute, no LLM/scoring/provider calls (bar width is a pure value→CSS mapping). Section-by-section mapping table + score/risk/audience/evidence display behaviors + missing/partial-data graceful degradation (spine always renders; optional sections omitted; `scores` null → 6 embedded metrics + "unavailable" for the 3; `fitReport` null → minimal fallback) all specified. Verification: `pnpm build` (typecheck, ui stays 8) + DTO curl (scores present/valid; malformed→null no 500) + disk-light online headless-Chrome drive (hero verdict/overall/confidence match DB, all 9 metrics = saved values, risk direction obvious, all 15 sections, audience bars + sampleSize, evidence notes, spine-only + scores-null fallbacks, unchanged queued/running/failed states). No scoring/pipeline/worker/schema changes; no reports-list/auth/share/export.
- 2026-07-09: Implemented and verified Unit 14 (Deterministic Scoring Module). New pure `packages/scoring` (`scoreAnalysis`), pipeline rewired to call it (deleted `placeholder-scores.ts`, removed the placeholder banner note, real scores+verdict now flow to `generateFitReport`). Addressed all Unit 13 review findings. Verified `pnpm build` + offline scoring `node -e` (27) + pipeline integration (11) + disk-light online worker E2E (10; real overall 46→WEAK persisted, idempotency). No Prisma schema/API/UI/worker-handler change; scoring independent of analysis; LLM computes no numbers. Disk healthy (~16Gi free). No commits made.
- 2026-07-09: Created `context/specs/14-deterministic-scoring-module.md` per explicit request (spec only, not implemented). Scopes pure deterministic scoring in `packages/scoring` (`weights.ts`, `types.ts`, `metrics.ts`, `verdict.ts`, `confidence.ts`, `score.ts`): `scoreAnalysis(input): ScoringResult {scores: ScoreBreakdown, verdict: ReportVerdict}`. Input types (`ScoringInput`/`ScoringSampleMeta`/`ScoringEvidence`/`ScoringBrief`) defined in scoring from shared value types — ingestion status passed as **booleans** (`websiteFetched`/`docsFetched`) so scoring stays independent of `packages/analysis` (review finding). 9 metrics with explicit risk direction (paid_promo_risk/bot_farm_risk HIGHER=WORSE); `scores.overall`=overall_fit, `components`=the other 8. **Locked weights** (EAM .35, AQ .20, CF .15, CGF .15, BS .10, GLF .05) → overall = rounded weighted sum; risks are NOT direct terms (they drive AQ via bot_farm penalty and BS via promo penalty, plus a verdict **risk-gate** capping at WEAK when either risk ≥70); content_fit capped at 90 so engaged-audience-match (core, 35%) dominates. Verdict thresholds 80/65/50/35 (STRONG/GOOD/OKAY/WEAK/AVOID). Target buckets derived deterministically from campaignGoal (goal→bucket table) + keyword hits over productCategory/targetUser/org.keywords. Every ScoreValue carries non-empty reasons; confidence from sample sizes + ingestion (points→low/medium/high; geo always low). Integration: pipeline builds `ScoringInput` from in-hand classifications/evidence/brief, calls `scoreAnalysis`, passes real scores+verdict to `generateFitReport` (LLM still invents no numbers); delete `placeholder-scores.ts` + the "SCORES ARE PLACEHOLDERS" banner; `packages/analysis` += `@kol-fit/scoring`; **no Prisma schema change** (scores column already holds ScoreBreakdown), no API/UI change. Addresses all Unit 13 review findings (replaces both placeholder scores and hardcoded OKAY verdict; scoring returns verdict; input types in scoring; scoring↮analysis independence). Verification offline scoring `node -e` (schema validity, weights arithmetic, core-metric dominance, risk direction, verdict+risk-gate, confidence, determinism, empty-audience edge) + pipeline integration (real scores, no placeholder text/note, verdict matches mapping, placeholder-scores.ts gone) + optional disk-light online worker check.
- 2026-07-09: Implemented and verified Unit 13 (Analysis Pipeline Skeleton with Mocks). New `packages/analysis/src/pipeline/` (`runAnalysis` + helpers), worker rewired to call it (deleted `placeholder-report.ts`), richer `Report` column mapping. Verified `pnpm build` + offline pipeline `node -e` (32 assertions) + disk-light online worker E2E (throwaway Postgres, 15 assertions; torn down). No Prisma schema/API/UI change; pipeline never imports `@kol-fit/db`; scores remain placeholder behind `buildPlaceholderScores()` for Unit 14. Disk healthy (~19Gi free). No commits made.
- 2026-07-09: Created `context/specs/13-analysis-pipeline-skeleton-with-mocks.md` per explicit request (spec only, not implemented). Scopes the first end-to-end pipeline in `packages/analysis/src/pipeline/` (`types.ts`, `select-posts.ts`, `collect-engagement.ts`, `placeholder-scores.ts`, `run-analysis.ts`): `runAnalysis(request: AnalysisRequestData, options?)` → `AnalysisResult { report: FitReport (validated), scores: ScoreBreakdown (placeholder), evidence: PipelineEvidence, llmModel, generatedAt }`. Stages: optional website/docs ingestion → mock Twitter (org/KOL profiles, posts capped `kolPostsFetched`, replies `kolRepliesFetched`, top `topPostsForDeepAnalysis` posts by engagement, per-post replies/quotes/retweeters, dedupe-by-user-id + cap `maxUniqueEngagedAccounts`) → mock LLM (`classifyOrgProfile` w/ manual-brief override + optional `websiteText`, `classifyKolContent`, `classifyAudienceAccounts`) → `buildPlaceholderScores()` → `generateFitReport({scores, verdict:"OKAY"})`. Key decisions: pipeline depends only on provider **interfaces/factories** (`createTwitterProvider`/`createLlmProvider`, mock via env), never concrete `Mock*`, and not on `@kol-fit/db` (persistence stays worker-side, no report-building duplication); `buildPlaceholderScores()` is the single seam Unit 14 replaces; placeholder scores (all 9 components value 0/low + marked reasons) passed to the report so numeric fields are consistent 0/OKAY/low, plus an explicit "SCORES ARE PLACEHOLDERS … Unit 14" evidence banner; website/docs ingestion wired but `performWebIngestion` defaults **false** in the worker (zero live calls in Unit 13) and only ever via injectable `fetchImpl` (offline verification). Worker: replace `buildPlaceholderReport` (delete `apps/worker/src/placeholder-report.ts`) with `runAnalysis`, map `AnalysisResult`→`Report` columns (now also `scores`/`audienceSummary`/`sampleSize`/`llmModel`; `promptVersion` null), preserving Unit 07 QUEUED→RUNNING→COMPLETED/FAILED transitions + unique-`requestId` upsert idempotency; `ReportEvidence`/`EngagedAccountSample` tables deferred (evidence in JSON columns). **No Prisma schema change needed** (all target columns exist since Unit 03) — explicitly justified in the spec. Deps at implementation: `packages/analysis` += shared/twitter/llm; `apps/worker` += analysis; no new npm packages. Verification: offline pipeline `node -e` (report validates, evidence complete, caps + dedupe, determinism, manual-brief, ingestion-injected on/off, provider decoupling, placeholder-scoring marked) + a disk-light online worker E2E (throwaway Postgres: full lifecycle, populated columns, idempotency) + schema-unchanged/no-duplication greps. No UI/API/scoring/live-network changes.
- 2026-07-09: Implemented and verified Unit 12 (Website/Docs Content Ingestion) in `packages/analysis/src/ingestion/`. Fully offline/disk-light verification (`pnpm build` + ~33-assertion `node -e` with injected mock `fetch` + fixed clock; no network/DB/browser). Caught and fixed a real IPv6 SSRF-guard bug (`URL.hostname` keeps `[...]` brackets) mid-verification. No commits made.
- 2026-07-09: Created `context/specs/12-website-docs-content-fetch.md` per explicit request (spec only, not implemented). Scopes a deterministic single-URL ingestion module inside `packages/analysis` (`src/ingestion/`: `types.ts`, `extract.ts`, `fetch-content.ts`, `org-context.ts`): `fetchUrlContent(url, kind, options)` and `ingestOrgContext({websiteUrl?, docsUrl?}, options)` returning `SourceContent` (`url`, `kind`, `status` fetched/failed/skipped, `title?`, `extractedText`, `charCount`, `httpStatus?`, `errorCode?`, `errorMessage?`, `fetchedAt?`) and `OrgContext` (website+docs+`combinedText`→`ClassifyOrgInput.websiteText`). Design decisions: exact-URL only, **no crawler/link-follow/sitemap/browser**; scheme allowlist (http/https) + lightweight SSRF host guard (localhost/private/link-local literals blocked, DNS-rebind revalidation noted as future); Content-Type allowlist (text/html always; text/plain+markdown for docs); two size gates (Content-Length + streamed body vs `maxBytes` 1MB) that fail `too_large` rather than truncate; `AbortController` timeout (5s); regex-based HTML→text extraction with a tiny entity map (no cheerio/jsdom — "avoid heavy deps"), truncated to `maxTextChars` (8000); 7 error codes; neither function ever throws and per-source failures are contained so analysis never aborts (Invariant 8). Injectable `fetchImpl`/`now` in `IngestOptions` make verification fully offline (canned Responses / in-process fixtures) and deterministic. No runtime deps (global `fetch`/`AbortController`/`TextDecoder`); `@types/node` dev + tsconfig `types:["node"]`; no zod, no shared dep. Verification is offline/disk-light (`pnpm build` + `node -e` with injected fetch across happy-path, extraction/entities/truncation, too_large, timeout, unsupported type, http_error, network_error, invalid_url, blocked_host, skipped/aggregate, determinism). No worker/API/UI/schema/TwitterAPI.io/OpenAI/scoring changes.
- 2026-07-09: Implemented and verified Unit 11 (LLM Provider Interface and Mock Provider) in `packages/llm`. Fully offline/disk-light verification (`pnpm build` + ~22-assertion `node -e` cross-fed by the Unit 10 Twitter mock: selection, model plumbing, output schema validation, determinism, manual-brief override, audience bucket variety, and the no-score-invention/pass-through boundary) — no DB/Postgres/browser used. Note: an initial verification run used an invalid test verdict `"STRONG_FIT"`; the provider correctly rejected it via `FitReportSchema.parse` (real enum is `STRONG|GOOD|OKAY|WEAK|AVOID`) — test data fixed, code unchanged. No commits made.
- 2026-07-09: Created `context/specs/11-llm-provider-interface-and-mock.md` per explicit request (spec only, not implemented). Scopes the `packages/llm` provider abstraction mirroring Unit 10's pattern: an `LlmProvider` interface with the 4 architecture capabilities (`classifyOrgProfile`→`OrgClassification`, `classifyKolContent`→`KolContentClassification`, `classifyAudienceAccounts`→`AudienceClassification`, `generateFitReport`→`FitReport`) + a `readonly model` from `LLM_MODEL`; LLM-call input types composed from shared types (live in `packages/llm`, may be refined in Unit 13); a deterministic `MockLlmProvider` that validates every output against the shared Zod schema and classifies engaged accounts into buckets via bio/handle keyword rules; and a `createLlmProvider({kind,model})` factory (kind: `options`→`LLM_PROVIDER` env→`mock`; `openai` throws "not implemented (Unit 17)"; model: `options`→`LLM_MODEL` env→`"mock"` sentinel). **Key boundary decision:** `generateFitReport` receives deterministic `scores`/`verdict` as *input* (from `packages/scoring`, Unit 14) and passes them through — the LLM never computes numeric scores; when absent (no scoring yet) the mock emits clearly-marked placeholders (`overallScore.value 0`, verdict `OKAY`, note "scores pending Unit 14"). The 5 scope outputs map onto 4 methods (paid-promo/brand-risk = `promoPatterns` + report narrative sections, no separate method). Prompt-boundary rules documented (prompts/model/secrets stay in `packages/llm`; outputs validated before trust; model id is config, recorded on `Report.llmModel`). New deps at implementation: `@kol-fit/shared` (workspace) + `@types/node` (dev); tsconfig `types:["node"]`; optional `LLM_PROVIDER=mock` added to `.env.example` (`LLM_MODEL` already present, plumbed not hardcoded). Verification fully offline/disk-light (`pnpm build` + `node -e`: selection, model plumbing, output schema validation, determinism, manual-brief override, audience bucket variety via the Unit 10 mock accounts, and no-score-invention). No worker/API/UI/schema/scoring/pipeline/OpenAI changes.
- 2026-07-09: Implemented and verified Unit 10 (Twitter Provider Interface and Mock Provider) in `packages/twitter`. Fully offline/disk-light verification (`pnpm build` + 27-assertion `node -e`: shape validation vs shared schemas, determinism, limit, provider selection, bucket variety) — no DB/Postgres/browser used, respecting the low-disk note. No commits made.
- 2026-07-09: Created `context/specs/10-twitter-provider-interface-and-mock.md` per explicit request (spec only, not implemented). Scopes the `packages/twitter` provider abstraction: a `TwitterProvider` interface with the 8 architecture methods returning shared normalized types (`getUserProfile`→`TwitterUser|null`; `getUserTweets`/`getUserReplies`/`searchTweets`→`Tweet[]`; `getTweetReplies`/`getTweetQuotes`/`getTweetRetweeters`→`EngagedAccountRaw[]` with the matching `EngagementSource`; `getFollowers`→`TwitterUser[]`), a deterministic `MockTwitterProvider` (pure fixtures — no `Math.random`/`Date.now`, `limit`-respecting, deep-equal per args), and a `createTwitterProvider({kind})` factory (resolution `options.kind`→`TWITTER_PROVIDER` env→default `mock`; `twitterapi` throws a clear not-implemented error until Unit 16). Decisions recorded: engagement-on-tweet methods return `EngagedAccountRaw` (engaging user's bio is the classification signal) vs `getFollowers`→`TwitterUser[]` (not tied to a tweet); reply/quote text deferred (not needed for audience classification; add later if engagement-quality needs it); **no new shared types** (reuse `TwitterUser`/`Tweet`/`EngagedAccountRaw`); mock validates against the shared Zod schemas. Mock fixtures must span audience-bucket signals (developers, founders, DeFi users, traders, airdrop farmers, meme users, bots/spam) so Unit 13 + later audience classification have variety. New deps at implementation: `@kol-fit/shared` (workspace) + `@types/node` (dev, for `process.env`); `tsconfig` gains `types:["node"]`; `.env.example` gains optional `TWITTER_PROVIDER=mock`. Verification is fully offline/disk-light (`pnpm build` + `node -e` shape/determinism/limit/variety/selection checks — no DB/Postgres/browser). No worker/API/UI/schema/LLM/scoring/pipeline changes; no live TwitterAPI.io; no discovery/search overbuild.
- 2026-07-08: Implemented and verified Unit 09 (Report Status Page). Read-only `GET /api/analyses/[id]` + polling `<AnalysisStatus>` page replacing the placeholder. Verified via `pnpm build`, offline (route 500 + SSR loading shell), online 13 API assertions across all 5 states + full e2e loop, and 7 headless-Chrome checks (completed renders report + polling stops; queued keeps polling). Schema/worker/POST-route untouched. `next-env.d.ts` churn reverted. No commits made. NOTE: the machine's disk hit ~100% full mid-unit (only ~400MB free at one point); freed space by removing scratchpad artifacts and kept verification disk-light (curl/Prisma for API states, minimal headless run). Disk recovered to ~4GB free after teardown — worth flagging to the user.
- 2026-07-08: Created `context/specs/09-report-status-page.md` per explicit request (spec only, not implemented). Scopes the real report status page: a thin read-only `GET /api/analyses/[id]` (`runtime=nodejs`, `dynamic=force-dynamic`) that `findUnique`s the request with `{job, report}` and returns `ApiResponse<AnalysisStatusResponse>` — 200 found / 404 `not_found` / 500 `internal_error`. The `/analyses/[id]` page (Server) renders a `"use client"` `<AnalysisStatus id>` that fetches with `cache:"no-store"`, polls via recursive `setTimeout` (~2500ms) while `job.status` is QUEUED/RUNNING, stops on COMPLETED/FAILED/404, tolerates transient poll errors (keeps last state), and renders header (org/KOL/status badge/generated ts) + per-state UI (loading/queued/running/completed/failed/not-found/load-error). Completed shows a **compact** placeholder card (verdict, overallScore, confidence, the "PLACEHOLDER…" note) from saved values only — not the Unit 15 renderer; no client recompute (DB is source of truth). Decisions recorded: DTO type in web-only `apps/web/lib/analysis-status.ts` imported by both route + client (no `packages/shared` change, no new Zod schema — report JSON already validated on write); top-level state derived from `job.status`; no fabricated progress %; no retry (Unit 21). **No schema/worker change** (all fields already exist). No new deps or UI primitives (reuse Card/Badge/Button/Separator + lucide). No auth/reports-list/share/export. Offline-safe verification defined (build; route 500 with no DB; page load-error state via headless Chrome) plus online lifecycle/completed/failed/not-found checks.
- 2026-07-08: Implemented and verified Unit 08 (Analysis Form UI). Real analysis form on `/` + shadcn Input/Textarea/Label/Select + temporary `/analyses/[id]` placeholder. Verified with `pnpm build`, offline headless-Chrome interaction (validation/loading/error states, 10/10), and online throwaway-Postgres submit→201→redirect. Schema/worker/queue untouched. `next-env.d.ts` churn reverted. No commits made.
- 2026-07-08: Created `context/specs/08-analysis-form-ui.md` per explicit request (spec only, not implemented). Scopes the first real analysis form in `apps/web`: a `"use client"` `<AnalysisForm />` on the home route `/` (Server page renders it), replacing the static empty-state preview and keeping the hero. Fields: required `orgHandle`/`kolHandle` (Input) at top; optional `websiteUrl`/`docsUrl` (url Input), `productCategory`/`region` (Input), `targetUser` (Textarea), `campaignGoal`/`stage` (Select, options from shared `CAMPAIGN_GOAL_LABELS`/`PRODUCT_STAGE_LABELS`) behind an "Add optional context" toggle. Client validation reuses `AnalysisRequestInputSchema` from `@kol-fit/shared` (pure Zod, client-safe) — omits empty optionals from the payload; submits to `POST /api/analyses`; branches on the shared `ApiResponse.ok`. Success → `useRouter().push('/analyses/[id]')`. Decisions recorded: destination route is `/analyses/[id]` (mirrors the API resource); to avoid a 404 this unit adds a **temporary placeholder** `app/analyses/[id]/page.tsx` (static confirmation only — no polling/report render), explicitly a Unit 09 stand-in. Loading (disabled inputs + spinner), inline field errors from Zod issues, and a form-level API/network error panel specified. New UI primitives: shadcn `Input`/`Textarea`/`Label`/`Select` (peer deps `@radix-ui/react-label`, `@radix-ui/react-select`) — the "expected components" Unit 02 deferred to the rendering unit. No form library (plain React state). No schema/worker/queue/provider/LLM/scoring/API-route changes; no auth/reports-list/share/export.
- 2026-07-08: Implemented and verified Unit 07 (Worker Process Skeleton and Placeholder Report). `apps/worker` is now a pg-boss consumer that drives the job lifecycle and upserts a placeholder `Report` (first report write). Offline + full online E2E (throwaway local Postgres: POST → worker → COMPLETED + valid placeholder report), plus idempotency and missing-record safety, all verified; `pnpm build` green. Prisma schema untouched. `next-env.d.ts` churn reverted. No commits made.
- 2026-07-08: Created `context/specs/07-worker-skeleton-placeholder-report.md` per explicit request (spec only, not implemented). Scopes turning `apps/worker` into a standalone pg-boss consumer: `boss.work(analysis.run, handler)` via `@kol-fit/queue`, iterating pg-boss's batch array (v12 delivers `Job[]`), validating the payload, loading `AnalysisJob`+`request`, driving QUEUED→RUNNING→COMPLETED (and →FAILED on error, `errorCode="worker_error"`, no secret leak), and upserting a placeholder `Report` (first report write). Decisions recorded: idempotency via `Report.requestId @unique` upsert + a `status==="COMPLETED"` short-circuit (prevents duplicate reports on retry/redelivery); placeholder is a **valid** `FitReport` (validated with `FitReportSchema`) clearly marked (overallScore 0, confidence low, `evidence.notes` placeholder string); missing/invalid records are logged + ack'd, never thrown; per-job try/catch isolates batch failures and acks (no retry storm — real retry policy is Unit 21); worker loads the repo-root `.env` via a first-imported `env.ts` (`dotenv`) because a standalone Node process doesn't inherit Next's env loading, and Prisma reads `DATABASE_URL` at import; **no Prisma schema change** (`Report.requestId` unique + `AnalysisJob` timestamps already exist). New deps at implementation: `@kol-fit/db` + `@kol-fit/queue` (workspace) + `dotenv` on `apps/worker`. No new env vars (reuses `DATABASE_URL` + `DIRECT_URL`). No provider/LLM/scoring/pipeline/ingestion/UI/API-route changes.
- 2026-07-08: Implemented and verified Unit 06 (pg-boss Queue Setup). New `packages/queue` + `POST /api/analyses` enqueue wiring. Offline + online (throwaway local Postgres) verification pass, including the enqueue-failure safeguard; `pnpm build` green. pg-boss 12 ESM-only handled via dynamic import from the CJS package. Prisma schema untouched. `architecture.md` updated to add `packages/queue`. `next-env.d.ts` dev/build churn reverted. No commits made.
- 2026-07-08: Created `context/specs/06-pg-boss-queue-setup.md` per explicit request (spec only, not implemented). Scopes pg-boss queue infra in a new `packages/queue` package (`@kol-fit/queue`: `QUEUE_NAMES.ANALYSIS_RUN` constant, `AnalysisRunPayloadSchema`, a lazy cached pg-boss singleton, and `enqueueAnalysisRun`), plus updating `POST /api/analyses` to enqueue `analysis.run` after the DB write and store the id on `AnalysisJob.pgBossJobId`. Decisions recorded: new package (not inline / not in `packages/db`) to isolate the queue boundary; pg-boss uses the **direct** connection (`DIRECT_URL`, not the PgBouncer-pooled URL) because of LISTEN/NOTIFY + advisory locks; lazy init so it never connects at import/build; pg-boss auto-creates its own `pgboss` schema/tables on first `start()` (not Prisma-managed, no migration); **no Prisma schema change** (`pgBossJobId` already exists); enqueue-failure marks the job `FAILED`/`errorCode="enqueue_failed"` to avoid silent orphans; offline path still fails cleanly with `internal_error`. New deps at implementation: `pg-boss` + `zod` in `packages/queue`; `apps/web` gains `@kol-fit/queue`. No new env var (reuses `DIRECT_URL`). No worker processor / report / provider / LLM / scoring / UI. `architecture.md` `packages/queue` doc-sync recorded as an open item.
- 2026-07-08: Implemented and verified Unit 05 (Analysis Request API). `POST /api/analyses` in `apps/web`. Both offline (400/400/500/405 via dev server) and online (201 + full row assertions against a throwaway local Postgres 14 stood up in the scratchpad, then torn down) verification pass; `pnpm build` green. No `serverExternalPackages` change needed. Prisma schema untouched. `next-env.d.ts` dev/build churn was reverted to keep the diff clean. No commits made.
- 2026-07-08: Created `context/specs/05-analysis-request-api.md` per explicit request (spec only, not implemented). Scopes the first API route `POST /api/analyses` in `apps/web`: validate with the shared `AnalysisRequestInputSchema`, atomically create `AnalysisRequest` + `AnalysisJob` (QUEUED) via a Prisma nested create, return `ApiResponse<{id,jobId,status,createdAt}>` (201) or `err()` (400 validation / 500 internal). Decisions recorded in the spec: no `Report` row yet (first report save is Unit 07; returned `id`=requestId is the stable polling key), no pg-boss enqueue (Unit 06), `workspaceId` null (no seed), and **no Prisma schema change** (Unit 03 schema already supports it). Offline-safe verification path defined (build + dev-server curl for 400 validation and graceful 500 when no DB); online path (201 + row assertions) requires a DB with tables applied (`prisma migrate dev`/`db push`, since no migration exists yet). New workspace dep at implementation: `apps/web` gains `@kol-fit/db`; possible `serverExternalPackages` build-compat tweak. No worker/provider/LLM/scoring/pipeline logic.
- 2026-07-08: Implemented and verified Unit 04 (Shared Schemas and Types) in `packages/shared`. Zod resolved to 4.4.3; adapted to Zod 4 APIs (`z.url()`, `z.partialRecord`). All spec verification passes (build, 14-assertion parse smoke test, enum-drift check vs Prisma, caps check, request-column parity). Kept `packages/shared` a dependency-free leaf (mirrors Prisma enum values rather than importing `@kol-fit/db`). No commits made.
- 2026-07-08: Created `context/specs/04-shared-schemas-and-types.md` per explicit request (spec only, not implemented). Scopes shared Zod schemas + inferred types in `packages/shared`: analysis request input (mirrors `AnalysisRequest` columns), 15-section fit report output, 9-metric score schema (no weights — those stay in `packages/scoring`), 15 audience buckets + distribution, status/verdict/engagement-source enums mirrored from the Unit 03 Prisma enums (shared stays a dependency-free leaf — no import of `@kol-fit/db`; DB remains the source of truth, drift check in verification), provider-neutral Twitter/X normalized types, provider-neutral LLM structured-output types, `ANALYSIS_CAPS` constants, and `ApiResponse`/error-code shapes with `ok()`/`err()` helpers. New dep at implementation: `zod` (in `packages/shared`). Proposed a small per-concern module layout under `src/` with a barrel `index.ts` (keeps `APP_NAME` importable). No DB/API/queue/worker/provider/LLM/scoring logic.
- 2026-07-08: Implemented and verified Unit 03 (Prisma Database Schema) in `packages/db`. Offline verification passes (`prisma validate`/`format`/`generate`, runtime export smoke test, `pnpm build`); live-DB steps (`migrate dev`, connection test) deferred pending Supabase credentials — no migration generated yet. Resolved Prisma 7.8.0 forced setup adaptations (config-file datasource, `prisma-client` generator with cjs output, driver adapter) — see Architecture Decisions. `next-env.d.ts` shows an unrelated Next-managed churn line (dev↔build routes-types path) from running `next build`. No commits made.
- 2026-07-08: Created `context/specs/03-prisma-database-schema.md` per explicit request (spec only, not implemented). Scopes Prisma setup in `packages/db`, the initial Supabase Postgres schema, client generation, and DB package exports. Models: `Workspace`, `AnalysisRequest`, `AnalysisJob`, `OrgProfile`, `KolProfile`, `Report`, `ReportEvidence`, optional `EngagedAccountSample`, `ProviderUsageLog`. Enums: `JobStatus`, `ReportStatus`, `ReportVerdict`, `EngagementSource`. Workspace-ready (nullable `workspaceId` on request + report), reports persisted from day one, deterministic `scores` kept separate from LLM `report` JSON, no raw-payload columns, indexes for workspace/history + job status + org/KOL handle + `createdAt`. New deps at implementation: `prisma`, `@prisma/client`, `dotenv-cli` (root-`.env` loading for the Prisma CLI). No new env vars (`DATABASE_URL`/`DIRECT_URL` already in `.env.example`). No queue/worker/provider/LLM/scoring/API/UI logic. Reconciliation with `architecture.md` model names recorded as an open item above.

- 2026-07-18: Extended provider + classification cache TTLs to 30 days
  (`CACHE_TTL_SECONDS`, `CACHE_TTL_TWEETS_SECONDS`, `CACHE_TTL_PROFILE_SECONDS`,
  `CLASSIFICATION_CACHE_TTL_SECONDS` = 2592000) in `.env` and documented the
  rationale + trade-off in `.env.example`. Motivation: repeat analyses of the
  same creator serve from the DB `ProviderCache`/classification cache instead of
  re-fetching TwitterAPI.io + re-calling OpenAI, cutting the 5-7 min wait to
  seconds on a cache hit. Trade-off documented: an analysis reflects a snapshot
  up to 30 days old. Reviewed the `ProviderCache` schema for the fetch/store
  path: it is already optimal — PK point lookup on `key` (`findUnique`), `Json`
  payload (Prisma maps to Postgres `jsonb`, binary/fast), `@@index([expiresAt])`
  for lazy cleanup. No schema change made (none warranted). Noted but did NOT
  build: result-level reuse (short-circuit a repeat of the same brand×creator
  pair to an existing recent Report) as the only higher-leverage latency win —
  it is a product decision left to the user.

- 2026-07-18: Built the instant-reuse path (Unit 41). Re-submitting the SAME
  brand × creator + brief by the SAME owner now returns the prior COMPLETED
  report immediately instead of re-running the 5-7 min pipeline, when that report
  is newer than a freshness window. Pieces:
  * `packages/shared/src/reuse.ts` — pure `resolveReuseWindowSeconds(env)`
    (default 30d = 2592000; `0` disables; invalid/blank → default), mirrors the
    resolveAbuseLimits idiom. Exported from the shared index.
  * `apps/web/app/api/analyses/route.ts` — `findReusableAnalysis(ownerId, input,
    window)` (findFirst, newest-first, matches every brief field null-for-null,
    `report: { is: { status: COMPLETED } }`). Short-circuits in POST BEFORE the
    tier gate + rate limit, so reuse never consumes or is blocked by quota and
    calls no external API. Returns `{ ...ids, reused: true }` (HTTP 200).
  * Schema: added `@@index([ownerId, orgHandle, kolHandle, createdAt])` on
    AnalysisRequest; applied to live Supabase via `prisma db push` (single
    non-destructive CREATE INDEX; `migrate diff` now reports no drift).
  * Env: `ANALYSIS_REUSE_WINDOW_SECONDS=2592000` in `.env`, documented in
    `.env.example`.
  * Tests: `scripts/checks/analysis-reuse.regression.cjs` (11 assertions) wired
    into `pnpm check` + `check:analysis-reuse`. Full suite green; `pnpm -r build`
    green. Verified the Prisma query against real rows (exact HIT with correct
    id; changed-brief MISS; stale-window MISS; wrong-owner MISS = no leak).
  * Client needs no change: the detail page already renders a COMPLETED report
    immediately, so navigating to the reused id shows the finished report; its
    "Generated <date>" line is the honest signal that it's a prior run.
  * Not built (offered): a force-refresh bypass (`?fresh=1`) and in-flight
    duplicate dedup — left as follow-ups.

- 2026-07-18: Cleanup sweep (dead code / envs / stale docs). Audited the repo —
  found NO orphan source files, NO unused dependencies, NO unused env vars in
  .env.example (all 56 consumed), NO dead code files. Everything removed was
  residue from the Google-only auth migration:
  * `.env`: removed the dead `AUTH_SESSION_SECRET` and `# AUTH_DEV_LOGIN=true`
    (dev login was deleted; both unreferenced in source and already absent from
    .env.example). Rewrote the auth-section comment to describe Google-only.
  * Stale comments corrected (now factually accurate): `auth/callback/route.ts`
    ("NOT YET LIVE-VERIFIED / dev logs in via POST /api/auth/session" → sole
    Google sign-in path), `middleware.ts` + `lib/auth/supabase.ts` (dropped the
    "NOT YET LIVE-VERIFIED / cannot run locally" markers — verified live 2026-07-18),
    `packages/auth/{index,types,mode}.ts` ("only node:crypto" → no runtime deps;
    "dev email login" → anonymous-only).
  * `context/architecture.md`: rewrote the auth section — @kol-fit/auth now only
    owns `resolveAuthMode` + types (removed the deleted `signSessionToken`/
    `verifySessionToken`/`devLoginAllowed` descriptions); dev mode = anonymous-only;
    supabase mode = live-verified Google sign-in.
  * Left untouched by design: append-only history (progress-tracker), historical
    spec `context/specs/28-*.md`, generated/`.next` artifacts.
  * Verified: auth pkg build OK, web typecheck OK, auth regression 5/5 green,
    no stale markers remain in source.

- 2026-07-18: Removed report delivery entirely (the Unit 24 email-a-PDF feature).
  Investigation found it was already unreachable dead code — the report-page
  email-capture UI was retired in Unit 39.1, so nothing called `/deliver`; the
  `ReportDelivery` table was empty (0 rows, verified), so no data was lost.
  Removed: `packages/mail` + `packages/report-pdf` (whole packages; 14→12
  workspace projects), the worker `report-deliver` handler + its registration,
  `POST /api/analyses/[id]/deliver`, the pg-boss `report.deliver` job
  (`enqueueReportDeliver`, `ReportDeliverPayload`, `REPORT_DELIVER` constant +
  its `createQueue` in boss.ts), `ReportDeliverInputSchema`/`ReportDeliverInput`
  (shared), the `ReportDelivery` model + `DeliveryStatus` enum + `Report.deliveries`
  relation (dropped from live Supabase via `prisma db push` — DropTable/DropEnum),
  the admin legacy-leads surface (`listAdminLeads`, `AdminLeadsTable`,
  `AdminLeadRow`, `DeliveryStatus` admin type, `DeliveryPill`, the 3 overview lead
  StatCards + the `leads` overview stat/queries), the MAIL_* env vars (.env +
  .env.example), and `report-delivery.regression.cjs` (+ its check wiring).
  Kept intact: the detailed-report concierge (Unit 35) — `/detailed`,
  `DetailedReportRequest`, the admin `/admin/leads` queue + nav badge — which is
  the current lead-capture funnel. Docs updated: architecture.md (Unit 24 section
  → "removed" note; admin read-list; owner call-sites), DEPLOY.md (dropped the
  MAIL env row). Verified: `pnpm -r build` green, full `pnpm check` 413/0,
  zero dangling references to mail/delivery in source.

- 2026-07-18: Report UI/UX pass on `FitReportView` (3 commits, `apps/web/components/report/`),
  addressing a scannability review of the finished report page. No data-model or
  scoring changes; weights stay internal (Unit 33) — the additions convey
  importance/direction, never percentages.
  * Reconcile the headline number: an "Okay fit / 55" sat next to a wall of 100s
    in the breakdown and read as inconsistent. Added a "Lifted by / Held back by"
    drivers strip inside the hero (directly under the score, derived from the
    existing `computeDrivers` values), plus a "weighted by impact — metrics nearer
    the top move the overall most" caption on the fit-metric list.
  * Removed the now-redundant standalone "Why" panel (its glance value moved into
    the hero; the detail lives in the breakdown) — tighter page, less repetition.
  * Risk & safety cards: added per-card direction labels ("Lower is better" for
    risk, "Higher is better" for brand safety) so a green 9 and a green 100 no
    longer read as inconsistent.
  * Moved the concierge CTA from the #2 slot (interrupting before any value) to
    the end of the report body, and captioned the audience donut so a large grey
    "Other" wedge isn't misread as a finding.
  * Verified in light + dark on a live report; web typecheck clean.

- 2026-07-18: Mobile fix — top nav was overflowing the viewport on phones.
  At a real 390px width the page scrolled sideways (scrollWidth 447), which
  made the whole app feel broken on mobile. Root cause was the top nav, not
  the report: "Analyze" / "History" / "Sign out" all showed text labels, so
  logo + nav couldn't coexist in the width. Fixed by collapsing the nav to
  icon-only below `sm` (labels kept for a11y via `sr-only` + `sm:not-sr-only`)
  and tightening header/main padding to `px-4` on mobile (`top-nav.tsx`,
  `auth/user-menu.tsx`, `app-shell.tsx`). Verified at 390px by loading each
  route in an isolated iframe (its own viewport → true breakpoints, since the
  dev window can't be resized below ~1462px here): report page overflow gone
  and every section reads cleanly; Home/Analyze already clean; the History
  table is intentionally swipe-scrollable inside its `overflow-x-auto` wrapper.

- 2026-07-18: Build hygiene, stopped tracking `apps/web/next-env.d.ts`.
  The file was showing up dirty after every session. Cause: Next 16 typed
  routes rewrites its import line on each run, pointing at
  `.next/dev/types/routes.d.ts` after `next dev` and `.next/types/routes.d.ts`
  after `next build`, so the file flipped whenever we switched between the two
  (8 commits of churn). It is fully generated and regenerated at the start of
  every dev/build, and `create-next-app` ignores it by default, so it is now in
  `.gitignore` and untracked. Verified by deleting it, running `pnpm -r build`
  (green, file regenerated with the build path) and `pnpm check` (exit 0).
  No source or config changes; `tsconfig.json` still lists it under `include`,
  which is a glob and does not error when the file is absent.

- 2026-07-18: Dead-code / stale-docs audit. Swept the repo for unused code,
  unused dependencies, and docs describing removed features.
  **Found no dead code and no unused dependencies** — an import-graph walk from
  55 entrypoints reached all 183 source files (the only unreachable files are
  `next.config.mjs`, `postcss.config.mjs`, `prisma.config.ts`, which are loaded
  by tooling, not imported). Two near-misses worth recording, since both would
  have broken production if deleted on a naive scan:
  `apps/worker/src/env.ts` is reached only through a **bare side-effect import**
  (`import "./env.js"` in `index.ts`, no `from` clause) and loads `.env` before
  Prisma/pg-boss read `process.env`; and `@supabase/supabase-js` is never
  imported directly but is a **peer dependency** of `@supabase/ssr` (pnpm:
  `@supabase+ssr@0.7.0_@supabase+supabase-js@2.110.3`), so dropping it breaks
  the install. `tw-animate-css` is likewise used from `globals.css`, not TS.
  **The staleness was all in docs.** Since CLAUDE.md directs every session to
  read `context/` before implementing, specs describing removed features read as
  current design. Annotated (not deleted — they are the record of *why* those
  features died): `24-report-delivery-lead-capture.md` (email/PDF delivery,
  removed `834ef06`), `29e-calibration-harness.md` (removed `73d379d`), and
  `28-user-authentication.md` (partly superseded: sign-in is Google-only as of
  `adc7566`, cleanup in `834ef06`; core anonymous-then-claim flow still live).
  Also dropped the `.gitignore` line for `scripts/calibration/last-run.json`
  (directory removed with the harness) and filled in the previously 19-byte
  `README.md`. Correction to an earlier claim in this same audit: `AGENTS.md`
  was reported as out of sync with `CLAUDE.md`, but it already lists all 7
  context files — that was a truncated-read artifact, and no edit was made.
  Verified: `pnpm -r build` green, `pnpm check` exit 0.

- 2026-07-18: Deploy target decided — **single VPS (RackNerd), single database**.
  User is deploying tomorrow and chose one Supabase project for production
  rather than a separate prod project. That decision also defuses a deploy
  blocker found while auditing: `DEPLOY.md` instructed
  `prisma migrate deploy`, but this project has **no migration history** at all
  (`packages/db/prisma/` holds only `schema.prisma`; every schema change to date
  went through `prisma db push`). On a *fresh* database that command is a no-op,
  so the documented path would have produced an empty schema and a dead app.
  Reusing the existing DB sidesteps it, and the checklist now says `db push`
  explicitly, noting `db:deploy` stays unused until a baseline migration exists.
  Added a **"Deploying on a VPS (systemd)"** section to `DEPLOY.md`: host setup
  (Node 20 via NodeSource, global pnpm@9, non-root `overlapx` user), the unit
  file, reverse proxy + TLS, ufw, and the redeploy loop. The reason it is needed:
  `scripts/start.mjs` fail-fast (either child dies -> whole process exits) is
  correct *only* when something restarts it; managed platforms do, a bare VPS
  does not, so a single worker crash would otherwise take the app down until
  someone SSHed in. Two details that would silently break the unit are called
  out inline: systemd's near-empty PATH (start.mjs spawns `pnpm --filter web
  start`, so pnpm must resolve) and `StartLimitBurst`/`StartLimitIntervalSec`
  belonging in `[Unit]`, not `[Service]`, since systemd v229. Also documented
  purging dev/test rows before go-live, and that cached provider data is safe to
  keep (keys namespaced by provider kind, `tw:v2:<kind>:`). Docs only, no code
  change.

- 2026-07-18: Reconciled this file's own `## Next Up` and `## Open Questions`.
  Both had drifted into pointing at finished work, which matters because
  CLAUDE.md sends every session here for "current phase, open questions, and
  next steps". `Next Up` still described Unit 19 (caching) as "implementation
  not started" though `packages/cache` ships and the `ProviderCache` table
  exists; it now lists the VPS deploy and Unit 41's 2 remaining items. Four open
  questions were verified resolved and moved to a "Recently resolved" subsection
  rather than deleted: calibration pairs (moot, 29E removed), live-DB
  verification (Supabase live since 2026-07-14), Unit 19 audience sampling (the
  first-N slice is gone — `provider.ts:287` now calls `sampleAudienceAccounts`,
  proportional by engagement source), and the `LLM_MODEL` model-choice question
  (model chosen, cheap tier via `LLM_MODEL_FAST`). Two were checked and **kept
  because they are still true**: the `architecture.md` naming drift
  (`OrgSnapshot`/`AnalysisReport` etc. at lines 186-191 vs the real
  `OrgProfile`/`Report`) and the missing baseline migration. The post-MVP
  roadmap entry was updated to mark the delivered items. Also confirmed
  `Current Phase`'s "mock remains the default provider" claim is still accurate
  (both `.env.example` and the two factories default to `mock`) — which is a
  deploy footgun worth remembering: unset `TWITTER_PROVIDER`/`LLM_PROVIDER` in
  production means live traffic silently runs on mock data. Docs only.
